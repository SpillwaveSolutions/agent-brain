# v9.6.0 Runtime Support Parity & Backlog Cleanup

Date: 2026-03-31

## Goal

Create the next milestone around runtime parity for Codex, OpenCode, and Gemini,
with project-local end-to-end installation tests that do not mutate the user's
global environment.

## Scope

1. Verify runtime support truthfully across code, docs, and planning artifacts.
2. Add or strengthen end-to-end tests that:
   - install into isolated project integration folders, never global locations
   - verify the generated install artifacts exist after install
   - invoke the target code-agent CLI headlessly from the integration folder
   - pass a prompt that exercises an installed Agent Brain skill or setup flow
   - assert on structured JSON status from the CLI interaction
3. Cover each supported code-agent runtime in parity scope:
   - Codex
   - OpenCode
   - Gemini
4. Clean up stale runtime-related backlog/todo items that no longer reflect the
   shipped state.

## Proposed Milestone Shape

- Milestone: v9.6.0 Runtime Support Parity & Backlog Cleanup
- Numbering: continue phase numbering after Phase 45
- Research: skip for now; scope is concrete and implementation-led

## Planned Outputs

- `.planning/PROJECT.md` updated for v9.6.0
- `.planning/STATE.md` reset for the new milestone
- `.planning/REQUIREMENTS.md` created for runtime parity work
- `.planning/ROADMAP.md` extended with new phases for v9.6.0

## Notes

- Existing evidence already shows Codex support is implemented in source and
  tests; the milestone should treat this as parity hardening and verification,
  not first-time Codex support.
- The worktree is currently dirty, so any later commit decision should avoid
  unrelated files.
