# Agent Brain

## What This Is

Agent Brain is a local-first RAG (Retrieval-Augmented Generation) service that indexes documentation and source code, providing intelligent semantic search via REST API for CLI tools and Claude Code integration. It combines vector search, BM25 keyword search, GraphRAG with schema-based entity types, two-stage reranking, and hybrid retrieval strategies — all with pluggable provider support for fully offline or cloud-backed operation.

## Core Value

**Developers can semantically search their entire codebase and documentation through a single, fast, local-first API that understands code structure and relationships.**

## Requirements

### Validated

- ✓ **CORE-01**: Document ingestion (PDF + Markdown) — v1.0
- ✓ **CORE-02**: Context-enriched chunking with Claude summarization — v1.0
- ✓ **CORE-03**: Vector search with ChromaDB + OpenAI embeddings — v1.0
- ✓ **CORE-04**: REST API (/query, /index, /health) — v1.0
- ✓ **CORE-05**: CLI tool (agent-brain) with status, query, index, reset — v1.0
- ✓ **BM25-01**: BM25 keyword search retriever — v2.0 (Feature 100)
- ✓ **BM25-02**: Hybrid retrieval with RRF fusion — v2.0 (Feature 100)
- ✓ **BM25-03**: Retrieval mode selection (vector/bm25/hybrid) — v2.0 (Feature 100)
- ✓ **CODE-01**: AST-aware code ingestion (10 languages) — v2.0 (Feature 101)
- ✓ **CODE-02**: Code summaries via SummaryExtractor — v2.0 (Feature 101)
- ✓ **CODE-03**: C# language support — v2.0 (Feature 110)
- ✓ **MULTI-01**: Per-project server instances with isolation — v2.0 (Feature 109)
- ✓ **MULTI-02**: Auto-port allocation — v2.0 (Feature 109)
- ✓ **MULTI-03**: Runtime discovery via runtime.json — v2.0 (Feature 109)
- ✓ **GRAPH-01**: Knowledge graph extraction (entities + relationships) — v2.0 (Feature 113)
- ✓ **GRAPH-02**: Graph-enhanced retrieval mode — v2.0 (Feature 113)
- ✓ **GRAPH-03**: Multi-mode query (vector/bm25/graph/hybrid/multi) — v2.0 (Feature 113)
- ✓ **PLUGIN-01**: Claude Code plugin with slash commands — v2.0 (Feature 114)
- ✓ **PLUGIN-02**: Specialized agents (researcher, indexer) — v2.0 (Feature 114)
- ✓ **QUEUE-01**: Server-side job queue with JSONL persistence — v2.0 (Feature 115)
- ✓ **RERANK-01**: Two-stage retrieval with optional reranking — v3.0 (Feature 123)
- ✓ **RERANK-02**: Ollama-based reranker (local-first, no API keys) — v3.0 (Feature 123)
- ✓ **RERANK-03**: Graceful degradation on reranker failure — v3.0 (Feature 123)
- ✓ **RERANK-04**: Reranking adds <100ms latency for top 100 candidates — v3.0 (Feature 123)
- ✓ **RERANK-05**: Configuration via env vars — v3.0 (Feature 123)
- ✓ **PROV-01**: YAML configuration for embedding providers — v3.0 (Feature 103)
- ✓ **PROV-02**: YAML configuration for summarization providers — v3.0 (Feature 103)
- ✓ **PROV-03**: Provider switching via config only — v3.0 (Feature 103)
- ✓ **PROV-04**: Fully offline operation with Ollama — v3.0 (Feature 103)
- ✓ **PROV-05**: API keys from environment variables — v3.0 (Feature 103)
- ✓ **PROV-06**: Provider config validated on startup — v3.0 (Feature 103)
- ✓ **PROV-07**: Embedding dimension mismatch prevention — v3.0 (Feature 103)
- ✓ **SCHEMA-01**: Domain-specific entity types (17 types across Code/Docs/Infra) — v3.0 (Feature 122)
- ✓ **SCHEMA-02**: Documentation entity types — v3.0 (Feature 122)
- ✓ **SCHEMA-03**: Enhanced relationship predicates (8 predicates) — v3.0 (Feature 122)
- ✓ **SCHEMA-04**: Entity type filtering in graph queries — v3.0 (Feature 122)
- ✓ **SCHEMA-05**: LLM extraction prompts use schema vocabulary — v3.0 (Feature 122)
- ✓ **TEST-01**: E2E test suite for OpenAI provider — v3.0 (Feature 124)
- ✓ **TEST-02**: E2E test suite for Anthropic provider — v3.0 (Feature 124)
- ✓ **TEST-03**: E2E test suite for Ollama provider — v3.0 (Feature 124)
- ✓ **TEST-04**: E2E test suite for Cohere provider — v3.0 (Feature 124)
- ✓ **TEST-05**: Provider health check endpoint — v3.0 (Feature 124)
- ✓ **TEST-06**: Verified provider configuration documentation — v3.0 (Feature 124)

- ✓ **STOR-01**: Storage abstraction protocol (11 methods) — v6.0
- ✓ **STOR-02**: Backend factory with env/YAML/default selection — v6.0
- ✓ **STOR-03**: ChromaDB backend wraps existing vector_store/bm25_manager — v6.0
- ✓ **STOR-04**: Contract tests for backend protocol compliance — v6.0
- ✓ **STOR-05**: Legacy parameter backward compatibility — v6.0
- ✓ **PGVEC-01**: pgvector extension for vector similarity search — v6.0
- ✓ **PGVEC-02**: Cosine, L2, inner product distance metrics — v6.0
- ✓ **PGVEC-03**: HNSW index with configurable m/ef_construction — v6.0
- ✓ **PGVEC-04**: Embedding dimension validation — v6.0
- ✓ **PGFTS-01**: tsvector full-text search with GIN index — v6.0
- ✓ **PGFTS-02**: Weighted relevance (A/B/C) for title/summary/content — v6.0
- ✓ **PGFTS-03**: Configurable language for stemming — v6.0
- ✓ **PGFTS-04**: RRF hybrid fusion for vector + keyword results — v6.0
- ✓ **INFRA-01**: Docker Compose for pgvector:pg16 development setup — v6.0
- ✓ **INFRA-02**: Async connection pooling with SQLAlchemy — v6.0
- ✓ **INFRA-03**: `/health/postgres` endpoint with pool metrics — v6.0
- ✓ **INFRA-04**: Auto schema initialization on backend startup — v6.0
- ✓ **INFRA-05**: Poetry extras for optional PostgreSQL dependencies — v6.0
- ✓ **CONF-01**: YAML storage.backend + storage.postgres configuration — v6.0
- ✓ **CONF-02**: Connection params (host, port, pool size, HNSW) — v6.0
- ✓ **CONF-03**: DATABASE_URL env var override — v6.0
- ✓ **V6TEST-01**: Contract tests with pytest markers + skip-without-DB — v6.0
- ✓ **V6TEST-02**: CI PostgreSQL service container in GitHub Actions — v6.0
- ✓ **V6TEST-03**: Backend wiring smoke tests (mock-based) — v6.0
- ✓ **V6TEST-04**: Service-level PostgreSQL E2E tests — v6.0
- ✓ **PLUG-01**: `/agent-brain-config` command for backend selection — v6.0
- ✓ **PLUG-02**: YAML generation for PostgreSQL config — v6.0
- ✓ **PLUG-03**: `/agent-brain-setup` with Docker detection — v6.0
- ✓ **PLUG-04**: PostgreSQL error pattern recognition in setup agent — v6.0
- ✓ **PLUG-05**: docker-compose.postgres.yml template — v6.0
- ✓ **PLUG-06**: Plugin version bump to v5.0.0 — v6.0
- ✓ **DOCS-01**: PostgreSQL setup guide — v6.0
- ✓ **DOCS-02**: Full configuration reference — v6.0
- ✓ **DOCS-03**: ChromaDB vs PostgreSQL performance tradeoffs guide — v6.0
- ✓ **PLUG-07**: Port auto-discovery for PostgreSQL (5432-5442 range) — v6.0.4
- ✓ **PLUG-08**: Plugin version bumped to v6.0.3 — v6.0.4
- ✓ **INFRA-06**: install.sh REPO_ROOT path corrected (doc-serve to agent-brain) — v6.0.4
- ✓ **FOLD-01..10**: Folder management — list, add, remove indexed folders via CLI/API — v7.0
- ✓ **FTYPE-01..07**: File type presets — `--include-type` shorthand for glob patterns — v7.0
- ✓ **INJECT-01..08**: Content injection pipeline — custom scripts and folder-level JSON metadata — v7.0
- ✓ **EVICT-01..10**: Manifest tracking and chunk eviction — incremental indexing with stale chunk removal — v7.0
- ✓ **XCUT-01..06**: Cross-cutting quality (dual-backend, atomic writes, --help, OpenAPI, >70% coverage, before-push) — v7.0
- ✓ **VAL-05**: v2 design doc filed at `docs/plans/2026-06-02-mcp-v2-subscriptions.md` — v10.2 (Phase 50)
- ✓ **(prereq for URI-01)**: `GET /query/chunk/{chunk_id}` endpoint with O(1) lookup, ChromaDB + Postgres impls — v10.2 (Phase 50)
- ✓ **(prereq for URI-02)**: `GET /graph/entity/{type}/{id}` endpoint with Kuzu + Simple impls, `#178` SIGSEGV 503 fallback — v10.2 (Phase 50)
- ✓ **(prereq for URI-04)**: `agent_brain_server/security/file_sandbox.py` — hard whitelist policy with 4 deny reasons, 10 MiB cap — v10.2 (Phase 50)
- ✓ **URI-01**: `chunk://<chunk_id>` resource readable via MCP `resources/read` — v10.2 (Phase 51, Plan 02)
- ✓ **URI-02**: `graph-entity://<type>/<id>` resource readable via MCP `resources/read`; 503 `kuzu_unavailable` fallback wired — v10.2 (Phase 51, Plan 02)
- ✓ **URI-03**: `job://<job_id>` resource readable via MCP `resources/read` (foundational dispatcher) — v10.2 (Phase 51, Plan 01)
- ✓ **URI-04**: `file://<abs-path>` resource readable, gated by Phase 50 sandbox helpers (shared, not forked) — v10.2 (Phase 51, Plan 03)
- ✓ **URI-05**: `resources/templates/list` advertises all 4 URI templates as RFC 6570 strings; `MIN_BACKEND_VERSION` bumped to `10.2.0` — v10.2 (Phase 51, Plan 04)
- ✓ **SUB-01**: `job://<job_id>` subscription with 1s polling cadence, auto-cancel on terminal status via `SubscriptionTerminated` sentinel — v10.2 (Phase 52, Plan 03)
- ✓ **SUB-02**: `corpus://status` subscription with 30s polling cadence, `request_id` drop set defeats uvicorn UUID churn — v10.2 (Phase 52, Plan 03)
- ✓ **SUB-03**: `corpus://folders` subscription with configurable 5s active cadence (`mcp_subscription_folders_active_interval_s` setting) — v10.2 (Phase 52, Plan 03)
- ✓ **SUB-04**: `notifications/resources/updated` payload conforms to 2025-03-26 MCP spec; URI-only shape today, `_meta.revision` (64-char hex SHA-256) future-pinned in tests — v10.2 (Phase 52, Plan 04)
- ✓ **SUB-05**: Per-session subscription tracking via `(id(session), uri)` key; cleanup via `run_stdio` try/finally + `cleanup_all()` on EOF; deterministic counter-based e2e test (not psutil) — v10.2 (Phase 52, Plan 04)
- ✓ **HTTP-01**: `agent-brain-mcp --transport http` starts a Streamable HTTP listener via `StreamableHTTPSessionManager` + uvicorn; SDK round-trip drives `streamablehttp_client` and asserts v1 surface symmetry (7 tools / 5 resources / 6 prompts) — v10.2 (Phase 53, Plans 01-03)
- ✓ **HTTP-02**: Loopback-only enforcement {127.0.0.1, localhost, ::1} at CLI entry + in-process; startup banner literal `(loopback only, no auth — do NOT expose this port)`; explicit `security_settings=loopback_transport_security()` because `StreamableHTTPSessionManager` doesn't auto-enable like `FastMCP`; psutil socket-bind verification — v10.2 (Phase 53, Plans 01-03)
- ✓ **HTTP-03**: Explicit selection via `click.Choice`; no silent fallback; `PortInUseError(exit_code=2)` for EADDRINUSE; pre-flight `socket.bind` probe because uvicorn 0.32.x catches OSError as SystemExit(1); CLI hoist of validation precedes `main_async` to avoid BackendUnavailable masking — v10.2 (Phase 53, Plans 01-03)
- ✓ **TOOL-01**: `explain_result` re-runs query with `explain=true` + filters by chunk_id; INVALID_PARAMS when chunk not in top-k; `top_k=50` default per CONTEXT decision F — v10.2 (Phase 54, Plan 02)
- ✓ **TOOL-02**: `add_documents` thin wrapper over `POST /index/add?force=`; #180 `allow_external` deliberately omitted (security guarantee) — v10.2 (Phase 54, Plans 01+03)
- ✓ **TOOL-03**: `inject_documents` wraps `POST /index/` with required injector_script/folder_metadata_file (Pydantic root validator); `Path(...).expanduser().resolve()` matching CLI; tool description names #181 allowlist — v10.2 (Phase 54, Plans 01+03)
- ✓ **TOOL-04**: `wait_for_job` async handler with 1s poll cadence (le=2.0 under MCP spec); `notifications/progress` via `_build_progress_notifier` closure; CancelledError → `cancel_job` cleanup; first `emits_progress=True` ToolSpec; inline poll loop chosen over `SubscriptionManager.start_polling` for request-response shape — v10.2 (Phase 54, Plans 01+04)
- ✓ **TOOL-05**: `list_folders` thin wrapper reusing v1 `client.list_folders()` — v10.2 (Phase 54, Plan 02)
- ✓ **TOOL-06**: `remove_folder` thin wrapper with `confirm: Literal[True]` Pydantic guard; 409 surfaces as BACKEND_CONFLICT (-32000), not INVALID_PARAMS — v10.2 (Phase 54, Plans 01+03)
- ✓ **TOOL-07**: `cache_status` thin wrapper over `GET /index/cache/`; 503-uninitialized surfaces as McpError — v10.2 (Phase 54, Plan 02)
- ✓ **TOOL-08**: `clear_cache` thin wrapper with `confirm: Literal[True]` Pydantic guard — v10.2 (Phase 54, Plans 01+03)
- ✓ **TOOL-09**: `list_file_types` returns vendored `FILE_TYPE_PRESETS` (no HTTP roundtrip); module docstring cites CLI source + Phase 55 parity contract — v10.2 (Phase 54, Plans 01+02)
- ✓ **VAL-01**: 16-tool parameterized contract suite (Layer 1 + Layer 2 share `_tool_matrix.py` SOT with import-time drift guard); 32 SDK assertions over stdio + 6 resource contract assertions — v10.2 (Phase 55, Plan 02)
- ✓ **VAL-02**: Subscription lifecycle E2E (subscribe→notify→unsubscribe for all 3 URIs) + disconnect cleanup via stderr-scrape fallback (debug endpoint absent — issue #194 filed for v10.3+); 4 SDK-driven tests — v10.2 (Phase 55, Plan 03)
- ✓ **VAL-03**: Streamable HTTP contract via `mcp.client.streamable_http.streamablehttp_client` (first repo usage); 6 SDK tests over the real `--transport http` subprocess + free-port allocation; reuses Phase 53's `fake_http_server_module` — v10.2 (Phase 55, Plan 04)
- ✓ **VAL-04**: `agent-brain-mcp` and `agent-brain-uds` folded into root `task before-push` + `task pr-qa-gate` (closes DR-5 from v1 design §14 #5); +60-90s local pre-push cost (162s wall-clock); CHANGELOG `[10.2.0]` entry shipped — v10.2 (Phase 55, Plan 05)
- ✓ **SECURITY-01**: REST API key auth on data routers (`/index`, `/query`, `/graph`, etc.) via `X-API-Key` header + `verify_api_key` FastAPI dep; startup gate refuses non-loopback bind without key (exit 2); `AGENT_BRAIN_API_KEY` propagated CLI → server → MCP backend; `agent-brain init` auto-generates key into `.agent-brain/config.json` (chmod 0o600); 60 new tests — v10.2.1 (issue #179)
- ✓ **DESIGN-V3-01**: v3 design doc filed at `docs/plans/2026-06-05-mcp-v3-cli-via-mcp.md` (323 lines, 7 numbered sections) and linked from `docs/roadmaps/mcp/v3-cli-via-mcp-and-frameworks.md` — v10.3 (Phase 56, Plan 01) — mirrors v10.2 Phase 50 design-first precedent
- ✓ **CLI-MCP-01**: `BackendClient` `@runtime_checkable typing.Protocol` at `agent-brain-cli/agent_brain_cli/client/protocol.py` (131 lines, 15 declared methods/attrs); existing `DocServeClient` structurally satisfies it (`isinstance(...)` True) without source modification — v10.3 (Phase 56, Plan 02)
- ✓ **CLI-MCP-02**: `McpStdioBackend` + `McpHttpBackend` skeletons in `agent-brain-mcp/agent_brain_mcp/client.py` alongside unchanged `ApiClient`; both structurally satisfy `BackendClient`; 24 method bodies (12×2) raise `NotImplementedError("Wired in Phase 57+")` — load-bearing sentinel string for Phase 57 transport-selector tests — v10.3 (Phase 56, Plan 03)
- ✓ **CLI-MCP-03**: `--transport mcp` + `--mcp-transport stdio|http` + `--mcp-url` top-level Click flags wired; `resolve_mcp_transport()` precedence helper; `open_backend(ctx) -> BackendClient` dispatcher (renamed from `open_client`); 20 callsites swapped atomically across 8 command modules; all 3 §3.5 misuse cases produce `exit code 2` with verbatim design-doc wording (incl. `shutil.which("agent-brain-mcp")` precheck for stdio + missing PATH); all 10 remaining `BackendClient` methods wired on both MCP backends per §2.3 mapping (`health`/`status`/`index`/`list_folders`/`delete_folder`/`list_jobs`/`get_job`/`cancel_job`/`cache_status`/`clear_cache`); `reset()` retains `NotImplementedError` with verbatim §3.5 wording (deferred Phase 57+ open decision per design doc §4 risks); v2 strict-schema gotcha surfaced (`_build_index_body` drops 9 CLI-only fields; `confirm: True` extends to `cancel_job` + `remove_folder` beyond `clear_cache`) — v10.3 (Phase 57, Plans 01 + 03)
- ✓ **CLI-MCP-04**: v3 Definition-of-Done anchor — `agent-brain-cli/tests/contract/test_transport_equivalence.py` proves byte-identical JSON output across `--transport uds` and `--transport mcp --mcp-transport stdio` after stripping `elapsed_seconds` + `indexed_at` via `_normalize.strip_volatile_fields`; **REAL** subprocess + **REAL** seeded UDS-backed corpus (NO stub fallback); shared corpus seeder hoisted to `tests/integration/_corpus.py` for Phase 58+ reuse; `_coerce_query_response` translator + late-imported `_parse_query_result` ensure both backends produce identical chunk ordering through the same translation layer — v10.3 (Phase 57, Plan 02)
- ✓ **CLI-MCP-08**: `<state_dir>/mcp.runtime.json` schema (5-field {host, port, pid, started_at, transport} verbatim per design doc §2.4) with `0o600` file permissions; `agent-brain-cli/agent_brain_cli/mcp_runtime.py` shared helpers (read/write/delete/acquire_lock/release_lock/is_listening) reused by both `mcp start`/`mcp stop` AND `McpHttpBackend.__init__` discovery; `LockAcquisitionError` carries verbatim "agent-brain mcp already running on port {port} (pid {pid}); run 'agent-brain mcp stop' first" wording; stale-pid reclamation via `psutil.pid_exists`; Phase 57 placeholder error string REMOVED from config.py and replaced with verbatim §3.5 wording — v10.3 (Phase 58, Plans 01 + 03)
- ✓ **CLI-MCP-09**: `agent-brain mcp start` Click subcommand at `commands/mcp.py` (`@click.group("mcp")` registered at top-level via `cli.add_command`); port allocation with `--port 8765` default + `AGENT_BRAIN_MCP_PORT` env + OS-fallback on `EADDRINUSE`; loopback-only enforcement (mirrors v10.2 HTTP-02); detached `subprocess.Popen(start_new_session=True)` launching `sys.executable -m agent_brain_mcp --transport http`; psutil-verified `is_listening()` poll (100ms interval, 10s default `--start-timeout`) BEFORE writing `mcp.runtime.json`; stdout/stderr piped to `<state_dir>/mcp.{stdout,stderr}.log`; lock acquired BEFORE Popen for atomic semantics — v10.3 (Phase 58, Plan 02)
- ✓ **CLI-MCP-10**: `agent-brain mcp stop` Click subcommand with `os.killpg(os.getpgid(pid), SIGTERM)` (process-group termination catches child processes); `--grace` polling (default 5s, `AGENT_BRAIN_MCP_STOP_GRACE` env); `os.killpg(..., SIGKILL)` escalation after grace expires; `delete_mcp_runtime` + `release_lock` cleanup on clean exit; idempotent (no-op exit 0 if not running); end-to-end integration test at `agent-brain-cli/tests/integration/test_mcp_helper_commands.py` proves the full `start → query without --mcp-url → stop` cycle works — v10.3 (Phase 58, Plan 03). **Pre-existing latent bug surfaced (not a Phase 58 regression):** `query.py`'s `--url` Click option has `envvar="AGENT_BRAIN_URL"` which silently routes around `--transport mcp` when the env var is set. Integration test hardens its env by stripping `AGENT_BRAIN_URL` + `AGENT_BRAIN_TRANSPORT`. Phase 60 (subprocess hygiene + env allowlist) is the natural fix point.
- ✓ **CLI-MCP-05**: `McpBackend` `@runtime_checkable` Protocol at `agent-brain-cli/agent_brain_cli/client/protocol.py` (lines 131-170, 5 method signatures) **separate from** `BackendClient`; load-bearing architectural pin verified (`DocServeClient ⊄ McpBackend` negative test at `test_mcp_backend_protocol_skeleton.py:57`); 10 method bodies wired via Pattern A (`asyncio.run(self._async_xxx(...))`) on both `McpStdioBackend` + `McpHttpBackend`; `open_mcp_backend(ctx)` factory at `transport.py` enforces `--transport mcp` via `click.UsageError`; `agent-brain prompt <name> [--arg K=V]... [--json]` command with unknown-name handler that surfaces server `McpError` + `list_prompts()` fallback (exit 2, sorted available names); `McpBackend` deliberately does NOT declare `__enter__`/`__exit__` (Pattern A: fresh client per call) — v10.3 (Phase 59, Plans 01 + 02)
- ✓ **CLI-MCP-06**: `agent-brain resources list` enumerates all 5 static URIs + 4 templated URI schemes (`chunk://`, `graph-entity://`, `job://`, `file://`) with mime types; `commands/resources.py:72-123` calls both `backend.list_resources()` + `backend.list_resource_templates()` and merges into sorted `rich.Table` — v10.3 (Phase 59, Plan 03)
- ✓ **CLI-MCP-07**: `agent-brain resources read <uri>` content-type dispatch matrix at `commands/resources.py:232-264` — `_JSON_MIME_LITERALS` → pretty-print, `_TEXT_MIME_PREFIXES` → passthrough, binary blob without `--output-file` → `click.UsageError("Resource is binary ...; pass --output-file PATH to save")` exit 2; SERVER-side `file://` sandbox enforcement preserved (CLI surfaces server's `outside_indexed_roots` verdict verbatim — no CLI pre-check; `outside_indexed_roots` grep count in `resources.py` = 0); 20 CliRunner tests + 3 e2e integration tests reusing Phase 57's `_corpus.py` seeder (real subprocess, no mocks) — v10.3 (Phase 59, Plan 03)
- ✓ **MCPHYG-01**: MCP stdio subprocess hygiene — `DEFAULT_ENV_ALLOWLIST` (drops `OPENAI_API_KEY`/`ANTHROPIC_API_KEY` unless opted in via `forward_env`; `AGENT_BRAIN_API_KEY` auto-forwarded per SECURITY-01), pinned cwd snapshot, `_hygienic_stdio_client` per-call wrapper, `close()` SIGTERM→grace→SIGKILL escalation — v10.3 (Phase 60, Plans 01-02)
- ✓ **MCPHYG-02**: 1000-invocation no-orphan stress test (`@pytest.mark.stress`, psutil children-delta primary assert + pgrep diagnostic) gated behind opt-in `task mcp:stress:orphan-test` (NOT in before-push) — v10.3 (Phase 60, Plan 03)
- ✓ **FRAME-01**: OpenAI Agents SDK smoke test over BOTH `MCPServerStdio` + `MCPServerStreamableHttp` (isolated pinned venv) — v10.3 (Phase 61, Plans 01-02)
- ✓ **FRAME-02..03**: LangChain (`langchain-mcp-adapters`) + LlamaIndex (`llama-index-tools-mcp`) stdio smoke tests — v10.3 (Phase 61, Plan 03)
- ✓ **FRAME-04..05**: Pydantic AI (`MCPServerStdio`) + Autogen/AG2 (`McpWorkbench`) stdio smoke tests — v10.3 (Phase 61, Plan 04)
- ✓ **FRAME-06..07**: TypeScript framework smoke tests — Mastra (`@mastra/mcp`) + Vercel AI SDK (`experimental_createMCPClient` from `@ai-sdk/mcp`) in `framework-matrix/ts/` (pnpm + vitest, single shared MCP subprocess fixture) — v10.3 (Phase 62)
- ✓ **TOOLING-V3-01**: `task mcp:framework-matrix` opt-in gated runner (`scripts/run_framework_matrix.sh`, `FRAMEWORK_MATRIX=1`/`--force`, clean no-op when unset, never in before-push) — v10.3 (Phase 63, Plan 01)
- ✓ **TOOLING-V3-02**: nightly advisory `.github/workflows/framework-matrix.yml` CI (schedule+dispatch only, `continue-on-error`, `framework-matrix (advisory)` status, structurally cannot block PRs) — v10.3 (Phase 63, Plan 02)
- ✓ **DOCS-V3-01**: `docs/INTEGRATIONS.md` (714 lines) — 7 framework pages + 5 editor config recipes (Goose/Continue.dev/Cline/Cursor/Cody) + SDK-pinning note + README cross-link — v10.3 (Phase 63, Plan 03)
- ✓ **CLI-MCP-04 (post-ship audit fix)**: `/gsd:audit-milestone` found the byte-identical guarantee held only from the project root — `open_backend`/`open_mcp_backend` dropped `AGENT_BRAIN_STATE_DIR` (filtered by Phase 60's `DEFAULT_ENV_ALLOWLIST`) and passed no `--state-dir`, so the spawned `agent-brain-mcp` fell back to cwd discovery. Fixed via `_stdio_command()` threading the resolved state dir as an explicit `--state-dir` arg (env override wins, mirroring the UDS leg); TDD RED→GREEN + contract test hardened to run from an isolated cwd — v10.3 (`fix(57)`, 2026-06-14)
- ✓ **OAUTH-01**: v4 OAuth 2.1 design doc filed at `docs/plans/2026-06-14-mcp-v4-oauth-design.md` (1000 lines, 10 mandatory sections) and **independent security-review gate passed** — cites live MCP Authorization spec 2025-11-25 (re-verified via context7; 2026-07-28 RC confirmed not yet landed), AS/RS/public-route boundary diagram, token-termination data flow (client OAuth token terminates at MCP boundary; MCP→REST keeps `AGENT_BRAIN_API_KEY`), 4-scope×16-tool table, `AGENT_BRAIN_OAUTH_RESOURCE` canonical-URI contract, CIMD-over-DCR decision, DPoP deferral (no current-spec MUST violated). Adversarial review found+closed 7 gaps (DNS-rebinding SSRF post-resolution IP check, empty-`OAUTH_RESOURCE` startup gate, import-time scope drift guard, PKCE `plain` rejection, FileTokenStorage `0o600`, all-mode termination contract, `/mcp/subscriptions` exemption audit); project-owner sign-off APPROVED 2026-06-14 — Phase 66+ unblocked — v10.4 (Phase 65, Plans 01-02)
- ✓ **OAUTH-02**: Protected Resource Metadata (RFC 9728) served at `/.well-known/oauth-protected-resource` AND the path-suffixed `/.well-known/oauth-protected-resource/mcp` (same document), publicly reachable with NO token (HTTP 200) — `build_prm_document()` in `agent_brain_mcp/oauth_metadata.py` returns `{resource, authorization_servers, scopes_supported}` (4 locked scopes `agent-brain:read|index|admin|subscribe`); `resource`←`AGENT_BRAIN_OAUTH_RESOURCE`, `authorization_servers`←`[AGENT_BRAIN_OAUTH_ISSUER]` w/ base-URL fallback — v10.4 (Phase 66, Plan 02)
- ✓ **OAUTH-03**: Authorization Server Metadata (RFC 8414) served at `/.well-known/oauth-authorization-server`, unauth HTTP 200, with `code_challenge_methods_supported: ["S256"]` hardcoded-from-spec (guard comment forbids `plain`), `grant_types_supported: ["authorization_code","refresh_token"]`, `response_types_supported: ["code"]`; authorize/token/register/jwks URLs are documented forward-references to Phase 67 routes. Mount-order contract enforced: all three well-known Routes sit ABOVE the `/mcp` Mount in `build_asgi_app()` (design doc Risk 3), proven by an index-order test written to survive Phase 67's `RequireAuthMiddleware` — v10.4 (Phase 66, Plan 02)
- ✓ **OAUTH-09**: `AGENT_BRAIN_AUTH` toggle (`AuthMode(str, Enum)` over {none, basic, oauth}, default none) wired at the MCP settings layer; `basic` formalizes the SECURITY-01 shared-secret Bearer path as the LAN bridge; `check_auth_startup_gate()` exits code 2 (CRITICAL log) on invalid mode OR oauth-mode empty/scheme-less/fragment `AGENT_BRAIN_OAUTH_RESOURCE`; `get_auth_dependency()` returns exactly one selector per mode (structural mutual exclusion). MCP→REST leg keeps `X-API-Key` in all 3 modes (no confused-deputy regression). 51 settings/gate tests + 58 endpoint tests (109 phase-66 tests green) — v10.4 (Phase 66, Plans 01-02)
- ✓ **OAUTH-04**: Co-located Authorization Server issues tokens via authorization-code + PKCE. New `agent_brain_mcp/oauth/` package: `keys.py` (boot-time 2048-bit RS256 keypair, `build_jwks()` public-only doc, process-lifetime singleton + optional PEM path), `tokens.py` (`mint_access_token()` RS256 with full claim set, `InMemoryTokenStore` single-use codes + rotating 30-day refresh, access TTL 900s), `provider.py` (`AgentBrainAuthServerProvider` implementing all 9 SDK `OAuthAuthorizationServerProvider` methods wired through `create_auth_routes()`); custom `GET /.well-known/jwks.json` route closes the SDK JWKS gap. PKCE S256-only **actively rejected** at the live `/authorize` route via the `reject_non_s256_pkce()` front-handler (plain / method-absent / challenge-absent → HTTP 400 `invalid_request` "PKCE plain method not supported") — v10.4 (Phase 67, Plans 01-02)
- ✓ **OAUTH-05**: Resource Server verifies inbound tokens — `oauth/verifier.py::LocalRs256Verifier` (SDK `TokenVerifier` protocol) validates RS256 signature, `exp`/`nbf` with 30s leeway, `iss`, and `aud`; returns `AccessToken|None` (never raises). `RequireAuthMiddleware(AuthenticationMiddleware(mcp_asgi_app, BearerAuthBackend(verifier)))` wraps ONLY the `/mcp` Mount → 401 + `WWW-Authenticate: Bearer ... resource_metadata=...` on missing/expired/invalid token; valid token passes through. `get_auth_dependency()` oauth branch now returns `"oauth-require-auth"` (Phase 66 `NotImplementedError` placeholder replaced). Scope check (#6) deliberately deferred to Phase 68 (`required_scopes=[]`, claims reachable at `request.state.auth`) — v10.4 (Phase 67, Plan 04)
- ✓ **OAUTH-08**: Resource Indicators (RFC 8707) — AS binds `aud` to the `resource` parameter unchanged in `mint_access_token`; RS validates `aud == AGENT_BRAIN_OAUTH_RESOURCE` via `jwt.decode(audience=...)`, rejecting cross-service token reuse (`test_mcp_wrong_aud_token_returns_401`). Confused-deputy invariant preserved: no code path forwards the client OAuth token upstream; the MCP→REST leg uses `X-API-Key` exclusively — v10.4 (Phase 67, Plans 02+04)
- ✓ **OAUTH-10**: Client registration via CIMD + static pre-registration (`provider.register_client` dispatches on client_id shape; DCR omitted for the single-user shape). `oauth/registration.py` enforces the full mandatory SSRF stack: domain allowlist (`AGENT_BRAIN_OAUTH_CLIENT_ID_ALLOWLIST`), unconditional private/loopback/link-local IP block, **DNS-rebinding post-resolution IP re-validation** (`socket.getaddrinfo` check before fetch — a client_id whose DNS resolves to RFC-1918 is rejected even when its hostname is allowlisted), and a 5s fetch timeout — v10.4 (Phase 67, Plan 03)
- ✓ **OAUTH-06**: Per-tool scope enforcement — `TOOL_SCOPE_REQUIREMENTS` (16 tools → 4 scopes) co-located with `TOOL_REGISTRY` in `tools/__init__.py` as the single SOT, with a fail-loud **import-time `RuntimeError` drift guard** (`_assert_every_tool_has_scope()`) naming any unscoped tool so the server refuses to start on drift. `oauth/scopes.py` provides `require_scope()` + `InsufficientScopeError.required`. A pre-dispatch `ScopeEnforcementMiddleware` (oauth-mode only) buffers the JSON-RPC body, maps `tools/call`/`resources/read`/`resources/subscribe`/`prompts/get` to the required scope, reads `scope["user"].scopes`, and emits a real **HTTP 403** + `WWW-Authenticate: Bearer error="insufficient_scope", scope="<required>", resource_metadata="<PRM>"` (NOT 401) before the session manager runs; `_enforce_scope()` defense-in-depth at the 4 server.py dispatch points. Composition: `AuthenticationMiddleware`(outermost) → `RequireAuthMiddleware` → `ScopeEnforcementMiddleware` → mcp app. 916 tests green (61 new); none/basic modes unchanged — v10.4 (Phase 68, Plans 01-02)
- ✓ **OAUTH-11**: Split AS/RS topology — `oauth/verifier.py::JwksTokenVerifier` (SDK `TokenVerifier` protocol) verifies tokens issued by an external AS (e.g. Keycloak) via remote JWKS using `PyJWKClient(cache_jwk_set=True, lifespan=300)` with kid-miss on-demand refresh, wrapped in `asyncio.to_thread()` to keep the event loop unblocked. `build_verifier()` selects JWKS vs introspection vs local RS256 by config behind the stable `verify_token(token) -> AccessToken | None` seam — topology is a config swap, not a code change. Proven end-to-end (SC#1) against a real Keycloak ≥22 container in CI (step-level `docker run ... start-dev`, path-filtered on `agent-brain-mcp/**` + nightly); realm bootstrap binds `aud` via an audience scope mapper since Keycloak lacks native RFC 8707 before 26.8 — v10.4 (Phase 70, Plans 01-03)
- ✓ **OAUTH-12**: Token introspection + revocation — `oauth/verifier.py::IntrospectionTokenVerifier` (RFC 7662) calls the AS introspection endpoint for opaque-token / external-AS deployments, validates `active: true` + `aud`, and rejects `active: false` (revocation rides on introspection). For the co-located path, a `jti` denylist on `InMemoryTokenStore` (`_revoked_jtis` + lock, `revoke_by_jti`/`is_jti_revoked`) lets `LocalRs256Verifier` reject a revoked token on next use (SC#3, proven against Keycloak via RFC 7009 revoke→introspect). SC#4 full path (tool call + refresh + scope-boundary 403) and the binding ≥90% oauth-module coverage gate (currently 90.53%) close the DoD; operator doc `SPLIT_AS_RS.md` ships — v10.4 (Phase 70, Plans 01-03)

## Current Milestone: v10.4 — MCP v4: OAuth 2.1 + GraphRAG Stability

**Goal:** Make Agent Brain safe to run remotely (OAuth 2.1 on the Streamable HTTP transport) and stabilize the GraphRAG/kuzu path that crashes and under-reports under sustained indexing — bugs first, then auth.

**Target features:**
- **OAuth 2.1 for remote MCP (#188 / MCP v4):** design-doc-first (independent security review before code), Protected Resource Metadata (RFC 9728) + Authorization Server Metadata (RFC 8414), PKCE (RFC 7636), Dynamic Client Registration (RFC 7591), Resource Indicators (RFC 8707), token introspection/revocation, optional DPoP (RFC 9449). Four scopes (`agent-brain:read|index|admin|subscribe`), 15-min access + rotating 30-day refresh tokens. Two deployment shapes: co-located AS/RS (self-hosted single binary) and split AS/RS (external IdP, Keycloak-in-CI). `McpHttpBackend` handles the `WWW-Authenticate` challenge + OAuth dance. Gated by `AGENT_BRAIN_AUTH=oauth` (default `none`), with `AGENT_BRAIN_AUTH=basic` shared-secret bridge for LAN.
- **GraphRAG stability (bugs first):** fix/mitigate the kuzu `SIGSEGV` under sustained GraphRAG indexing (#178) and the GraphSnapshotManager auto-replay scope-gap + under-reporting preflight counters (#184).
- **Housekeeping:** `/mcp/subscriptions` debug endpoint (#194), MCP roadmap meta update (#189), close shipped-but-open issues (#187, #161, #167).

**Source design:** `docs/roadmaps/mcp/v4-oauth-for-remote.md` + `docs/plans/2026-05-28-mcp-uds-transport-design.md` §11 (v4 row), §15.3. **Issue:** [#188](https://github.com/SpillwaveSolutions/agent-brain/issues/188). Builds directly on v10.3's `McpHttpBackend` (the hard dependency, now shipped).

**Deferred to a later milestone (captured, not lost):**
- **Enterprise Hardening & Cloud Deployment** (#200-205): secrets abstraction, GCP/AWS/Azure reference deploys, Postgres lock+queue for multi-replica, read/write split, in-house control plane, DLP redaction. Parent plan: `docs/plans/2026-06-09-enterprise-hardening-and-cloud-deployment.md`. OAuth (this milestone) is a prerequisite for the governed-MCP pieces.
- **Feature backlog:** Claude-native provider (#164), batch query (#163), Rust CLI (#162), schema-versioned entity types (#160) + user-defined graph schemas RFC (#183), VS Code ext (#158), multi-repo federated search (#157), streaming updates (#156), per-source-type embeddings (#155), GraphRAG agentic workflow (#154), Voyage AI provider (#152).
- **CLI polish:** drop EOL Gemini runtime; setup-experience audit (wizard/install-agent for MCP transport + API-key auth — pairs naturally with OAuth setup UX).

### Out of Scope

- **OAuth 2.1 for remote MCP**: Deferred to v4 (#188) — depends on v3's `McpHttpBackend` (this milestone)
- **MCP sampling / completion**: Out of scope for v2 (advanced LLM-in-the-server pattern; not required for tool/resource/subscription completeness)
- **MCP plugin auto-registration**: Out of scope for v2 (deferred to a follow-up; requires manifest design)
- **Runtime parity revival for Codex / OpenCode / Gemini (v9.6.0 phases 47–49)**: Deferred — re-evaluate once MCP v3 framework matrix work begins; converter-level + CLI-level tests already cover install behavior structurally
- **Web UI**: CLI-first philosophy — agents are primary consumers
- **Multi-tenancy**: Local-first philosophy — one instance per project
- **AlloyDB-specific features**: Standard PostgreSQL + pgvector for maximum portability
- **ChromaDB-to-PostgreSQL migration tool**: Users re-index from source when switching backends
- **GraphRAG on PostgreSQL**: Stays ChromaDB-only for now, deferred to future milestone
- **Alembic schema migrations**: Manual SQL scripts via Docker Compose for now

## Context

**Current State (v10.4 IN PROGRESS — Phase 70 complete 2026-06-22, final milestone phase):**
- **Phase 70 complete** (OAUTH-11/OAUTH-12): the split AS/RS topology is validated end-to-end against a real Keycloak ≥22 container in CI. `JwksTokenVerifier` (PyJWKClient cached JWKS, 5-min TTL + kid-miss refresh, `asyncio.to_thread`) accepts Keycloak-issued JWTs (SC#1); `IntrospectionTokenVerifier` (RFC 7662, `active:false`→reject) round-trips opaque tokens (SC#2); jti denylist on `InMemoryTokenStore` + RFC 7009 revocation reject a revoked token on next use (SC#3); the full tool-call + refresh + scope-boundary-403 path runs against the live RS (SC#4). `build_verifier()` selects the topology by config — single stable `verify_token()` seam. CI runs a step-level `docker run ... start-dev` (NOT a services: block) path-filtered on `agent-brain-mcp/**` + nightly; realm bootstrap binds `aud` via an audience scope mapper (Keycloak lacks native RFC 8707 before 26.8). Binding ≥90% oauth-module coverage gate wired in CI (currently 90.53%); operator doc `SPLIT_AS_RS.md` + Phase 70 spec re-verification note shipped. 3 plans, 3 waves, 1021 fast-suite tests green, `task before-push` clean. **This is the final phase of v10.4** — milestone ready for `/gsd:audit-milestone` → `/gsd:complete-milestone`.
- **Phase 68 complete** (OAUTH-06): every MCP tool now enforces its required scope. A pre-dispatch `ScopeEnforcementMiddleware` returns HTTP 403 `insufficient_scope` (not 401) for under-scoped calls; the 16-tool→4-scope map is a single SOT with an import-time drift guard that stops the server on an unscoped tool. 916 tests green. Engages only in `oauth` mode. Next: Phase 69 (McpHttpBackend client-side OAuth dance, OAUTH-07). NOTE: Phase 64 (GraphRAG stability, #212) and Phases 67/68 are on PRs/branches; ensure #212 + the Phase 67/68 branches land on main in order.
- v10.4 MCP v4 (OAuth 2.1) underway. Phase 65 filed + security-gated the OAuth design doc (OAUTH-01); Phase 66 stood up the public discovery root + auth-mode toggle (OAUTH-02/03/09). **Phase 67 complete**: token issuance + verification now work end-to-end in the single co-located binary — a client can complete the authorization-code + PKCE S256 dance against the co-located AS, receive an RS256 JWT (`aud` bound to `AGENT_BRAIN_OAUTH_RESOURCE`), and the RS validates it on every `/mcp` call via `RequireAuthMiddleware` (401 + `WWW-Authenticate` on failure). New `agent_brain_mcp/oauth/` package (keys/tokens/provider/registration/verifier); custom `/.well-known/jwks.json`; live `/authorize` PKCE-plain rejection; CIMD registration with the full SSRF stack incl. DNS-rebinding post-resolution check; SC#5 mode mutual-exclusion proven (JWT fails basic / API key passes basic). mcp SDK bumped `^1.12.0→^1.27.2` with zero drift regressions. OAUTH-04/05/08/10 validated; 824 tests green, 88% MCP coverage; all 5 ROADMAP success criteria verified (5/5 must-haves). Per-tool scope enforcement (#6) deferred to Phase 68; client-side OAuth dance to Phase 69; split AS/RS + Keycloak to Phase 70. Next: Phase 68 (Per-Tool Scope Enforcement).
- v10.3 MCP v3 milestone shipped: 8 phases (56-63), 24 plans, 23/23 requirements validated. CLI is now a reference MCP client (`--transport mcp` + `--mcp-transport stdio|http`, byte-identical to UDS); MCP server smoke-tested against all 7 frameworks (5 Python via Phase 61, 2 TypeScript via Phase 62); operator surface landed in Phase 63 (`task mcp:framework-matrix` opt-in runner, nightly advisory CI, `docs/INTEGRATIONS.md`)
- Milestone audit (`/gsd:audit-milestone`) passed: 8/8 phases verified, 23/23 requirements satisfied, cross-phase integration sound. The audit's integration trace caught a CLI-MCP-04 DoD-anchor env-forwarding gap (stdio MCP dropped `AGENT_BRAIN_STATE_DIR`); fixed on-branch via `--state-dir` propagation (`fix(57)`) with TDD + a hardened contract test
- Shipped on branch `plan/phase-61-python-framework-matrix` → PR [#211](https://github.com/SpillwaveSolutions/agent-brain/pull/211) (phases 61-63 + audit fix; phases 56-60 already on main)
- **Prior state (v10.2 SHIPPED, 2026-06-03):**
- v10.2 shipped: MCP v2 milestone — 6 phases (50-55), 24 plans, ~530 new tests; agent-brain-mcp at 91.83% coverage, agent-brain-uds at 99%
- Full 16-tool MCP server (7 v1 + 9 v2) with resource subscriptions, Streamable HTTP transport (loopback-only), and 4 addressable URI schemes (`chunk://`, `graph-entity://`, `job://`, `file://`)
- DR-5 from MCP v1 design closed: `agent-brain-mcp` + `agent-brain-uds` folded into root `task before-push` + `task pr-qa-gate` (caught a silently-broken UDS smoke test on the first run that had been broken since 10.1.0)
- Two-layer contract test architecture: in-process `fake_httpx_client` (Layer 1) + SDK-driven subprocess (Layer 2) sharing a single `_tool_matrix.py` source of truth with import-time drift guard
- **Prior state (v9.5.0 SHIPPED, 2026-03-31):** Config Validation & Language Support milestone — 5 phases, 9 plans, 58 commits, +8,693 lines
- `install-agent` already exposes Codex, OpenCode, Gemini, and skill-runtime targets, with converter-level and CLI-level tests covering installation behavior
- Existing end-to-end CLI coverage is Claude-centric; runtime parity for project-local install plus headless execution is not yet verified for Codex, OpenCode, and Gemini
- Repo-local runtime trees are uneven today: `.opencode/plugins/agent-brain/` exists, `.gemini/` is absent, and `.codex/` is used for GSD skills rather than a generated Agent Brain install tree
- Config validate/migrate/diff commands with line numbers, fix suggestions, and wizard integration
- Object Pascal AST-aware ingestion (.pas/.pp/.dpr/.dpk) with function/class extraction
- OpenCode installer reference-quality: singular dirs, agent frontmatter, path rewriting, permission pre-auth
- Reproducible benchmark suite: MODE_SUPPORT_MATRIX, fixed query set, dual timing, baseline BENCHMARKS.md (5-10ms p50)
- Nested storage.postgres.* config validation, pool_timeout tunability
- 4 reliability bug fixes: 120s start timeout, state_dir resolution, ChromaDB telemetry, Gemini migration
- 1392+ tests passing (1001 server + 391 CLI)
- Dual-backend architecture: ChromaDB (default) + PostgreSQL (optional)
- Folder management: list, add, remove indexed folders via CLI/API
- File type presets: 11 built-in presets for `--include-type` shorthand
- Content injection: custom Python scripts and folder-level JSON metadata
- Manifest-based incremental indexing: only processes changed/new files
- Chunk eviction: automatic stale chunk removal for deleted/changed files
- 7 embedding/summarization/reranking providers supported
- Full GraphRAG with schema-based entity types (ChromaDB only)

**Technology Stack:**
- Python 3.10+ with Poetry packaging
- FastAPI + Uvicorn server
- ChromaDB vector store (default) + PostgreSQL/pgvector (optional)
- LlamaIndex for document processing
- Pluggable providers: OpenAI, Anthropic, Ollama, Cohere, Gemini, Grok, SentenceTransformers

**Architecture Principles** (from constitution.md):
1. Monorepo Modularity — packages independently testable
2. OpenAPI-First — contracts before code
3. Test-Alongside — tests with implementation
4. Observability — structured logging, health endpoints
5. Simplicity — YAGNI, sensible defaults

## Constraints

- **Local-First**: Must work without cloud dependencies (Ollama support critical)
- **Pre-Push Quality Gate**: `task before-push` MUST pass before any push
- **Test Coverage**: >50% coverage required for CI
- **Package Isolation**: Cross-package deps flow server <- cli/skill (never reverse)
- **Environment Safety**: Runtime parity tests must install only into repo-owned integration projects — never the user's global Codex, OpenCode, or Gemini directories

## Key Decisions

| Decision | Rationale | Outcome |
|----------|-----------|---------|
| SentenceTransformers CrossEncoder for reranking | Better accuracy than Ollama, ~50ms latency | ✓ Good |
| Two-stage retrieval optional (off by default) | Graceful degradation, backward compatible | ✓ Good |
| YAML-driven provider config | Single config file, no code changes to switch providers | ✓ Good |
| Dual-layer validation (startup warning + indexing error) | Warns on startup, blocks only when data integrity at risk | ✓ Good |
| Literal types (not Enum) for entity schema | Better for LLM prompts, less verbose, easier to extend | ✓ Good |
| Permissive schema enforcement (log unknown, don't reject) | Enables schema evolution without breaking existing data | ✓ Good |
| Over-fetch 3x then post-filter for type queries | Ensures enough results after filtering without complex query rewriting | ✓ Good |
| Skill + CLI over MCP | User preference: simpler, less context overhead | ✓ Good |
| GraphRAG with SimplePropertyGraphStore | Simpler than full Kuzu integration for v1 | ✓ Good |
| JSONL job queue over Redis | Local-first, no external dependencies | ✓ Good |
| Minimal FastAPI app for health endpoint tests | Avoids ChromaDB initialization in test environment | ✓ Good |
| CI matrix with conditional API key checks | Tests skip gracefully, config tests always run | ✓ Good |
| StorageBackendProtocol abstraction | Clean separation, contract-testable, dual-backend support | ✓ Good |
| pgvector + tsvector over BM25 for PostgreSQL | Native DB features, no separate index files, better scaling | ✓ Good |
| Async SQLAlchemy for PostgreSQL connections | Non-blocking I/O, connection pooling built-in | ✓ Good |
| RRF fusion for PostgreSQL hybrid search | Same algorithm as ChromaDB, consistent cross-backend behavior | ✓ Good |
| GraphRAG stays ChromaDB-only | Avoids complexity, deferred to future milestone | ✓ Good |
| Conditional ChromaDB init in main.py lifespan | PostgreSQL backend skips ChromaDB setup entirely | ✓ Good |
| Exclude historical files from doc-serve path cleanup | Legacy/planning files document what was true at the time | ✓ Good |
| Structural verification only for Phase 11 requirements | Functional correctness already validated in Phase 10 | ✓ Good |
| Atomic JSONL writes via temp + Path.replace() | POSIX atomic, safe for process crashes during write | ✓ Good |
| Two-step ChromaDB delete (query IDs then delete) | Guards against empty ids=[] collection wipe bug | ✓ Good |
| Hardcode FILE_TYPE_PRESETS in CLI | Avoid cross-package dependency on server | ✓ Good |
| ContentInjector writes only to chunk.metadata.extra | Prevents injectors from overwriting schema fields | ✓ Good |
| ManifestTracker SHA-256 keyed manifest paths | Flat directory, no path-separator issues across OS | ✓ Good |
| mtime fast-path before SHA-256 checksum | O(1) for ~95% of unchanged files | ✓ Good |
| eviction_summary as dict[str, Any] on JobRecord | Pydantic-friendly serialization, no server import in CLI | ✓ Good |
| Project-local runtime parity installs only | Protect local Codex/OpenCode/Gemini environments from test pollution | — Pending |
| Headless JSON status is the runtime parity contract | Gives one machine-verifiable success signal across runtimes with different UX surfaces | — Pending |
| **v10.2: TOOL_REGISTRY locked at 16 tools via `_tool_matrix.py` SOT** | Single source of truth shared by Layer 1 (in-process) and Layer 2 (SDK) contract tests with import-time drift guard prevents silent tool surface drift | ✓ Good |
| **v10.2: `_MetaInjectingServerSession` exploits Pydantic `extra="allow"`** | Carries both transport axes (server build + listen) in MCP `_meta` without forking the SDK's `Implementation` class | ✓ Good |
| **v10.2: pre-flight `socket.bind` probe before uvicorn handoff** | uvicorn 0.32.x catches `OSError` and calls `sys.exit(1)`, so a port-in-use error must be detected before the handoff to produce a clear error message | ✓ Good |
| **v10.2: synchronous subscription cleanup with `finally` as defense-in-depth** | `asyncio.CancelledError` skips coroutine bodies on pre-await cancellation; sync cleanup in `unsubscribe`/`cleanup_session`/`cleanup_all` is required for correctness | ✓ Good |
| **v10.2: DR-5 closure — fold agent-brain-mcp + agent-brain-uds into root QA gates** | +60-90s local pre-push cost (162s wall-clock) is worth catching silent regressions like the `test_smoke.py` `__version__ == "10.0.7"` assertion that was broken since 10.1.0 | ✓ Good |
| **v10.2: hard whitelist by canonical absolute path for `file://` sandbox** | Symlink resolution at read-time keeps policy current as folders change; 4 deny reasons + 10 MiB cap; no `--no-resolve` escape hatch in v2 (deferred to v3 with auth) | ✓ Good |
| **v10.3: `BackendClient` + `McpBackend` as separate `@runtime_checkable` Protocols** | One shape (tools) for HTTP/UDS/MCP, a distinct shape (prompts/resources) for MCP-only; `DocServeClient ⊄ McpBackend` negative pin prevents accidental conflation | ✓ Good |
| **v10.3: Pattern A — fresh stdio subprocess per MCP call (`asyncio.run`)** | No persistent session/context-manager on `McpBackend`; simpler lifecycle, and subprocess hygiene (cwd/env/SIGTERM-SIGKILL) applies per-call | ✓ Good |
| **v10.3: env allowlist drops API keys unless opted in (`forward_env`)** | Subprocess hygiene contract — framework matrix inherits a sane env boundary; `AGENT_BRAIN_API_KEY` auto-forwarded for SECURITY-01 | ✓ Good |
| **v10.3: framework matrix is opt-in + nightly-advisory only** | SDK drift is expected; gating behind `FRAMEWORK_MATRIX=1` and a non-blocking CI status keeps PRs fast while still tracking drift | ✓ Good |
| **v10.3: byte-identical equivalence as the v3 DoD anchor (CLI-MCP-04)** | A real-subprocess contract test proving `--transport mcp` == `--transport uds` is the single objective "the CLI is a faithful MCP client" signal | ✓ Good |
| **v10.3: propagate state dir via explicit `--state-dir` arg, not env** | Audit found the env allowlist legitimately strips `AGENT_BRAIN_STATE_DIR`; passing it as an argument respects the security boundary while fixing discovery from any cwd | ✓ Good (post-ship audit fix) |

## Evolution

This document evolves at phase transitions and milestone boundaries.

**After each phase transition** (via `$gsd-transition`):
1. Requirements invalidated? → Move to Out of Scope with reason
2. Requirements validated? → Move to Validated with phase reference
3. New requirements emerged? → Add to Active
4. Decisions to log? → Add to Key Decisions
5. "What This Is" still accurate? → Update if drifted

**After each milestone** (via `$gsd-complete-milestone`):
1. Full review of all sections
2. Core Value check — still the right priority?
3. Audit Out of Scope — reasons still valid?
4. Update Context with current state

---
*Last updated: 2026-06-22 — Phase 70 (Split AS/RS + Keycloak-in-CI + Integration Tests) COMPLETE — the FINAL phase of milestone v10.4. Validated OAUTH-11 + OAUTH-12 (3 plans, 3 waves, 1021 fast-suite tests green, `agent_brain_mcp.oauth` at 90.53%, `task before-push` clean). Plan 70-01: split-AS verifier engine behind the stable `verify_token()` seam — `JwksTokenVerifier` (PyJWKClient `cache_jwk_set=True, lifespan=300` + kid-miss refresh, `asyncio.to_thread` to avoid blocking the loop — Pitfall 1), `IntrospectionTokenVerifier` (RFC 7662, `isinstance(aud,str)` aud-list normalization — Pitfall 2, `active:false`→None), `jti` denylist on `InMemoryTokenStore` (`_revoked_jtis`+lock) checked by `LocalRs256Verifier`, and `build_verifier()`/`resolve_split_as_settings()` selector wired into `http.py` (replacing the single `build_local_verifier()` call). Plan 70-02: real Keycloak 26.1 in CI proving SC#1 (Keycloak JWT accepted via cached JWKS), SC#2 (introspection round-trip), SC#3 (RFC 7009 revoke→reject); `keycloak` pytest marker (`addopts` excludes `not keycloak`) + `conftest_keycloak.py` fixtures bridged into `conftest.py` (pytest only auto-discovers literal `conftest.py`); `scripts/keycloak_bootstrap.sh` binds `aud` via `oidc-audience-mapper` (RFC 8707 deviation documented — no native Resource Indicators before Keycloak 26.8); CI via step-level `docker run ... start-dev` NOT a `services:` block (Pitfall 5/6 — service containers can't override commands + prod `start` fails health checks), path-filtered on `agent-brain-mcp/**` + nightly in `e2e-nightly.yml`. Plan 70-03: SC#4 (full tool call + token refresh + read-only-token→admin-tool scope-boundary 403 with `WWW-Authenticate ... insufficient_scope`), binding ≥90% oauth-module coverage gate in CI (`--cov-fail-under=90`, currently 90.53%), `task mcp:oauth-cov`/`mcp:keycloak` targets, operator doc `agent-brain-mcp/docs/SPLIT_AS_RS.md` (Shape A/B, env vars, revocation, 403-vs-401 contract), and the Phase 70 spec re-verification note in the design doc (SEP-2575 confirms per-request Bearer validation is forward-compatible — no SDK bump). gsd-verifier: 11/11 must-haves passed; human-verify items are CI-runtime confirmations only (keycloak tests are opt-in, correctly gated). **v10.4 (Phases 64-70) now fully executed — next: `/gsd:audit-milestone` then `/gsd:complete-milestone`.** PRIOR: 2026-06-17 — Phase 69 (McpHttpBackend Client-Side OAuth Dance) COMPLETE. Validated OAUTH-07 (4 plans, 3 waves, 4/4 ROADMAP success criteria; `task before-push` green at 974 MCP + 577 CLI + 1388 server + 32 UDS tests; `agent_brain_mcp/oauth/` coverage ~91%, already over the Phase 70 ≥90% DoD bar). Client side is SDK-driven: `OAuthClientProvider` (httpx.Auth) plugs into `streamablehttp_client(url, auth=...)` and owns the whole dance (PRM→OASM discovery, PKCE S256, loopback callback, refresh). Plan 69-01: `oauth/token_storage.py::FileTokenStorage` — the SDK 4-method `TokenStorage` protocol persisting BOTH `OAuthToken` and `OAuthClientInformationFull` to `state_dir/mcp-oauth-tokens.json` with unconditional `os.chmod(0o600)` (design-doc Probe 6) + corrupt-file→warn+no-token graceful path. Plan 69-02: `oauth/oauth_handlers.py` — `build_redirect_handler` (webbrowser.open + URL-to-stderr headless fallback) + `LoopbackCallbackServer` (HTTPServer bound to OS-assigned port 0, redirect_uri fixed pre-DCR) + `build_callback_handler`. Plan 69-03: `oauth/oauth_client.py::build_oauth_client_provider` (DCR — no CIMD; full scope union read+index+admin; timeout 300) + the load-bearing refactor collapsing all 17 `streamablehttp_client(self.url)` sites in `McpHttpBackend` into ONE `_http_session()` async CM injecting `auth=self._get_auth()`; opt-in `AGENT_BRAIN_MCP_AUTH=oauth` default OFF → byte-identical auth=None path; provider cached lazily once per instance; `transport.py` threads `state_dir`; `McpStdioBackend` untouched; lazy SDK imports preserved. Plan 69-04: dedicated confused-deputy test (SC#4/OAUTH-08 — asserts X-API-Key present AND the OAuth access-token value absent from every outgoing MCP→REST header, targeting `config.py::_open_http_client`) + hermetic e2e tests for SC#1 (401→dance→retry, redirect spy fires once), SC#2 (persist→reuse, spy NOT re-called), SC#3 (expired+refresh→silent refresh). One runtime deviation: the executor and the gsd-verifier both hit transient API 529s; 69-04's Task-3 finalization and goal-backward verification were completed inline by the orchestrator with full grep/test evidence (see 69-VERIFICATION.md) — no code lost. Next: Phase 70 (Split AS/RS + Keycloak-in-CI + integration tests — OAUTH-11/12, the v10.4 milestone closer). PRIOR: 2026-06-16 — Phase 68 (Per-Tool Scope Enforcement) COMPLETE. Validated OAUTH-06 (2 plans, 4/4 ROADMAP success criteria, 916 tests green). Plan 68-01: `oauth/scopes.py` (`VALID_SCOPES` frozenset, `require_scope()`, `InsufficientScopeError.required`) + `TOOL_SCOPE_REQUIREMENTS` (16 tools → 4 scopes; query_count + server_health → read) co-located with `TOOL_REGISTRY` as the single SOT + import-time `RuntimeError` drift guard (`_assert_every_tool_has_scope()`, fail-loud at module import, not test-only) + 31 SOT tests. Plan 68-02: pre-dispatch `ScopeEnforcementMiddleware` (oauth-mode only) buffering the JSON-RPC body and emitting real HTTP 403 + `WWW-Authenticate error="insufficient_scope", scope="<required>", resource_metadata="<PRM>"` before the session manager (the catch-handler-exception approach was rejected because the MCP lowlevel server converts handler exceptions to JSON-RPC errors in HTTP 200); `_enforce_scope()` defense-in-depth at 4 server.py dispatch points; Level B TestClient tests prove SC#1 (read→read 200), SC#2 (read→index 403), SC#3 (read→admin 403), prompts 403, none/basic no-op. Composition fixed to `AuthenticationMiddleware`(outermost)→`RequireAuthMiddleware`→`ScopeEnforcementMiddleware`→mcp app (ASGI outer-first; user must be set before guards read it) — Phase 66/67 middleware tests stay green. OAUTH-07 (Phase 69) erroneously marked complete by an executor and reverted to Pending. `task before-push` green. Next: Phase 69 (McpHttpBackend client-side OAuth dance — OAUTH-07). PRIOR: 2026-06-15 — Phase 67 (Co-Located AS + RS Middleware) COMPLETE. Validated OAUTH-04/05/08/10 (4 plans, 3 waves, 5/5 ROADMAP success criteria, 824 tests green, 88% MCP coverage). Wave 1 (67-01): bumped `mcp ^1.12.0→^1.27.2` (the floor shipping `mcp.server.auth`) + added `PyJWT[crypto]`/`authlib`/`pwdlib[argon2]`, proven zero SDK-drift across the 15-minor jump (664 tests). Wave 2 (67-02): new `agent_brain_mcp/oauth/` package — boot-time RS256 keypair + `build_jwks()`, `mint_access_token()` (900s access / rotating 30-day refresh, `aud`←resource), `InMemoryTokenStore`, 9-method `AgentBrainAuthServerProvider`, `reject_non_s256_pkce()` helper. Wave 3 (67-03): `oauth/registration.py` CIMD + static registration with the full SSRF stack (allowlist + unconditional private-IP block + DNS-rebinding post-resolution `getaddrinfo` re-validation + 5s timeout; the mandatory RFC-1918-rebinding test passes). Wave 3 (67-04): `oauth/verifier.py::LocalRs256Verifier` (sig/exp/nbf-30s-leeway/iss/aud), `RequireAuthMiddleware`+`BearerAuthBackend` wrapping only the `/mcp` Mount, custom `/.well-known/jwks.json` + live `/authorize` PKCE-plain front-handler mounted auth-exempt ABOVE the middleware wrap (Phase 66 mount-order test stays green 33/33), `get_auth_dependency()` oauth placeholder replaced, SC#5 mode-exclusion proof (JWT fails basic / API key passes basic). Scope guard clean — scope check #6 deferred to Phase 68, client dance to Phase 69, split AS/RS to Phase 70, no token-forwarding upstream (confused-deputy invariant held). `task before-push` green. Next: Phase 68 (Per-Tool Scope Enforcement — OAUTH-06). PRIOR: 2026-06-14 — Phase 66 (OAuth Settings Foundation + PRM/OASM Public Endpoints) COMPLETE. Validated OAUTH-02/03/09 (2 plans, 4/4 success criteria, 109 phase tests, 89% MCP coverage). Plan 66-01: `AGENT_BRAIN_AUTH` `AuthMode(str,Enum)` toggle + `check_auth_startup_gate()` (exit-2 on invalid mode / oauth-mode empty/scheme-less/fragment `AGENT_BRAIN_OAUTH_RESOURCE`) + `get_auth_dependency()` structural mutual-exclusion selector in `agent_brain_mcp/config.py`, mirroring the SECURITY-01 server startup gate (TDD, 51 tests). Plan 66-02: hand-rolled `oauth_metadata.py` PRM (RFC 9728, served at base + `/mcp` path-suffixed variant) + OASM (RFC 8414, `code_challenge_methods_supported:["S256"]` hardcoded w/ guard comment) document builders, three well-known Routes wired into `build_asgi_app()` ABOVE the `/mcp` Mount (mount-order contract, design doc Risk 3) with the startup gate called at app-build, plus the unauthenticated-200 + mount-order index test written to survive Phase 67's `RequireAuthMiddleware` (58 tests). Scope guard clean — no `/authorize`·`/token`·`/register`·jwks routes, no middleware enforcement, no `_tool_matrix.py` scope map, no `FileTokenStorage` (Phases 67-70). `/mcp/subscriptions` design-doc audit resolved MOOT (endpoint not mounted in http.py; carries to Phase 64 HOUSE-01). `task before-push` green (653 passed). Next: Phase 67 (Co-Located AS + RS Middleware — OAUTH-04/05/08/10). PRIOR: 2026-06-14 — Phase 64 (GraphRAG Stability + Subscriptions Debug Endpoint) COMPLETE — first phase of v10.4. Validated GSTAB-01/02/03 + HOUSE-01: kuzu SIGSEGV (#178) isolated out-of-process (`build_from_documents_isolated()` spawn subprocess → `GraphBuildFailedError`, job degrades not server); #184 closed on both fronts (`agent-brain graph restore-from-snapshot` CLI + doctor WARN/--fix, and live kuzu `COUNT(*)` health counts killing the `0/100` vs `5677/4366` drift); `/mcp/subscriptions` debug endpoint (#194) closes the v10.2 deferred item. Merged to main via PR #212 after resolving an `http.py` mount-order + planning-doc conflict against Phases 65-67. PRIOR: 2026-06-14 — milestone v10.4 started (MCP v4: OAuth 2.1 + GraphRAG Stability). Scope set after a full backlog sweep (25 open issues + 8 GSD todos bucketed): bugs first (#178 kuzu SIGSEGV, #184 snapshot scope-gap), then OAuth 2.1 (#188). Enterprise hardening (#200-205) + feature backlog deferred. Research-first chosen for the OAuth/MCP-auth stack. PRIOR: 2026-06-14 after v10.3 milestone — MCP v3 (CLI-via-MCP + Framework Matrix) SHIPPED. 8 phases (56-63), 24 plans, 23/23 requirements. Milestone audit passed; CLI-MCP-04 DoD-anchor env-forwarding gap found by the integration trace and fixed on-branch (`fix(57)`, TDD + hardened contract test). PR #211 open (phases 61-63 + fix). Next: `/gsd:new-milestone` (strongest candidate: OAuth 2.1 for HTTP transport, #188). PRIOR: 2026-06-12 — Phase 63 (Tooling + docs + integration page) COMPLETE — final phase of milestone v10.3. Validated TOOLING-V3-01 / TOOLING-V3-02 / DOCS-V3-01 (3/3 plans, 8/8 must-haves). Plan 63-01: `scripts/run_framework_matrix.sh` (187-line gated sequential runner — `FRAMEWORK_MATRIX=1`/`--force` opt-in, clean no-op exit 0 when unset, runs all 7 framework smoke tests) surfaced as `task mcp:framework-matrix` via a bare `framework-matrix:` task in `agent-brain-mcp/Taskfile.yml` (+ root doc-comment block, mirroring the Phase 60 `mcp:stress:orphan-test` idiom); human-verify gate confirmed the matrix NEVER runs in `before-push` (full before-push: 1334 server tests, zero framework bootstrap). Plan 63-02: `.github/workflows/framework-matrix.yml` nightly advisory CI — triggers schedule (`0 7 * * *`)+`workflow_dispatch` ONLY (no push/pull_request), `continue-on-error: true`, posts `framework-matrix (advisory)` commit status via github-script; structurally cannot block a PR (main has zero required checks). Plan 63-03: `docs/INTEGRATIONS.md` (714 lines) — 7 framework pages with copy-pasteable snippets mirroring the actual smoke-test adapter primitives, a Config Recipes section for 5 docs-only editors (Goose, Continue.dev, Cline, Cursor, Cody), an SDK-pinning table, + README cross-link. Milestone v10.3 (Phases 56-63, 24 plans, 23/23 requirements) now fully executed — next: `/gsd:complete-milestone` to archive + ship. PRIOR: Phase 62 (TypeScript framework adapter matrix) COMPLETE. Validated FRAME-06..07 (2/2 plans, 4/4 success criteria). New `framework-matrix/ts/` Node subtree (pnpm@10.12.4 + vitest + TypeScript) mirrors the Phase 61 Python harness: a SINGLE shared seeded MCP subprocess fixture via vitest `globalSetup` (canonical `AB_FWM_STATE_DIR` handoff, SIGTERM→10s/5s→SIGKILL teardown), `src/corpus.ts` ported byte-for-byte from `_harness.py` (same `authenticate` corpus + `search_documents` SMOKE_TOOL/QUERY/ARGS + 5-shape `assertNonEmptySearch`), 8 exact-pinned deps with committed `pnpm-lock.yaml`, `src/fingerprint.ts` `stage()` wrapper (`connect|list-tools|call|assert|disconnect`) for clean per-framework error messages. Two smoke tests close FRAME-06 (Mastra `@mastra/mcp` `MCPClient`) + FRAME-07 (Vercel AI SDK — `experimental_createMCPClient` resolved to its stable home `@ai-sdk/mcp` 1.0.48 via context7, since research is disabled and the `ai`-package symbol was pre-v1 beta). Single `pnpm test` runs all 4 TS test files (42 tests); tests skip gracefully without OPENAI_API_KEY (Phase 61 precedent). Opt-in isolation enforced by `test/isolation.test.ts` (TS suite absent from 4 pyprojects + root Taskfile); `task before-push` green at 544 passed, 111 deselected. `framework-matrix/ts/node_modules/` gitignored. Next: Phase 63 (tooling + docs + integration page — `task mcp:framework-matrix`, nightly advisory CI, `docs/INTEGRATIONS.md`).*

*Prior: 2026-06-11 — Phase 61 (Python framework adapter matrix) COMPLETE. Validated FRAME-01..05 (4/4 plans, 4/4 must-haves). Shared opt-in harness at `framework-matrix/` (`_harness.py` result-shape normalizer for all 5 frameworks + tiny keyless corpus, `conftest.py` seeded MCP server + real HTTP listener factory + session-autouse orphan guard, `pytest.ini` `framework` marker absent from `before-push`, `bootstrap_venv.sh` exact-pin drift guard). Five per-framework smoke tests connect → call `search_documents` → assert non-empty: OpenAI Agents (BOTH `MCPServerStdio` + `MCPServerStreamableHttp`), LangChain (`langchain-mcp-adapters`), LlamaIndex (`llama-index-tools-mcp`), Pydantic AI (`MCPServerStdio`), Autogen/AG2 (`McpWorkbench`). Every SDK exact-pinned with source URL + pin date. All inherit Phase 60 orphan-free teardown via `McpStdioBackend`. Repo-root `.agent-brain/` runtime state gitignored. `task before-push` green: 544 passed, 111 deselected (framework tests opt-in, not collected). Next: Phase 62 (TypeScript framework matrix — Mastra + Vercel AI SDK).*

*Prior: 2026-06-08 — Phase 59 (CLI prompts + resources commands) COMPLETE. Validated CLI-MCP-05, CLI-MCP-06, CLI-MCP-07 (3/3 plans, 4/4 must-haves). `McpBackend` `@runtime_checkable` Protocol shipped SEPARATE from `BackendClient` — load-bearing architectural split pinned by negative `isinstance` test (`DocServeClient ⊄ McpBackend`). 10 wire bodies replace the `"Wired in Phase 59 Plan 02"` sentinel via Pattern A (`asyncio.run`). `agent-brain prompt <name> [--arg K=V]... [--json]` + `agent-brain resources list|read <uri>` commands wired with content-type dispatch matrix (JSON pretty-print, text passthrough, binary blob requires `--output-file`). §3.5 no-silent-fallback contract holds: unknown prompt name + outside-sandbox `file://` both exit 2 with server verdict verbatim. SERVER-side sandbox enforcement preserved — `outside_indexed_roots` literal grep count in `resources.py` = 0. 56 new tests (17 protocol + 26 wire/command + 20 resources unit + 3 e2e). Next: Phase 60 (subprocess hygiene + 1000-invocation orphan test) — addresses the latent `AGENT_BRAIN_URL` env-routing quirk surfaced in Phase 58.*
