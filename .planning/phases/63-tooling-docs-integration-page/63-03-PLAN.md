---
phase: 63-tooling-docs-integration-page
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/INTEGRATIONS.md
  - README.md
autonomous: true
requirements: [DOCS-V3-01]
must_haves:
  truths:
    - "An operator opens docs/INTEGRATIONS.md and finds a short copy-pasteable page for each of the 7 smoke-tested frameworks (server command, transport, capabilities, minimal connect snippet mirroring the smoke test)"
    - "An operator finds a separate Config Recipes section with an MCP-server config snippet for each of Goose, Continue.dev, Cline, Cursor, Cody, clearly labeled config-only / not smoke-tested in v10.3"
    - "An operator finds an SDK-pinning note pointing at the per-framework requirements.txt (Python) + framework-matrix/ts/package.json + PINS.md (TS)"
  artifacts:
    - path: "docs/INTEGRATIONS.md"
      provides: "7 framework integration pages + 5 editor config recipes + SDK-pinning note + opt-in matrix note"
      min_lines: 120
      contains: "## Config Recipes"
    - path: "README.md"
      provides: "Cross-link to docs/INTEGRATIONS.md from the docs list"
      contains: "INTEGRATIONS.md"
  key_links:
    - from: "docs/INTEGRATIONS.md OpenAI Agents page"
      to: "framework-matrix/openai-agents/test_openai_agents_smoke.py"
      via: "connect snippet mirrors the smoke test (MCPServerStdio + MCPServerStreamableHttp)"
      pattern: "MCPServerStdio"
    - from: "docs/INTEGRATIONS.md SDK-pinning section"
      to: "framework-matrix/<fw>/requirements.txt + framework-matrix/ts/PINS.md"
      via: "explicit file-path references"
      pattern: "requirements.txt"
---

<objective>
Author `docs/INTEGRATIONS.md` — the operator integration page for v10.3. One SHORT copy-pasteable page per framework for all 7 smoke-tested frameworks (OpenAI Agents, LangChain, LlamaIndex, Pydantic AI, Autogen, Mastra, Vercel AI SDK), a separate "Config Recipes" section for 5 editor integrations (Goose, Continue.dev, Cline, Cursor, Cody) that ship docs-only in v10.3, and an SDK-pinning note. Cross-link it from the README.

Purpose: Give operators a single place to wire `agent-brain-mcp` into any supported framework or editor, with snippets that mirror the actual (tested) smoke tests so they are known-good.

Output: `docs/INTEGRATIONS.md` + a README docs-list cross-link.
</objective>

<execution_context>
@/Users/richardhightower/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardhightower/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/63-tooling-docs-integration-page/63-CONTEXT.md

# Source material — the SMOKE TESTS are the source of truth for the connect snippets
@framework-matrix/README.md
@framework-matrix/_harness.py
@framework-matrix/openai-agents/test_openai_agents_smoke.py
@framework-matrix/ts/README.md
@framework-matrix/ts/PINS.md
@README.md

<interfaces>
<!-- Contracts extracted from the codebase. Mirror these in the snippets. -->

Canonical smoke-test parameters (framework-matrix/_harness.py — use VERBATIM in every page):
- Tool: `search_documents` (SMOKE_TOOL)
- Query: `"authenticate user login"` (SMOKE_QUERY)
- Args: `{"query": "authenticate user login"}` (SMOKE_ARGS)
- stdio launch spec (stdio_server_params): command `agent-brain-mcp`, args `["--backend", "uds", "--state-dir", "<state>/.agent-brain"]`, env carries PATH, HOME, AGENT_BRAIN_STATE_DIR (+ OPENAI_API_KEY when present).

The 7 smoke-tested frameworks + their adapter primitives + per-page source file:
| Framework | Adapter primitive | Source smoke test (mirror its connect flow) | Requirement |
|-----------|-------------------|---------------------------------------------|-------------|
| OpenAI Agents SDK | `MCPServerStdio` + `MCPServerStreamableHttp` (`agents.mcp`) | framework-matrix/openai-agents/test_openai_agents_smoke.py | FRAME-01 |
| LangChain | `langchain-mcp-adapters` | framework-matrix/langchain/test_langchain_smoke.py | FRAME-02 |
| LlamaIndex | `llama-index-tools-mcp` | framework-matrix/llama-index/test_llama_index_smoke.py | FRAME-03 |
| Pydantic AI | `MCPServerStdio` | framework-matrix/pydantic-ai/test_pydantic_ai_smoke.py | FRAME-04 |
| Autogen (AG2) | `McpWorkbench` | framework-matrix/autogen/test_autogen_smoke.py | FRAME-05 |
| Mastra | `@mastra/mcp` `MCPClient` (stdio) → `listToolsets()` | framework-matrix/ts/test/mastra.test.ts | FRAME-06 |
| Vercel AI SDK | `@ai-sdk/mcp` `createMCPClient` + `Experimental_StdioMCPTransport` | framework-matrix/ts/test/vercel-ai-sdk.test.ts | FRAME-07 |

Server-launch commands operators use:
- stdio: run `agent-brain-mcp` directly (the adapter spawns it as a subprocess via the stdio launch spec above).
- streamable HTTP: `agent-brain mcp start` launches `agent-brain-mcp --transport http` on a loopback port and writes `mcp.runtime.json` (Phase 58). `agent-brain mcp stop` tears it down.

Capabilities to list on each page (the MCP surface, v2): the `search_documents` tool is the smoke-tested capability; the server also exposes 16 tools total, 5 static resources + 4 templated URI schemes (`chunk://`, `graph-entity://`, `job://`, `file://`), and 6 prompts. Keep the capabilities blurb SHORT — lead with `search_documents`, mention "+ 15 more tools, resources, prompts (see USER_GUIDE)".

The 5 docs-only config recipes (NO smoke test in v10.3 — label clearly):
Goose, Continue.dev, Cline, Cursor, Cody. Each gets an MCP-server config snippet in THAT editor's current config format.

SDK pins to reference:
- Python: `framework-matrix/<fw>/requirements.txt` (one per framework: openai-agents, langchain, llama-index, pydantic-ai, autogen).
- TS: `framework-matrix/ts/package.json` + `framework-matrix/ts/PINS.md`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the 7 framework integration pages + opt-in matrix note + SDK-pinning note</name>
  <read_first>
    - .planning/phases/63-tooling-docs-integration-page/63-01-SUMMARY.md (if it exists — confirm the documented gate flag names + opt-in behavior match the actual runner. This doc is markdown and may be authored before 63-01 lands; if the SUMMARY is absent, document the gate behavior from Plan 63-01's must_haves, and a later read can reconcile.)
    - framework-matrix/_harness.py (SMOKE_TOOL / SMOKE_QUERY / SMOKE_ARGS / stdio_server_params — the canonical connect inputs every snippet must mirror)
    - framework-matrix/openai-agents/test_openai_agents_smoke.py (the exact MCPServerStdio + MCPServerStreamableHttp connect→list_tools→call_tool flow to mirror in the OpenAI Agents page)
    - framework-matrix/langchain/test_langchain_smoke.py, framework-matrix/llama-index/test_llama_index_smoke.py, framework-matrix/pydantic-ai/test_pydantic_ai_smoke.py, framework-matrix/autogen/test_autogen_smoke.py (the per-framework connect flow + adapter import each page mirrors)
    - framework-matrix/ts/test/mastra.test.ts + framework-matrix/ts/test/vercel-ai-sdk.test.ts (the TS connect flows)
    - framework-matrix/ts/PINS.md + framework-matrix/ts/package.json (TS pins for the pinning note)
    - each framework-matrix/<fw>/requirements.txt (Python pins for the pinning note — read each to confirm the filename/path)
  </read_first>
  <action>
    Create `docs/INTEGRATIONS.md`. Structure (use these EXACT H2 headings so they are grep-verifiable):

    1. A short intro paragraph: what agent-brain-mcp is (MCP server exposing semantic codebase/doc search), and that these snippets MIRROR the smoke tests in `framework-matrix/` so they are known-good.

    2. An "Opt-in test matrix" note: state that `task mcp:framework-matrix` runs all 7 framework smoke tests, is SLOW + OPT-IN (gated behind `FRAMEWORK_MATRIX=1` or `--force`, a no-op when the gate is unset), is NOT part of `task before-push`, and runs nightly as an advisory (non-blocking) CI job. (This satisfies the SC1 "documented as slow + opt-in in docs/INTEGRATIONS.md" requirement — cross-reference Plan 01's Taskfile target. The documented gate behavior — `FRAMEWORK_MATRIX=1` / `--force`, no-op-when-unset — MUST match Plan 63-01's must_haves / 63-01-SUMMARY.md.)

    3. Seven framework pages — one H2 each, in this order, with these EXACT heading strings:
       `## OpenAI Agents`, `## LangChain`, `## LlamaIndex`, `## Pydantic AI`, `## Autogen`, `## Mastra`, `## Vercel AI SDK`.
       Each page is SHORT and copy-pasteable and contains:
         a. Server command — how to launch the MCP server for this framework: the stdio invocation (`agent-brain-mcp` spawned by the adapter, with `--backend uds --state-dir <state>/.agent-brain`) and, where the framework's smoke test uses HTTP (OpenAI Agents `MCPServerStreamableHttp`), the `agent-brain mcp start` → `mcp.runtime.json` HTTP path.
         b. Transport — stdio and/or streamable-http (match what the smoke test actually exercises: OpenAI Agents = both; the other 4 Python + 2 TS = stdio).
         c. Capabilities — lead with the `search_documents` tool (args `{"query": "authenticate user login"}`), then a one-liner "+ 15 more tools, 5 resources + 4 URI schemes, 6 prompts — see docs/USER_GUIDE.md".
         d. A MINIMAL connect snippet that MIRRORS the corresponding smoke test (same adapter import, same connect→list-tools→call(search_documents)→assert-non-empty shape). Each snippet MUST use the SAME adapter primitive as its smoke test (OpenAI Agents → `MCPServerStdio` + `MCPServerStreamableHttp`; LangChain → `langchain-mcp-adapters`; LlamaIndex → `llama-index-tools-mcp`; Pydantic AI → `MCPServerStdio`; Autogen → `McpWorkbench`; Mastra → `@mastra/mcp`; Vercel AI SDK → `createMCPClient`). Pull the adapter API shape from the smoke-test file; do NOT invent a different API. For current adapter syntax that may have drifted since the pins, RESOLVE via context7: e.g. `npx ctx7@latest library "LangChain MCP adapters"` then `npx ctx7@latest docs <id> "load MCP tools over stdio and call a tool"`; same for "LlamaIndex MCP tools", "Pydantic AI MCPServerStdio", "Autogen McpWorkbench", "OpenAI Agents SDK MCPServerStdio", "Mastra MCP client", "Vercel AI SDK MCP client". The smoke test is the authoritative shape; context7 fills in any current-syntax detail. Keep each snippet to the minimal connect→call→print path.

    4. A "## SDK Pinning" section: explain that the matrix pins every SDK version to control churn, and point operators at the exact files to align their environment:
       - Python: `framework-matrix/openai-agents/requirements.txt`, `framework-matrix/langchain/requirements.txt`, `framework-matrix/llama-index/requirements.txt`, `framework-matrix/pydantic-ai/requirements.txt`, `framework-matrix/autogen/requirements.txt` (one per framework). Mention `framework-matrix/bootstrap_venv.sh <fw>` exits 3 on pin drift.
       - TS: `framework-matrix/ts/package.json` (exact pins, no `^`/`~`) + `framework-matrix/ts/PINS.md` (pin manifest with source URLs + dates).

    Keep the whole doc operator-focused and concise. Do NOT hard-code guessed adapter APIs — mirror the smoke tests + confirm current syntax via context7. (Config Recipes section is added in Task 2.)
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && for h in "## OpenAI Agents" "## LangChain" "## LlamaIndex" "## Pydantic AI" "## Autogen" "## Mastra" "## Vercel AI SDK" "## SDK Pinning"; do grep -qF "$h" docs/INTEGRATIONS.md || { echo "MISSING: $h"; exit 1; }; done && grep -q "search_documents" docs/INTEGRATIONS.md && grep -q "FRAMEWORK_MATRIX" docs/INTEGRATIONS.md && grep -q "requirements.txt" docs/INTEGRATIONS.md && grep -q "PINS.md" docs/INTEGRATIONS.md && grep -q "MCPServerStdio" docs/INTEGRATIONS.md && grep -q "McpWorkbench" docs/INTEGRATIONS.md && grep -q "createMCPClient" docs/INTEGRATIONS.md && grep -q "@mastra/mcp" docs/INTEGRATIONS.md && grep -q "MCPServerStreamableHttp" docs/INTEGRATIONS.md && grep -q "langchain-mcp-adapters" docs/INTEGRATIONS.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/INTEGRATIONS.md` contains all 7 framework H2 headings exactly: `## OpenAI Agents`, `## LangChain`, `## LlamaIndex`, `## Pydantic AI`, `## Autogen`, `## Mastra`, `## Vercel AI SDK`.
    - It contains a `## SDK Pinning` section that references `requirements.txt` AND `framework-matrix/ts/PINS.md`.
    - Every framework page's connect snippet uses the SAME adapter primitive as its smoke test — ALL of these tokens appear in the doc: `MCPServerStdio`, `MCPServerStreamableHttp` (OpenAI Agents — both transports), `langchain-mcp-adapters` (LangChain), `McpWorkbench` (Autogen), `@mastra/mcp` (Mastra), `createMCPClient` (Vercel AI SDK).
    - Every framework page mentions the `search_documents` tool.
    - The doc states the matrix is opt-in / slow / gated on `FRAMEWORK_MATRIX` (no-op when unset) and NOT in before-push, and this documented gate behavior matches Plan 63-01's must_haves (`FRAMEWORK_MATRIX=1` / `--force`).
  </acceptance_criteria>
  <done>All 7 framework pages exist with smoke-test-mirroring connect snippets (each using its own adapter primitive), the opt-in matrix note that matches 63-01's gate behavior, and the SDK-pinning section pointing at the pin files.</done>
</task>

<task type="auto">
  <name>Task 2: Author the Config Recipes section (5 editors) + README cross-link</name>
  <read_first>
    - docs/INTEGRATIONS.md (the file from Task 1 — append the Config Recipes section)
    - README.md (lines ~189-194 — the docs list: USER_GUIDE / API_REFERENCE entries — add the INTEGRATIONS.md cross-link in the same style)
    - framework-matrix/README.md (the "config-only, not smoke-tested" framing precedent)
  </read_first>
  <action>
    Append a `## Config Recipes` section to `docs/INTEGRATIONS.md`. Open it with a clear banner: these 5 editor integrations are CONFIG-ONLY and NOT smoke-tested in v10.3 (framework drift is too fast; promoting any to a real smoke test is a future-milestone decision). Then one short H3 sub-page per editor, in this order with these EXACT headings:
    `### Goose`, `### Continue.dev`, `### Cline`, `### Cursor`, `### Cody`.

    Each editor sub-page contains a minimal MCP-server config snippet in THAT editor's current config format, wiring `agent-brain-mcp` as a stdio MCP server (command `agent-brain-mcp`, args `--backend uds --state-dir <your-project>/.agent-brain`, env with `AGENT_BRAIN_STATE_DIR` + `OPENAI_API_KEY`). Each editor uses a DIFFERENT config file + schema (Goose `~/.config/goose/config.yaml` mcp servers block; Continue.dev `config.json`/`config.yaml` mcpServers; Cline VS Code settings `cline_mcp_settings.json`; Cursor `~/.cursor/mcp.json` / project `.cursor/mcp.json`; Cody / Sourcegraph editor config). These config schemas drift fast and MUST be resolved at execution time — DO NOT hard-code a guessed schema. For each editor RESOLVE the current MCP config format via context7:
      - `npx ctx7@latest library "Goose"` (Block's Goose AI agent) → `npx ctx7@latest docs <id> "configure an MCP stdio server in goose config"`
      - `npx ctx7@latest library "Continue.dev"` → docs query "add an MCP server to Continue config"
      - `npx ctx7@latest library "Cline"` → docs query "configure MCP server settings (cline_mcp_settings.json)"
      - `npx ctx7@latest library "Cursor"` → docs query "add a custom MCP server in mcp.json"
      - `npx ctx7@latest library "Cody"` (Sourcegraph Cody) → docs query "configure an MCP server"
    Use the official library name with proper punctuation (per the context7 rule). If a library resolve returns no good MCP-config match, note in the snippet comment that the format should be confirmed against that editor's current docs (link), rather than inventing a schema. Each snippet stays minimal + clearly labeled "config-only, not smoke-tested in v10.3", and each MUST contain the `agent-brain-mcp` server invocation (so the recipe actually wires THIS server, not a placeholder).

    Then update `README.md`: add a docs-list entry cross-linking `docs/INTEGRATIONS.md` (e.g. `- [Integrations](docs/INTEGRATIONS.md) - Connect agent-brain-mcp to LLM frameworks + editors`) placed alongside the existing USER_GUIDE / API_REFERENCE entries (around lines 189-194), matching their bullet style.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && grep -qF "## Config Recipes" docs/INTEGRATIONS.md && for h in "### Goose" "### Continue.dev" "### Cline" "### Cursor" "### Cody"; do grep -qF "$h" docs/INTEGRATIONS.md || { echo "MISSING: $h"; exit 1; }; done && grep -q "not smoke-tested in v10.3" docs/INTEGRATIONS.md && python3 -c "import sys,re; t=open('docs/INTEGRATIONS.md').read(); i=t.find('## Config Recipes'); sec=t[i:]; n=sec.count('agent-brain-mcp'); print('agent-brain-mcp in Config Recipes:', n); sys.exit(0 if n>=5 else 1)" && grep -q "INTEGRATIONS.md" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/INTEGRATIONS.md` contains `## Config Recipes` and all 5 editor H3 headings exactly: `### Goose`, `### Continue.dev`, `### Cline`, `### Cursor`, `### Cody`.
    - The Config Recipes section contains the literal phrase `not smoke-tested in v10.3` labeling these as config-only.
    - Each of the 5 editor recipes wires `agent-brain-mcp` as the MCP server — `agent-brain-mcp` appears at least 5 times WITHIN the Config Recipes section (one per editor recipe), confirming each recipe actually contains the server invocation (not a guessed/empty schema).
    - `README.md` contains a cross-link to `docs/INTEGRATIONS.md`.
  </acceptance_criteria>
  <done>The Config Recipes section documents all 5 editors with current (context7-resolved) MCP config formats, each containing the `agent-brain-mcp` server invocation, clearly labeled config-only, and the README links to the new page.</done>
</task>

</tasks>

<verification>
- docs/INTEGRATIONS.md has all 7 framework H2 pages + `## Config Recipes` with 5 editor H3s + `## SDK Pinning`.
- Connect snippets mirror the corresponding smoke tests (same adapter primitive — all 5+ primitives grep-verified).
- Config recipes labeled config-only / not smoke-tested; each contains the `agent-brain-mcp` invocation; formats resolved via context7.
- README cross-links the new page.
</verification>

<success_criteria>
- DOCS-V3-01 closed: one short copy-pasteable page per framework for all 7 smoke-tested frameworks (server command, transport, capabilities, connect snippet) + a separate Config Recipes section for Goose/Continue.dev/Cline/Cursor/Cody (config-only) + an SDK-pinning note pointing at the per-framework requirements.txt + framework-matrix/ts/PINS.md.
</success_criteria>

<output>
After completion, create `.planning/phases/63-tooling-docs-integration-page/63-03-SUMMARY.md`
</output>
</content>
