---
phase: 63-tooling-docs-integration-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/run_framework_matrix.sh
  - agent-brain-mcp/Taskfile.yml
  - Taskfile.yml
autonomous: false
requirements: [TOOLING-V3-01]
must_haves:
  truths:
    - "Operator runs `FRAMEWORK_MATRIX=1 task mcp:framework-matrix` and all 7 framework smoke tests run sequentially, each self-bootstrapped"
    - "Operator runs `task mcp:framework-matrix` with the gate unset and it prints the opt-in message and exits 0 without running any test"
    - "`task before-push` still exits 0 and never invokes the framework matrix"
  artifacts:
    - path: "scripts/run_framework_matrix.sh"
      provides: "Sequential self-bootstrap+run+teardown runner for the 5 Python + TS frameworks, gated on FRAMEWORK_MATRIX/--force"
      min_lines: 40
      contains: "FRAMEWORK_MATRIX"
    - path: "agent-brain-mcp/Taskfile.yml"
      provides: "Bare-name `framework-matrix:` task that the root `includes: mcp:` alias surfaces as `mcp:framework-matrix`"
      contains: "framework-matrix:"
    - path: "Taskfile.yml"
      provides: "Documentation comment block describing the opt-in `mcp:framework-matrix` target (no duplicate root task block)"
      contains: "mcp:framework-matrix"
  key_links:
    - from: "agent-brain-mcp/Taskfile.yml framework-matrix task"
      to: "scripts/run_framework_matrix.sh"
      via: "shell invocation from repo root"
      pattern: "run_framework_matrix.sh"
    - from: "scripts/run_framework_matrix.sh"
      to: "framework-matrix/bootstrap_venv.sh"
      via: "per-Python-framework bootstrap call in the sequential loop"
      pattern: "bootstrap_venv.sh"
    - from: "scripts/run_framework_matrix.sh"
      to: "framework-matrix/ts"
      via: "pnpm install --frozen-lockfile && pnpm test"
      pattern: "pnpm test"
---

<objective>
Land `task mcp:framework-matrix` — the operator-facing, opt-in, self-bootstrapping target that runs all 7 framework smoke tests (5 Python via Phase 61, 2 TypeScript via Phase 62) sequentially with per-framework setup + teardown. The nightly CI (Plan 02) calls this SAME target with no extra setup.

Purpose: Give operators (and the nightly CI) one turnkey command to validate the MCP server against every framework adapter, while guaranteeing it NEVER runs during `task before-push` or a bare `task` invocation (gated behind `FRAMEWORK_MATRIX=1` or `--force`).

Output: `scripts/run_framework_matrix.sh` (the sequential runner), a bare `framework-matrix:` task in `agent-brain-mcp/Taskfile.yml` (surfaced as `mcp:framework-matrix` via the existing `includes: mcp:` alias), and a documentation comment block in the root `Taskfile.yml`.
</objective>

<execution_context>
@/Users/richardhightower/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardhightower/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/63-tooling-docs-integration-page/63-CONTEXT.md

# Source-of-truth analogues the executor MUST mirror
@Taskfile.yml
@agent-brain-mcp/Taskfile.yml
@framework-matrix/README.md
@framework-matrix/bootstrap_venv.sh
@framework-matrix/pytest.ini
@framework-matrix/ts/README.md

<interfaces>
<!-- Contracts extracted from the codebase. Use directly — no exploration needed. -->

The Taskfile include-namespace idiom (Phase 60-03 precedent — AVOID the cyclic-include trap):
- Root `Taskfile.yml` line 33-35 declares `includes: mcp:` pointing at `./agent-brain-mcp/Taskfile.yml` with `dir: ./agent-brain-mcp`.
- A task named BARE `stress:orphan-test:` in `agent-brain-mcp/Taskfile.yml` (lines 110-125) is surfaced at the root as `task mcp:stress:orphan-test` — the include alias prepends the `mcp:` namespace automatically.
- DO NOT name the per-package task `mcp:framework-matrix:` — that triggers `task: Found multiple tasks (mcp:framework-matrix) included by "mcp"`. Name it BARE `framework-matrix:`.
- DO NOT add a duplicate root-level `framework-matrix:` task block — root only gets an explanatory COMMENT block (mirror the comment block at root Taskfile.yml lines 224-234).
- `dir:` for the include is `./agent-brain-mcp`, so a per-package task's `{{.ROOT_DIR}}` is the MONOREPO root when dispatched via `task mcp:framework-matrix`. The runner script lives at repo-root `scripts/` and must be invoked relative to the repo root.

The Python bootstrap contract (framework-matrix/bootstrap_venv.sh):
- Invocation: `bash framework-matrix/bootstrap_venv.sh <framework>` from anywhere (it cds to repo root via `git rev-parse --show-toplevel`).
- Frameworks (exact dir names): `openai-agents`, `langchain`, `llama-index`, `pydantic-ai`, `autogen`.
- Creates `framework-matrix/<fw>/.venv`, installs pinned requirements + local agent-brain packages, exits 3 on pin drift.
- Run a framework's tests with its OWN venv python:
  `framework-matrix/<fw>/.venv/bin/pytest framework-matrix/<fw>/ -m framework`
- Tests skip gracefully when OPENAI_API_KEY / agent-brain binaries are absent (Phase 61 precedent).

The TS contract (framework-matrix/ts/):
- `cd framework-matrix/ts && pnpm install --frozen-lockfile && pnpm test`
- `pnpm test` runs vitest across all 4 test files; server-dependent tests skip gracefully without OPENAI_API_KEY/binaries.

Smoke-test count: 5 Python frameworks + 1 TS suite = the "7 frameworks" (5 Python + Mastra + Vercel AI SDK, the latter two both inside the single TS `pnpm test`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write scripts/run_framework_matrix.sh — gated sequential self-bootstrap runner</name>
  <read_first>
    - framework-matrix/bootstrap_venv.sh (the per-framework bootstrap contract this script calls; note exit-3 on drift and the repo-root cd behavior)
    - framework-matrix/README.md (the opt-in contract + "Run the Framework Tests" + per-framework venv pytest invocation)
    - framework-matrix/ts/README.md (the `pnpm install --frozen-lockfile && pnpm test` contract + graceful-skip behavior)
    - scripts/before_push_lock_guard.sh (an existing repo `scripts/*.sh` for this repo's shell idiom: `set -euo pipefail`, repo-root resolution)
  </read_first>
  <action>
    Create `scripts/run_framework_matrix.sh` (POSIX-ish bash, `set -euo pipefail`, executable `chmod +x`). It is the SINGLE turnkey runner the Taskfile target and nightly CI both call.

    GATING (mirror the Phase 60-03 opt-in posture):
    - Accept an optional `--force` first argument. The script runs the matrix when EITHER the env var `FRAMEWORK_MATRIX=1` is set OR `--force` is passed.
    - If NEITHER gate is present: print exactly the one-line opt-in message
      `framework-matrix: slow + opt-in; set FRAMEWORK_MATRIX=1 (or pass --force) to run`
      and `exit 0` WITHOUT running any framework. This is the no-op path that keeps it out of `before-push`.

    REPO-ROOT: resolve and `cd` to the repo root using `git rev-parse --show-toplevel` (fallback to `dirname $0/..` when not in a git tree) — same idiom as bootstrap_venv.sh, so relative paths resolve regardless of caller cwd.

    SEQUENTIAL PYTHON LOOP — iterate over EXACTLY these 5 framework dir names in order:
    `openai-agents langchain llama-index pydantic-ai autogen`.
    For each `<fw>`:
      1. SETUP: `bash framework-matrix/bootstrap_venv.sh "<fw>"` (self-bootstrap — installs pinned deps; exit 3 on drift propagates).
      2. RUN: `framework-matrix/<fw>/.venv/bin/pytest framework-matrix/<fw>/ -m framework` and capture its exit status into a per-framework result (do NOT abort the whole loop on a single framework failure — record pass/fail and continue so every framework is exercised; mirror Phase 61's per-framework isolation intent). Tests skip gracefully without OPENAI_API_KEY (Phase 61 precedent) — a skip is NOT a failure.
      3. TEARDOWN: each framework is fully set up, run, and torn down before the next so heavy dep trees don't collide and orphan subprocesses don't accumulate (orphan-free contract inherited from Phase 60 via the conftest orphan guard). No persistent state carries between frameworks.

    TYPESCRIPT LEG (the 6th/7th frameworks — Mastra + Vercel AI SDK both run inside one invocation):
      - `cd framework-matrix/ts && pnpm install --frozen-lockfile && pnpm test` (back to repo root after). Capture its exit status as the TS result. Server-dependent TS tests skip gracefully without OPENAI_API_KEY/binaries.

    SUMMARY + EXIT: after all legs, print a per-framework PASS/FAIL/SKIP summary line per framework, then `exit 1` if ANY framework leg hard-failed (non-skip failure), else `exit 0`. Document at the top of the script (comment) that it is SLOW + OPT-IN and that the nightly CI calls it with `FRAMEWORK_MATRIX=1`.

    Keep it readable; this is glue/orchestration, no business logic — no tdd block needed.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && bash scripts/run_framework_matrix.sh; test $? -eq 0 && grep -q "FRAMEWORK_MATRIX" scripts/run_framework_matrix.sh && grep -q "set -euo pipefail" scripts/run_framework_matrix.sh && for fw in openai-agents langchain llama-index pydantic-ai autogen; do grep -q "$fw" scripts/run_framework_matrix.sh || exit 1; done && grep -q "bootstrap_venv.sh" scripts/run_framework_matrix.sh && grep -q "pnpm test" scripts/run_framework_matrix.sh</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/run_framework_matrix.sh` exists, is executable (`test -x scripts/run_framework_matrix.sh`), and contains `set -euo pipefail`.
    - Running `bash scripts/run_framework_matrix.sh` with `FRAMEWORK_MATRIX` UNSET and no `--force` exits 0 and prints a line containing `slow + opt-in` and `FRAMEWORK_MATRIX=1` (grep stdout) WITHOUT running any pytest/pnpm.
    - `grep -c "bootstrap_venv.sh" scripts/run_framework_matrix.sh` >= 1 and all 5 framework dir names (`openai-agents`, `langchain`, `llama-index`, `pydantic-ai`, `autogen`) appear in the script.
    - The script contains both `pnpm install --frozen-lockfile` and `pnpm test`.
    - The script gates on `--force` OR `FRAMEWORK_MATRIX` (grep returns both tokens).
  </acceptance_criteria>
  <done>The gated runner exists; gate-unset is a no-op exit 0; gate-set drives all 5 Python bootstraps + the TS pnpm leg sequentially with per-framework teardown.</done>
</task>

<task type="auto">
  <name>Task 2: Wire bare `framework-matrix:` task in agent-brain-mcp/Taskfile.yml + root documentation comment</name>
  <read_first>
    - agent-brain-mcp/Taskfile.yml (the `stress:orphan-test:` bare-name task at lines 110-125 — copy its placement + desc style; note it has `deps: [install]` and a `desc:` documenting opt-in/slow)
    - Taskfile.yml (root — the `includes: mcp:` alias at lines 33-35 + the explanatory comment block for `mcp:stress:orphan-test` at lines 224-234 — mirror that comment-only pattern at root; also confirm the `before-push:` chain at lines 196-222 does NOT and must NOT reference framework-matrix)
  </read_first>
  <action>
    In `agent-brain-mcp/Taskfile.yml`, add a NEW task named BARE `framework-matrix:` (NOT `mcp:framework-matrix:` — the root `includes: mcp:` alias prepends the namespace; naming it with the prefix triggers the `Found multiple tasks (mcp:framework-matrix) included by "mcp"` cyclic-include collision that bit Phase 60-03). Place it right after `stress:orphan-test:` for discoverability.

    The task:
    - `desc:` a multi-line description stating it is SLOW + OPT-IN, runs all 7 framework smoke tests sequentially with self-bootstrap, gated behind `FRAMEWORK_MATRIX=1` or `--force`, NOT in `task before-push`, surfaced at root as `task mcp:framework-matrix` via the `includes: mcp:` alias (mirror the `stress:orphan-test` desc wording).
    - Do NOT add `deps: [install]` — the runner self-bootstraps per-framework venvs and the TS pnpm env; a poetry `install` dep is unnecessary and would slow the no-op gate path. (If a dep is needed for the script to be found, none is — the script is at repo-root `scripts/`.)
    - `cmds:` a single command that invokes the repo-root runner. Because the include `dir:` is `./agent-brain-mcp`, resolve the repo root explicitly so the script path is correct regardless of cwd:
      `bash "$(git rev-parse --show-toplevel)/scripts/run_framework_matrix.sh" {{.CLI_ARGS}}`
      Passing `{{.CLI_ARGS}}` lets `task mcp:framework-matrix -- --force` forward `--force` to the runner.

    In the ROOT `Taskfile.yml`, add an explanatory COMMENT block (NO task block — a root `framework-matrix:` task block would collide with the include alias, exactly like the `stress:orphan-test` precedent). Place it adjacent to the existing `mcp:stress:orphan-test` comment block (lines 224-234). The comment must state: opt-in/slow, gated on `FRAMEWORK_MATRIX=1` or `--force`, surfaced as `task mcp:framework-matrix` via the bare per-package name + `includes: mcp:` alias, runs all 7 frameworks sequentially via `scripts/run_framework_matrix.sh`, and that it is deliberately NOT in the `before-push` chain.

    Do NOT touch the root `before-push:` task. After wiring, the matrix must remain absent from the before-push chain.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && grep -q "framework-matrix:" agent-brain-mcp/Taskfile.yml && grep -q "run_framework_matrix.sh" agent-brain-mcp/Taskfile.yml && ! grep -qE "^\s*mcp:framework-matrix:" agent-brain-mcp/Taskfile.yml && grep -q "mcp:framework-matrix" Taskfile.yml && ! grep -qE "^\s*framework-matrix:" Taskfile.yml && task mcp:framework-matrix 2>&1 | grep -q "slow + opt-in"</automated>
  </verify>
  <acceptance_criteria>
    - `agent-brain-mcp/Taskfile.yml` contains a task named bare `framework-matrix:` (grep `^\s*framework-matrix:`) and does NOT contain `mcp:framework-matrix:` (no namespace prefix on the per-package task).
    - `agent-brain-mcp/Taskfile.yml` `framework-matrix:` cmds invoke `run_framework_matrix.sh` via `git rev-parse --show-toplevel`.
    - Root `Taskfile.yml` mentions `mcp:framework-matrix` in a COMMENT and has NO root-level `framework-matrix:` task block (grep `^\s*framework-matrix:` on root Taskfile returns nothing).
    - `task mcp:framework-matrix` (with `FRAMEWORK_MATRIX` unset) resolves the namespaced target WITHOUT a "Found multiple tasks" error, prints the `slow + opt-in` message, and exits 0.
    - `task mcp:framework-matrix -- --force` forwards `--force` to the runner (the runner then attempts the matrix; CLI_ARGS forwarding is present in the cmd).
  </acceptance_criteria>
  <done>`task mcp:framework-matrix` resolves cleanly via the include alias (no cyclic collision), runs the gated runner, and the root Taskfile documents it without a colliding task block.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify the gate never leaks into before-push</name>
  <action>Human-verify checkpoint (no code change). Run the steps in how-to-verify to confirm the gated target is a clean no-op when the gate is unset and that `task before-push` never invokes the framework matrix.</action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && task mcp:framework-matrix 2>&1 | grep -q "slow + opt-in"</automated>
  </verify>
  <done>Gate-unset path is a no-op exit 0 printing the opt-in message; the root before-push cmds block contains zero framework-matrix references.</done>
  <what-built>
    `task mcp:framework-matrix` (opt-in, gated) + `scripts/run_framework_matrix.sh` (sequential self-bootstrap runner). The whole point of the gating is that this slow target NEVER runs during a normal developer push.
  </what-built>
  <how-to-verify>
    Run, from the repo root, in order:
    1. `task mcp:framework-matrix` — confirm it prints the `slow + opt-in; set FRAMEWORK_MATRIX=1` message and exits 0 with NO pytest/pnpm output (the no-op path).
    2. `grep -rn "framework-matrix\|run_framework_matrix" Taskfile.yml | grep -v "^#" | grep -v "comment"` — confirm the only NON-comment hit in the root before-push CHAIN is none (the `before-push:` task at lines ~196-222 must not list framework-matrix). Visually confirm the `before-push:` cmds block contains no framework-matrix reference.
    3. `task before-push` — confirm it exits 0 AND its output never shows a framework bootstrap / `pnpm test` / `-m framework` run. (This is the expensive full gate; allow it to run to completion — it must stay green at the Phase 62 baseline of 544 passed, 111 deselected.)
    4. `FRAMEWORK_MATRIX=1 task mcp:framework-matrix -- ` (optional, only if you have OPENAI_API_KEY + bootstrapped venvs available locally) — confirm it begins bootstrapping `openai-agents` first; you may Ctrl-C after the first framework starts since the full run is slow.
    Type "approved" if (1) is a clean no-op, (2)+(3) prove before-push never invokes the matrix, or describe what leaked.
  </how-to-verify>
  <read_first>
    - Taskfile.yml (root before-push chain lines 196-222 — confirm no framework-matrix reference)
  </read_first>
  <acceptance_criteria>
    - `task mcp:framework-matrix` with the gate unset exits 0 and prints the opt-in message (no test run).
    - `task before-push` exits 0 and its command chain (root Taskfile lines 196-222) contains zero references to framework-matrix / run_framework_matrix / `-m framework` / `pnpm test`.
    - Grepping the root `before-push:` cmds block for `framework-matrix` returns 0 matches.
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe what leaked into before-push.</resume-signal>
</task>

</tasks>

<verification>
- `task mcp:framework-matrix` resolves via the `includes: mcp:` alias with no cyclic-include error.
- Gate-unset path is a no-op exit 0 with the documented opt-in message.
- `task before-push` stays green and never triggers the matrix.
- The runner sequences all 5 Python bootstraps + the TS `pnpm test` leg with per-framework teardown.
</verification>

<success_criteria>
- TOOLING-V3-01 closed: `task mcp:framework-matrix` runs all 7 framework smoke tests sequentially with per-framework setup/teardown, gated behind `FRAMEWORK_MATRIX=1`/`--force`, documented slow+opt-in in the Taskfile.
- No cyclic-include collision (bare per-package name + root comment-only).
- Matrix is absent from the before-push chain (grep-verified).
</success_criteria>

<output>
After completion, create `.planning/phases/63-tooling-docs-integration-page/63-01-SUMMARY.md`
</output>
