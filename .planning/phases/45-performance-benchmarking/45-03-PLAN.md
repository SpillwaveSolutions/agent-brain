---
phase: 45-performance-benchmarking
plan: 03
type: execute
wave: 2
depends_on: ["45-02"]
files_modified:
  - docs/BENCHMARKS.md
autonomous: false
requirements: [PERF-03]

must_haves:
  truths:
    - "BENCHMARKS.md exists with baseline latency numbers"
    - "BENCHMARKS.md includes run metadata: date, OS, Python version, backend, graph-enabled, corpus identity, chunk count, warmups, iterations"
    - "BENCHMARKS.md states which modes are unsupported and why"
    - "Baseline was generated after --prepare-docs-corpus (fresh corpus)"
  artifacts:
    - path: "docs/BENCHMARKS.md"
      provides: "Baseline benchmark results documentation"
      contains: "Baseline"
      min_lines: 30
  key_links:
    - from: "docs/BENCHMARKS.md"
      to: "scripts/query_benchmark.py"
      via: "generated from --json output"
      pattern: "query_benchmark"
---

<objective>
Generate baseline benchmark numbers by running the benchmark script against a live server with --prepare-docs-corpus, then create docs/BENCHMARKS.md from the captured JSON output.

Purpose: Without documented baselines, there is no reference point for detecting regressions. The BENCHMARKS.md file records the initial measurement.
Output: docs/BENCHMARKS.md with baseline latency tables and full run metadata.
</objective>

<execution_context>
@/Users/richardhightower/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardhightower/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-performance-benchmarking/45-CONTEXT.md
@.planning/phases/45-performance-benchmarking/45-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run baseline benchmark and create BENCHMARKS.md</name>
  <files>docs/BENCHMARKS.md</files>
  <read_first>
    - scripts/query_benchmark.py
    - scripts/benchmark_queries.json
    - .planning/phases/45-performance-benchmarking/45-CONTEXT.md
  </read_first>
  <action>
This task requires a live Agent Brain server. Steps:

1. Start the server if not running: `cd agent-brain-server and poetry run agent-brain-serve` in background (or verify it is already running via `curl http://127.0.0.1:8000/health`)

2. Run the benchmark with fresh corpus preparation and JSON output:
   ```
   python scripts/query_benchmark.py --prepare-docs-corpus --json > /tmp/benchmark_baseline.json
   ```

3. Also run with Rich table output to stdout for visual verification:
   ```
   python scripts/query_benchmark.py
   ```

4. Read the JSON output from /tmp/benchmark_baseline.json and extract all metadata and results.

5. Create docs/BENCHMARKS.md with the following structure, filling in REAL values from the JSON:

The file must contain:
- Title: "Agent Brain Benchmark Results"
- "How to Run" section with the three commands (--prepare-docs-corpus, --json, quick)
- "Baseline Run" section with ALL metadata fields: Date, OS, Python, Backend, GraphRAG Enabled, Corpus, Chunks Indexed, Iterations, Warmups, Query Set
- "Client-Observed Latency (ms)" table with columns: Mode, Status, p50, p95, p99, Mean, QPS
- Exactly 5 rows in the table (vector, bm25, hybrid, graph, multi)
- Unsupported modes show their reason in the Status column with dashes for latency columns
- Supported modes with annotations (like multi on postgres) include the annotation
- "Notes" section explaining unsupported modes and measurement methodology
- "Interpreting Results" section explaining p50, p95, p99, QPS, Status

CRITICAL: Values MUST come from captured JSON. Do NOT invent numbers.
  </action>
  <verify>
    <automated>test -f /Users/richardhightower/clients/spillwave/src/agent-brain/docs/BENCHMARKS.md && grep -q "Baseline" /Users/richardhightower/clients/spillwave/src/agent-brain/docs/BENCHMARKS.md && grep -q "p50" /Users/richardhightower/clients/spillwave/src/agent-brain/docs/BENCHMARKS.md && grep -q "Python" /Users/richardhightower/clients/spillwave/src/agent-brain/docs/BENCHMARKS.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - docs/BENCHMARKS.md exists
    - grep -q "Baseline" docs/BENCHMARKS.md returns 0
    - grep -q "p50" docs/BENCHMARKS.md returns 0
    - grep -q "p95" docs/BENCHMARKS.md returns 0
    - grep -q "p99" docs/BENCHMARKS.md returns 0
    - grep -q "QPS" docs/BENCHMARKS.md returns 0
    - grep -q "Backend" docs/BENCHMARKS.md returns 0
    - grep -q "GraphRAG" docs/BENCHMARKS.md returns 0
    - grep -q "Chunks" docs/BENCHMARKS.md returns 0
    - grep -q "Iterations" docs/BENCHMARKS.md returns 0
    - grep -q "Python" docs/BENCHMARKS.md returns 0
    - grep -q "benchmark_queries.json" docs/BENCHMARKS.md returns 0
    - Table has all 5 modes (vector, bm25, hybrid, graph, multi)
    - Latency values are real numbers, not placeholder text
  </acceptance_criteria>
  <done>docs/BENCHMARKS.md contains baseline benchmark results with real latency data from a --prepare-docs-corpus run, including all required metadata fields</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify baseline benchmark results</name>
  <files>docs/BENCHMARKS.md</files>
  <action>
    Present docs/BENCHMARKS.md to the user for verification. The user should confirm that:
    1. Metadata section has real date, OS, Python version, backend type, graph status
    2. Latency table has 5 rows (vector, bm25, hybrid, graph, multi)
    3. Unsupported modes show reason (e.g., "UNSUPPORTED: Chroma-only" for graph on Postgres)
    4. Numbers look reasonable (typical p50 for local queries: 10-500ms range)
  </action>
  <verify>User confirms results are reasonable</verify>
  <done>User has approved the baseline benchmark results</done>
</task>

</tasks>

<verification>
- docs/BENCHMARKS.md exists with real baseline data
- Metadata includes all 9 required fields (date, OS, Python, backend, graph-enabled, corpus, chunks, warmups, iterations)
- Table has exactly 5 mode rows
- Unsupported modes annotated with reason
</verification>

<success_criteria>
1. docs/BENCHMARKS.md has real latency numbers from actual benchmark run
2. All 9 metadata fields present with real values
3. All 5 modes represented (ok or unsupported with reason)
4. User has verified the results look reasonable
</success_criteria>

<output>
After completion, create `.planning/phases/45-performance-benchmarking/45-03-SUMMARY.md`
</output>
