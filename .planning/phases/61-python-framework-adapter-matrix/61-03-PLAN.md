---
phase: 61-python-framework-adapter-matrix
plan: 03
type: execute
wave: 2
depends_on: ["61-01"]
files_modified:
  - framework-matrix/langchain/requirements.txt
  - framework-matrix/langchain/test_langchain_smoke.py
  - framework-matrix/langchain/README.md
  - framework-matrix/llama-index/requirements.txt
  - framework-matrix/llama-index/test_llama_index_smoke.py
  - framework-matrix/llama-index/README.md
autonomous: true
requirements: [FRAME-02, FRAME-03]
user_setup: []
must_haves:
  truths:
    - "LangChain (via langchain-mcp-adapters) connects to agent-brain-mcp, loads the search_documents tool, invokes it, and gets a non-empty result"
    - "LlamaIndex (via llama-index-tools-mcp) connects to agent-brain-mcp, exposes search_documents as a tool, calls it, and gets a non-empty result"
    - "Both SDKs + their MCP adapters are pinned in isolated requirements files with source URL + pin-date comments"
    - "Both tests are keyless, offline against the seeded corpus, <30s, orphan-free"
  artifacts:
    - path: "framework-matrix/langchain/requirements.txt"
      provides: "pinned langchain + langchain-mcp-adapters with source + pin-date comments"
      contains: "langchain-mcp-adapters=="
    - path: "framework-matrix/langchain/test_langchain_smoke.py"
      provides: "one @pytest.mark.framework test asserting non-empty search_documents via langchain-mcp-adapters"
      min_lines: 40
    - path: "framework-matrix/llama-index/requirements.txt"
      provides: "pinned llama-index-tools-mcp with source + pin-date comments"
      contains: "llama-index-tools-mcp=="
    - path: "framework-matrix/llama-index/test_llama_index_smoke.py"
      provides: "one @pytest.mark.framework test asserting non-empty search_documents via llama-index-tools-mcp"
      min_lines: 40
  key_links:
    - from: "framework-matrix/langchain/test_langchain_smoke.py"
      to: "langchain_mcp_adapters (MultiServerMCPClient / load_mcp_tools)"
      via: "stdio connect -> load tools -> invoke search_documents -> assert non-empty"
      pattern: "langchain_mcp_adapters|load_mcp_tools|MultiServerMCPClient"
    - from: "framework-matrix/llama-index/test_llama_index_smoke.py"
      to: "llama_index.tools.mcp (McpToolSpec / BasicMCPClient)"
      via: "stdio connect -> to_tool_list -> call search_documents -> assert non-empty"
      pattern: "llama_index.tools.mcp|McpToolSpec|BasicMCPClient"
---

<objective>
FRAME-02 + FRAME-03: smoke-test LangChain (via `langchain-mcp-adapters`) and
LlamaIndex (via `llama-index-tools-mcp`) against `agent-brain-mcp` over stdio.
Each test connects, surfaces the `search_documents` tool through the framework's
MCP adapter, invokes it with a query that hits the seeded corpus, and asserts a
non-empty result. Both are stdio-only, keyless (no LLM agent loop), offline,
<30s, orphan-free.

Purpose: these two frameworks share an identical test shape (stdio adapter ->
load tools -> call -> assert) and have no cross-dependency, so they pair in one
plan and run in parallel with 61-02 and 61-04. Each still gets its OWN isolated
venv + pinned requirements per the dependency-isolation decision.

Output: `framework-matrix/langchain/*` and `framework-matrix/llama-index/*`.
</objective>

<execution_context>
@/Users/richardhightower/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardhightower/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/61-python-framework-adapter-matrix/61-CONTEXT.md

# Shared harness this plan consumes (created in Plan 61-01)
@framework-matrix/conftest.py
@framework-matrix/_harness.py
@framework-matrix/bootstrap_venv.sh

# stdio connect reference
@agent-brain-mcp/tests/test_e2e_stdio.py

<interfaces>
<!-- Both adapters churn; the executor MUST fetch current docs via ctx7 before
     pinning + writing connect calls. -->
LangChain MCP adapter. PyPI: langchain-mcp-adapters. Typical surface:
  from langchain_mcp_adapters.client import MultiServerMCPClient
  client = MultiServerMCPClient({"agent-brain": {"command": ..., "args": [...],
                                                  "transport": "stdio", "env": {...}}})
  tools = await client.get_tools()           # LangChain BaseTool list
  # pick the search_documents tool; await tool.ainvoke({"query": ...})
(Older versions: load_mcp_tools(session) against an mcp ClientSession.)

LlamaIndex MCP adapter. PyPI: llama-index-tools-mcp. Typical surface:
  from llama_index.tools.mcp import BasicMCPClient, McpToolSpec
  mcp_client = BasicMCPClient(command_or_url=..., args=[...], env={...})  # stdio
  tool_spec = McpToolSpec(client=mcp_client)
  tools = await tool_spec.to_tool_list_async()
  # find search_documents; await tool.acall(query=...)

From Plan 61-01 (_harness.py): SMOKE_TOOL="search_documents",
SMOKE_QUERY, SMOKE_ARGS={"query": SMOKE_QUERY}, stdio_server_params(state_dir),
assert_non_empty_search(result).
From Plan 61-01 (conftest.py): seeded_mcp_server (session state_dir fixture).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FRAME-02 LangChain - pin langchain-mcp-adapters + stdio smoke test</name>
  <files>framework-matrix/langchain/requirements.txt, framework-matrix/langchain/README.md, framework-matrix/langchain/test_langchain_smoke.py</files>
  <read_first>
    - framework-matrix/_harness.py (SMOKE_TOOL, SMOKE_ARGS, stdio_server_params, assert_non_empty_search)
    - framework-matrix/conftest.py (seeded_mcp_server fixture)
    - framework-matrix/bootstrap_venv.sh (venv this requirements file feeds)
    - .planning/phases/61-python-framework-adapter-matrix/61-CONTEXT.md (FRAME-02 = langchain-mcp-adapters; per-framework venv + pin decision)
  </read_first>
  <behavior>
    - test_langchain_search_returns_results: langchain-mcp-adapters connects to
      agent-brain-mcp over stdio (using stdio_server_params from the seeded
      server) -> get tools -> the search_documents tool is present -> invoke it
      with {"query": SMOKE_QUERY} -> assert_non_empty_search passes (>=1 result).
    - Skips cleanly via seeded_mcp_server when prerequisites absent.
  </behavior>
  <action>
    Fetch current docs:
    `npx ctx7@latest library "langchain-mcp-adapters" "MultiServerMCPClient stdio get_tools load_mcp_tools"`
    then `npx ctx7@latest docs <id> "connect stdio command args call tool ainvoke"`.

    Create `framework-matrix/langchain/requirements.txt` pinning EXACT versions of
    `langchain-mcp-adapters` plus the minimal `langchain-core` it needs, each with
    a `# source: <pypi-url>  pinned: <YYYY-MM-DD>` comment. Do NOT pull a full
    `langchain` model stack or any provider extras - only what the MCP adapter
    import path requires (keyless).

    Create `framework-matrix/langchain/README.md` (3-4 lines: FRAME-02, bootstrap +
    run commands like Plan 61-02's README).

    Create `framework-matrix/langchain/test_langchain_smoke.py`:
    - `pytestmark = pytest.mark.framework`.
    - `async def test_langchain_search_returns_results(seeded_mcp_server)`:
      - `command, args, env = stdio_server_params(seeded_mcp_server)`.
      - Build the adapter connection per the ctx7 docs. Preferred (newer API):
        `MultiServerMCPClient({"agent-brain": {"command": command, "args": args,
        "transport": "stdio", "env": env}})`; `tools = await client.get_tools()`.
      - Find the tool whose `.name == SMOKE_TOOL`; assert it exists.
      - Invoke it: `result = await tool.ainvoke(SMOKE_ARGS)` (LangChain BaseTool
        ainvoke takes the args dict).
      - `assert_non_empty_search(result)` (the helper normalizes the LangChain
        ToolMessage/string/structured return; if it returns a JSON string, the
        helper parses it - or extract `.content` and pass that).
    Keep <30s (session-scoped server). Rely on the Plan-61-01 session orphan-guard.
  </action>
  <verify>
    <automated>grep -Eq "langchain-mcp-adapters==[0-9]" framework-matrix/langchain/requirements.txt && grep -Eq "pinned: 20[0-9]{2}-[0-9]{2}-[0-9]{2}" framework-matrix/langchain/requirements.txt && cd framework-matrix && python -c "import ast; ast.parse(open('langchain/test_langchain_smoke.py').read())" && grep -q "search_documents" langchain/test_langchain_smoke.py && grep -q "assert_non_empty_search" langchain/test_langchain_smoke.py && grep -q "seeded_mcp_server" langchain/test_langchain_smoke.py</automated>
  </verify>
  <acceptance_criteria>
    - `framework-matrix/langchain/requirements.txt` pins `langchain-mcp-adapters==<version>` exactly with a `# source:` URL + `pinned: YYYY-MM-DD` comment; no provider/model extras.
    - `framework-matrix/langchain/test_langchain_smoke.py` parses, is `framework`-marked, consumes `seeded_mcp_server`, surfaces the `search_documents` tool through the adapter, invokes it, and runs `assert_non_empty_search`.
    - `framework-matrix/langchain/README.md` documents bootstrap + run.
    - After `sh framework-matrix/bootstrap_venv.sh langchain`, `framework-matrix/langchain/.venv/bin/pytest framework-matrix/langchain/ -m framework` exits 0 (or SKIPs without OPENAI_API_KEY) in <30s.
  </acceptance_criteria>
  <done>LangChain connects to agent-brain-mcp via langchain-mcp-adapters, invokes search_documents, and asserts a non-empty result against the seeded corpus.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FRAME-03 LlamaIndex - pin llama-index-tools-mcp + stdio smoke test</name>
  <files>framework-matrix/llama-index/requirements.txt, framework-matrix/llama-index/README.md, framework-matrix/llama-index/test_llama_index_smoke.py</files>
  <read_first>
    - framework-matrix/_harness.py (SMOKE_TOOL, SMOKE_ARGS, stdio_server_params, assert_non_empty_search)
    - framework-matrix/conftest.py (seeded_mcp_server fixture)
    - framework-matrix/bootstrap_venv.sh (venv this requirements file feeds)
    - .planning/phases/61-python-framework-adapter-matrix/61-CONTEXT.md (FRAME-03 = llama-index-tools-mcp)
  </read_first>
  <behavior>
    - test_llama_index_search_returns_results: llama-index-tools-mcp connects to
      agent-brain-mcp over stdio (using stdio_server_params) -> to_tool_list
      exposes search_documents -> call it with query=SMOKE_QUERY ->
      assert_non_empty_search passes (>=1 result).
    - Skips cleanly via seeded_mcp_server when prerequisites absent.
  </behavior>
  <action>
    Fetch current docs:
    `npx ctx7@latest library "llama-index-tools-mcp" "BasicMCPClient McpToolSpec stdio to_tool_list call tool"`
    then `npx ctx7@latest docs <id> "BasicMCPClient command args stdio McpToolSpec to_tool_list_async acall"`.

    Create `framework-matrix/llama-index/requirements.txt` pinning EXACT versions of
    `llama-index-tools-mcp` plus the minimal `llama-index-core` it imports, each
    with a `# source: <pypi-url>  pinned: <YYYY-MM-DD>` comment. No LLM/embedding
    provider extras (keyless).

    Create `framework-matrix/llama-index/README.md` (3-4 lines: FRAME-03, bootstrap
    + run commands).

    Create `framework-matrix/llama-index/test_llama_index_smoke.py`:
    - `pytestmark = pytest.mark.framework`.
    - `async def test_llama_index_search_returns_results(seeded_mcp_server)`:
      - `command, args, env = stdio_server_params(seeded_mcp_server)`.
      - Per ctx7 docs: construct a stdio `BasicMCPClient` from command/args/env
        (newer API takes `command_or_url=command, args=args, env=env`); wrap in
        `McpToolSpec(client=mcp_client)`; `tools = await
        tool_spec.to_tool_list_async()`.
      - Find the FunctionTool whose name == SMOKE_TOOL (the adapter may prefix or
        suffix tool names - match by substring `search_documents` if exact match
        fails, and assert at least one matches).
      - Call it: `result = await tool.acall(**SMOKE_ARGS)` (i.e. `query=SMOKE_QUERY`).
      - `assert_non_empty_search(result)` (helper normalizes LlamaIndex ToolOutput;
        if needed, pass `result.raw_output` or `str(result)` / `result.content`).
    Keep <30s. Rely on the session orphan-guard.
  </action>
  <verify>
    <automated>grep -Eq "llama-index-tools-mcp==[0-9]" framework-matrix/llama-index/requirements.txt && grep -Eq "pinned: 20[0-9]{2}-[0-9]{2}-[0-9]{2}" framework-matrix/llama-index/requirements.txt && cd framework-matrix && python -c "import ast; ast.parse(open('llama-index/test_llama_index_smoke.py').read())" && grep -q "search_documents" llama-index/test_llama_index_smoke.py && grep -q "assert_non_empty_search" llama-index/test_llama_index_smoke.py && grep -q "seeded_mcp_server" llama-index/test_llama_index_smoke.py</automated>
  </verify>
  <acceptance_criteria>
    - `framework-matrix/llama-index/requirements.txt` pins `llama-index-tools-mcp==<version>` exactly with `# source:` URL + `pinned: YYYY-MM-DD` comment; no provider extras.
    - `framework-matrix/llama-index/test_llama_index_smoke.py` parses, is `framework`-marked, consumes `seeded_mcp_server`, surfaces `search_documents` via McpToolSpec/BasicMCPClient, calls it, and runs `assert_non_empty_search`.
    - `framework-matrix/llama-index/README.md` documents bootstrap + run.
    - After `sh framework-matrix/bootstrap_venv.sh llama-index`, `framework-matrix/llama-index/.venv/bin/pytest framework-matrix/llama-index/ -m framework` exits 0 (or SKIPs without OPENAI_API_KEY) in <30s.
  </acceptance_criteria>
  <done>LlamaIndex connects to agent-brain-mcp via llama-index-tools-mcp, calls search_documents, and asserts a non-empty result against the seeded corpus.</done>
</task>

</tasks>

<verification>
- Both `requirements.txt` files pin their MCP adapter exactly with source URL + pin date in isolated per-framework dirs.
- Both smoke tests surface `search_documents` through the framework adapter and assert non-empty results.
- Both tests are `framework`-marked (opt-in) and NOT collected by `task before-push`.
- With prerequisites present, both pass <30s with zero orphan subprocesses.
</verification>

<success_criteria>
- ROADMAP Phase 61 SC2 met for LangChain + LlamaIndex: each connects to agent-brain-mcp, calls search_documents, asserts non-empty results.
- Each isolated venv resolves with no pip upgrade messages (bootstrap pin-freshness check passes).
</success_criteria>

<output>
After completion, create `.planning/phases/61-python-framework-adapter-matrix/61-03-SUMMARY.md`
</output>
