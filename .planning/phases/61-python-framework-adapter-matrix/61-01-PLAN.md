---
phase: 61-python-framework-adapter-matrix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - framework-matrix/README.md
  - framework-matrix/conftest.py
  - framework-matrix/_harness.py
  - framework-matrix/pytest.ini
  - framework-matrix/bootstrap_venv.sh
autonomous: true
requirements: [FRAME-01]
must_haves:
  truths:
    - "A live agent-brain-serve + indexed tiny corpus can be spun up once and shared by every framework smoke test"
    - "A live agent-brain-mcp --transport http listener on a free loopback port can be spun up for the FRAME-01 streamable-http leg"
    - "The `framework` pytest marker is registered and the framework-matrix suite is opt-in (NOT collected by task before-push)"
    - "A per-framework venv can be bootstrapped from a pinned requirements file with zero pip upgrade messages"
    - "Each framework test gets a deterministic teardown path so zero orphan subprocesses survive between tests"
  artifacts:
    - path: "framework-matrix/conftest.py"
      provides: "session-scoped seeded_mcp_server (real agent-brain-serve + indexed corpus over UDS) + http_mcp_listener (real agent-brain-mcp --transport http binary) fixtures + framework marker auto-tagging"
      min_lines: 120
    - path: "framework-matrix/_harness.py"
      provides: "keyless connect→call search_documents→assert non-empty helper contract (normalizes 5 framework result shapes) + corpus dict + stdio params builder + orphan-free assertion helper"
      min_lines: 60
    - path: "framework-matrix/bootstrap_venv.sh"
      provides: "per-framework venv creation from framework-matrix/<framework>/requirements.txt with --no-deps-upgrade verification"
      min_lines: 30
    - path: "framework-matrix/pytest.ini"
      provides: "framework marker registration + addopts that does NOT auto-run anything in before-push"
      contains: "framework:"
  key_links:
    - from: "framework-matrix/conftest.py"
      to: "agent-brain-cli/tests/integration/_corpus.py:start_seeded_server"
      via: "mirrored real-server seeding logic (Popen agent-brain-serve + POST /index/ + poll /health/status)"
      pattern: "agent-brain-serve"
    - from: "framework-matrix/conftest.py"
      to: "agent-brain-mcp --transport http"
      via: "real agent-brain-mcp --transport http binary, /healthz readiness poll, SIGTERM→SIGKILL teardown — NOT the fake-server fixture"
      pattern: "transport.*http"
---

<objective>
Build the shared foundation for the Phase 61 Python framework adapter matrix: a
new top-level `framework-matrix/` directory containing (1) a session-scoped
harness that spins up ONE real `agent-brain-serve` with an indexed tiny corpus
(so `search_documents` returns non-empty results) shared across all 5 framework
smoke tests, (2) a `agent-brain-mcp --transport http` loopback listener fixture
for the FRAME-01 streamable-http leg, (3) the `framework` pytest marker that is
opt-in and NEVER collected by `task before-push`, (4) a keyless
connect→call→assert helper contract, and (5) a per-framework venv bootstrap
script enforcing exact pins.

Purpose: every per-framework plan (61-02..61-04) implements against these
contracts instead of re-deriving the harness. This plan owns the shared HTTP
fixture that FRAME-01's `MCPServerStreamableHttp` leg consumes, hence FRAME-01
appears here.

Output: `framework-matrix/{README.md, conftest.py, _harness.py, pytest.ini,
bootstrap_venv.sh}` — a runnable (but framework-test-empty) harness skeleton.
</objective>

<execution_context>
@/Users/richardhightower/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardhightower/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-python-framework-adapter-matrix/61-CONTEXT.md

# Canonical harness to mirror (REAL agent-brain-serve + indexed corpus seeder)
@agent-brain-cli/tests/integration/_corpus.py
@agent-brain-cli/tests/integration/test_resources_e2e.py

# REAL `agent-brain-mcp --transport http` binary spawner + /healthz readiness
# poll + /mcp mount path. THIS is the HTTP-listener reference for the
# http_mcp_listener fixture. NOTE: this reference file tears down with SIGINT;
# our http_mcp_listener MUST instead use SIGTERM→wait(grace)→SIGKILL per the
# Phase 60 hygiene contract (see _corpus.py:start_seeded_server teardown).
@agent-brain-mcp/tests/test_cli_backends_query_wire.py

# Marker registration + addopts deselection precedent (the `stress` marker)
@agent-brain-mcp/pyproject.toml

# search_documents integration target + its input schema
@agent-brain-mcp/agent_brain_mcp/tools/search.py

<!-- DO NOT use agent-brain-mcp/tests/e2e/conftest.py as a seeding reference:
     its `indexed_server` fixture is an unimplemented Phase-4 STUB that calls
     pytest.skip(...) and yields {}. Copying from it produces a
     seeded_mcp_server that always skips. Mirror
     agent-brain-cli/tests/integration/_corpus.py:start_seeded_server
     EXCLUSIVELY for the seeding logic. -->

<interfaces>
<!-- Contracts the executor builds against. Extracted from the codebase. -->

search_documents tool input (agent-brain-mcp/agent_brain_mcp/schemas.py):
```python
class SearchDocumentsInput(BaseModel):
    query: str
    mode: Literal["semantic","bm25","hybrid","graph","multi"] = "hybrid"
    top_k: int = 10            # ge=1 le=100
    similarity_threshold: float = 0.0
    alpha: float = 0.5
    # source_types / languages / file_paths / explain are optional
```
Tool call name over MCP wire: "search_documents". Minimal args: {"query": "<text>"}.
A non-empty result means the SDK response carries a non-empty results list /
structuredContent with total_results >= 1.

Real-server seeder (agent-brain-cli/tests/integration/_corpus.py):
```python
def prerequisites_available() -> tuple[bool, str]   # checks OPENAI_API_KEY + agent-brain-serve/mcp/agent-brain on PATH
@contextmanager
def start_seeded_server(state_dir: Path, corpus: Mapping[str,str]) -> Iterator[Path]
# Popen(["agent-brain-serve"]) with AGENT_BRAIN_STATE_DIR + AGENT_BRAIN_UDS=1
#   + AGENT_BRAIN_UDS_PATH=<state>/.agent-brain/agent-brain.sock,
# POST /index/ {folder_path, force:false, recursive:true},
# poll /health/status until indexing_in_progress==false AND total_documents>0,
# teardown SIGTERM→wait 10s→SIGKILL + pkill -f agent-brain-mcp.
```

agent-brain-mcp HTTP listener invocation (agent-brain-mcp/agent_brain_mcp/cli.py;
real-binary spawn + readiness pattern in
agent-brain-mcp/tests/test_cli_backends_query_wire.py):
```
agent-brain-mcp --transport http --host 127.0.0.1 --port <free> [--backend uds --state-dir <dir>]
# loopback-only; mount path is "/mcp"; readiness probe GET http://127.0.0.1:<port>/healthz -> 200
```

marker/addopts precedent (agent-brain-mcp/pyproject.toml):
```toml
markers = [ "stress: ... opt-in via `task mcp:stress:orphan-test`; NOT in task before-push" ]
addopts = "-m 'not e2e and not e2e_http and not contract and not stress'"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create framework-matrix/ layout + README + per-framework venv bootstrap script</name>
  <files>framework-matrix/README.md, framework-matrix/bootstrap_venv.sh</files>
  <read_first>
    - .planning/phases/61-python-framework-adapter-matrix/61-CONTEXT.md (dependency-isolation decision: per-framework venvs, no shared requirements.txt; "no pip upgrade messages" acceptance signal)
    - agent-brain-cli/tests/integration/_corpus.py (prerequisites_available pattern to reuse)
  </read_first>
  <action>
    Create the new top-level directory `framework-matrix/` (does not exist yet).

    1. `framework-matrix/README.md` — one short paragraph stating: this is the
       Phase 61/62 framework adapter matrix; tests are OPT-IN (run with the
       `framework` pytest marker, never in `task before-push`); each framework
       lives in its own subdir `framework-matrix/<framework>/` with its OWN
       isolated venv + pinned `requirements.txt`; the operator Taskfile target +
       nightly CI land in Phase 63 (this phase ships tests + harness only).
       Document the per-framework subdir convention: `openai-agents/`,
       `langchain/`, `llama-index/`, `pydantic-ai/`, `autogen/`.

    2. `framework-matrix/bootstrap_venv.sh` — a POSIX `sh` script taking one arg
       `<framework>` (e.g. `openai-agents`). It must:
       - `set -euo pipefail`
       - FIRST, before resolving any path: `cd "$(git rev-parse --show-toplevel)"`
         (fall back to `cd "$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)"` when
         not inside a git work tree) so that the repo-root-relative local package
         paths (`./agent-brain-server` etc.) AND
         `framework-matrix/$1/requirements.txt` resolve regardless of the cwd the
         script was invoked from. (framework-matrix/ is at repo root.)
       - resolve `DIR="framework-matrix/$1"`; error out (exit 2) if
         `$DIR/requirements.txt` is missing.
       - create `$DIR/.venv` via `python3 -m venv "$DIR/.venv"` if absent.
       - `"$DIR/.venv/bin/pip" install --upgrade pip >/dev/null`
       - install the framework requirements AND the local Agent Brain
         packages (server, uds, mcp, cli) into the same venv so
         `agent-brain-serve` / `agent-brain-mcp` / `agent-brain` resolve on the
         venv PATH:
         `"$DIR/.venv/bin/pip" install -r "$DIR/requirements.txt"`
         then `"$DIR/.venv/bin/pip" install ./agent-brain-server ./agent-brain-uds ./agent-brain-mcp ./agent-brain-cli`
         (use whatever install form the repo already uses — path installs;
         because we cd'd to repo root above, these relative paths resolve).
       - PIN-FRESHNESS CHECK: after install, run
         `"$DIR/.venv/bin/pip" install -r "$DIR/requirements.txt" 2>&1 | tee /tmp/ab_fwm_$1.log`
         and `grep -q "Requirement already satisfied" /tmp/ab_fwm_$1.log` while
         asserting NO line matching `^Collecting ` and NO line matching
         `Successfully installed` (i.e. a second install is a no-op). If a second
         install collects/installs anything, exit 3 with a message naming the
         drifted package — this is the "no pip upgrade messages" acceptance
         mechanism from CONTEXT.
       - echo a final `bootstrapped <framework> venv at $DIR/.venv` line.
       Make it executable (`chmod +x`).
  </action>
  <verify>
    <automated>test -f framework-matrix/README.md && test -x framework-matrix/bootstrap_venv.sh && sh -n framework-matrix/bootstrap_venv.sh && grep -q "framework" framework-matrix/README.md && grep -q "rev-parse --show-toplevel" framework-matrix/bootstrap_venv.sh && grep -q "no-op\|already satisfied\|Collecting" framework-matrix/bootstrap_venv.sh</automated>
  </verify>
  <acceptance_criteria>
    - `framework-matrix/README.md` exists and contains the word `framework` and the phrase `before-push` (documenting opt-in).
    - `framework-matrix/bootstrap_venv.sh` exists, is executable (`test -x`), passes `sh -n` syntax check, takes a `<framework>` arg, cd's to repo root via `git rev-parse --show-toplevel` (so local package paths resolve from any cwd), and contains the pin-freshness re-install no-op check (greps for `Collecting`/`already satisfied`).
    - README documents the 5 per-framework subdir names: `openai-agents`, `langchain`, `llama-index`, `pydantic-ai`, `autogen`.
  </acceptance_criteria>
  <done>framework-matrix/ exists with README + executable bootstrap script that cd's to repo root and enforces exact pins via a re-install no-op check.</done>
</task>

<task type="auto">
  <name>Task 2: framework marker registration + opt-in pytest config (NOT in before-push)</name>
  <files>framework-matrix/pytest.ini</files>
  <read_first>
    - agent-brain-mcp/pyproject.toml (lines 101-108: markers list + addopts deselection of e2e/e2e_http/contract/stress)
    - .planning/phases/61-python-framework-adapter-matrix/61-CONTEXT.md (CI-gating decision: opt-in, mirror the stress-marker precedent, NOT wired into gating path)
  </read_first>
  <action>
    Create `framework-matrix/pytest.ini` (standalone — `framework-matrix/` is NOT
    part of any package's pytest rootdir, so the marker lives here and the suite
    is invoked explicitly from this dir). Contents:

    ```ini
    [pytest]
    asyncio_mode = auto
    markers =
        framework: Python LLM-framework adapter smoke test (FRAME-01..05) — connects to a live agent-brain-mcp, calls search_documents, asserts non-empty results. OPT-IN: the Taskfile target + nightly CI land in Phase 63; NEVER collected by `task before-push`. Each framework runs in its OWN isolated venv (framework-matrix/<fw>/.venv).
    addopts = -m framework
    ```

    The `addopts = -m framework` means a bare `pytest framework-matrix/` only runs
    framework-marked tests, and — crucially — NOTHING in `framework-matrix/` is on
    any package's collection path. Verify the package default runs are untouched:
    `task before-push` collects only agent-brain-server/uds/mcp/cli test trees,
    none of which import `framework-matrix/`. Do NOT add `framework-matrix/` to any
    package pyproject `testpaths`, and do NOT touch the root Taskfile (Phase 63
    owns the operator target).

    Also confirm (read-only, no edit) that none of the four package pyproject
    `addopts`/`testpaths` reference `framework-matrix` — if any did, that would
    pull these tests into before-push.
  </action>
  <verify>
    <automated>grep -q "framework:" framework-matrix/pytest.ini && grep -q "before-push" framework-matrix/pytest.ini && ! grep -rq "framework-matrix" agent-brain-server/pyproject.toml agent-brain-cli/pyproject.toml agent-brain-mcp/pyproject.toml agent-brain-uds/pyproject.toml Taskfile.yml</automated>
  </verify>
  <acceptance_criteria>
    - `framework-matrix/pytest.ini` registers a `framework:` marker whose description contains `before-push` (documenting it is NOT in the gate).
    - `addopts = -m framework` present (default collection = framework-marked only).
    - grep proves NO package pyproject.toml NOR root Taskfile.yml references `framework-matrix` (the suite is not wired into any default/gating run).
    - `task before-push` still passes unchanged (framework tests are not on any package collection path).
  </acceptance_criteria>
  <done>The `framework` marker is registered in framework-matrix/pytest.ini, opt-in, and provably absent from every package's collection path and the root Taskfile.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Shared real-server harness (conftest fixtures) + keyless connect→call→assert helper</name>
  <files>framework-matrix/_harness.py, framework-matrix/conftest.py</files>
  <read_first>
    - agent-brain-cli/tests/integration/_corpus.py (start_seeded_server + prerequisites_available + _find_free_port + _poll_health — mirror this EXACTLY for the seeding + SIGTERM→SIGKILL teardown logic)
    - agent-brain-mcp/tests/test_cli_backends_query_wire.py (the REAL `agent-brain-mcp --transport http` binary spawn + GET /healthz readiness poll + /mcp mount path — mirror its spawn+readiness shape, but use SIGTERM→wait(grace)→SIGKILL teardown, NOT its SIGINT)
    - agent-brain-mcp/tests/stress/test_orphan_subprocess.py (psutil children-delta orphan-free assertion pattern)
    - agent-brain-mcp/agent_brain_mcp/tools/search.py (search_documents response shape)
    - DO NOT read agent-brain-mcp/tests/e2e/conftest.py for seeding — its indexed_server is a Phase-4 stub that calls pytest.skip and yields {}.
  </read_first>
  <behavior>
    - When OPENAI_API_KEY + agent-brain-serve/mcp on PATH: the `seeded_mcp_server`
      session fixture yields a state_dir whose UDS-backed server has a non-empty
      index (total_documents > 0), reusable by every framework test.
    - When prerequisites missing: every framework test SKIPs (not fails) with the
      reason string from prerequisites_available().
    - `http_mcp_listener` factory yields a live `agent-brain-mcp --transport http`
      URL `http://127.0.0.1:<port>/mcp` whose /healthz answered 200 before yield;
      teardown is SIGTERM→wait(grace)→SIGKILL (Phase 60 contract).
    - `assert_non_empty_search(call_result)` raises AssertionError with a clear
      message when the framework adapter's search_documents call returns 0 results,
      normalizing all 5 framework result shapes (see action pseudocode).
    - `assert_no_orphans(parent_pid, baseline)` raises if any agent-brain
      subprocess survived after teardown (psutil children-delta == 0 contract).
  </behavior>
  <action>
    Create `framework-matrix/_harness.py` and `framework-matrix/conftest.py`.

    `framework-matrix/_harness.py` (pure helpers, no pytest):
    - `FRAMEWORK_CORPUS: dict[str,str]` — a tiny corpus guaranteeing a non-empty
      hit. Reuse the spirit of agent-brain-mcp/tests/e2e/fixtures/tiny_corpus:
      include `auth.py` and `auth.md` with the literal token `authenticate`
      appearing in the text, plus `query_service.py`. Keep it ~4 small files.
    - `SMOKE_QUERY: str = "authenticate user login"` and
      `SMOKE_TOOL: str = "search_documents"` and
      `SMOKE_ARGS: dict = {"query": SMOKE_QUERY}` — the single canonical
      connect→call→assert input every framework adapter uses.
    - `stdio_server_params(state_dir: Path) -> tuple[str, list[str], dict[str,str]]`
      returning `(command, args, env)` for spawning `agent-brain-mcp` over stdio
      with `AGENT_BRAIN_STATE_DIR=<state_dir>/.agent-brain`, `--backend uds`. This
      is the per-framework stdio launch spec (OpenAI Agents MCPServerStdio,
      Pydantic AI MCPServerStdio, langchain/llama-index/autogen all consume it).
    - `assert_non_empty_search(results: object) -> None` — MUST normalize the 5
      distinct framework result shapes below to an integer count, then
      `assert count >= 1, "<framework> search_documents returned 0 results
      against the seeded corpus"`. Implement defensive shape-sniffing in this order
      (each framework's call_tool returns a different envelope):

        def _count(results) -> int:
            # 1. MCP SDK CallToolResult (OpenAI Agents, Pydantic AI low-level):
            #    object with .structuredContent (dict) and/or .content (list of
            #    content blocks). Prefer structuredContent.
            sc = getattr(results, "structuredContent", None)
            if sc is not None:
                return _count_payload(sc)           # dict path below
            content = getattr(results, "content", None)
            if content is not None:                  # list[TextContent|...]
                # each block may carry .text holding a JSON string with
                # {"results":[...]} / {"total_results":N}; parse first text block
                return _count_content_list(content)
            # 2. LangChain: ToolMessage with .content (str or list) OR a bare str.
            if isinstance(results, str):
                return _count_text(results)          # JSON-parse or non-empty str
            tm_content = getattr(results, "content", None)  # ToolMessage.content
            #    (already handled by the .content branch above if object-shaped)
            # 3. LlamaIndex: ToolOutput with .raw_output / .content / .raw_input;
            #    prefer .raw_output then .content.
            for attr in ("raw_output", "raw_input"):
                v = getattr(results, attr, None)
                if v is not None:
                    return _count_payload(v)
            # 4. Pydantic AI: a list of content parts (TextPart/ToolReturnPart)
            #    or a plain list of result dicts.
            if isinstance(results, list):
                return _count_list(results)          # len of non-empty entries
            # 5. Autogen McpWorkbench ToolResult: object with .result (list of
            #    TextResultContent, each .content a JSON string) and .is_error.
            tr = getattr(results, "result", None)
            if tr is not None:
                return _count_content_list(tr)
            # dict fallback (structuredContent already-unwrapped):
            if isinstance(results, dict):
                return _count_payload(results)
            return 0

      where `_count_payload(d)` reads `d["total_results"]` if present, else
      `len(d.get("results", []))`; `_count_content_list(lst)` extracts the first
      block's text (`getattr(block,"text",None)` or `block.get("text")`),
      json.loads it, and delegates to `_count_payload`, falling back to
      "non-empty text == 1"; `_count_list(lst)` returns the count of truthy
      entries; `_count_text(s)` tries json.loads→_count_payload, else
      `1 if s.strip() else 0`. Keep it tolerant: any extraction failure on a
      non-empty envelope counts as >=1 rather than raising the wrong error.
    - `assert_no_orphans(parent_pid: int, baseline: set[int]) -> None` — mirror
      tests/stress/test_orphan_subprocess.py `_children_pids` psutil delta logic;
      raise AssertionError listing surviving PIDs if `(now - baseline)` is non-empty.

    `framework-matrix/conftest.py`:
    - Copy `prerequisites_available`, `_find_free_port`, `_poll_health`,
      `start_seeded_server` LOGIC from agent-brain-cli/tests/integration/_corpus.py
      (do NOT import across package boundaries — framework-matrix has no package
      root; inline/adapt the functions). Use FRAMEWORK_CORPUS from _harness. Do
      NOT model any seeding logic on agent-brain-mcp/tests/e2e/conftest.py — that
      file's indexed_server is an unimplemented stub.
    - `@pytest.fixture(scope="session") def seeded_mcp_server(tmp_path_factory)`:
      call prerequisites_available(); `pytest.skip(reason)` if not ok; create a
      short state dir under tempfile.mkdtemp(prefix="abfwm-") (AF_UNIX-safe);
      enter start_seeded_server(state_dir, FRAMEWORK_CORPUS); yield the resolved
      state_dir. Session-scoped so all 5 framework tests share ONE server.
    - `@pytest.fixture def http_mcp_listener(seeded_mcp_server)`: a factory that
      Popens the REAL `agent-brain-mcp --transport http --host 127.0.0.1
      --port <free> --backend uds --state-dir <state>/.agent-brain` binary (NOT a
      fake python script), polls GET http://127.0.0.1:<port>/healthz until 200
      (mirror the readiness loop in
      agent-brain-mcp/tests/test_cli_backends_query_wire.py), yields
      `http://127.0.0.1:<port>/mcp`; teardown sends **SIGTERM**, `proc.wait(grace)`,
      then escalates to **SIGKILL** on TimeoutExpired (Phase 60 contract — do NOT
      use SIGINT). This is the FRAME-01 streamable-http leg's server.
    - `def pytest_collection_modifyitems(config, items)`: auto-tag every test under
      `framework-matrix/` with `pytest.mark.framework` (mirror e2e/conftest.py
      auto-mark idiom — the marker auto-tag idiom is fine to copy; only its
      seeding fixtures are off-limits) so individual test files don't each have to
      declare it.
    - Add a session-scoped autouse `_orphan_guard` fixture that snapshots
      `_children_pids(os.getpid())` at session start and, at session teardown,
      calls assert_no_orphans — proving zero orphan agent-brain subprocesses
      survive the whole matrix run (success criterion #4).
  </action>
  <verify>
    <automated>cd framework-matrix && python -c "import ast; ast.parse(open('_harness.py').read()); ast.parse(open('conftest.py').read())" && python -c "import importlib.util,sys; spec=importlib.util.spec_from_file_location('h','_harness.py'); m=importlib.util.module_from_spec(spec); spec.loader.exec_module(m); assert m.SMOKE_TOOL=='search_documents'; assert m.SMOKE_ARGS=={'query': m.SMOKE_QUERY}; assert len(m.FRAMEWORK_CORPUS)>=3; \nclass _CTR:\n  structuredContent={'results':[{'score':0.9}]}\nm.assert_non_empty_search(_CTR()); \nclass _CB:\n  structuredContent=None\n  content=[{'type':'text','text':'{\"results\":[{\"score\":0.9}]}'}]\nm.assert_non_empty_search(_CB()); print('harness ok')" && grep -q "SIGTERM" conftest.py && grep -q "transport.*http\|--transport" conftest.py</automated>
  </verify>
  <acceptance_criteria>
    - `framework-matrix/_harness.py` parses and exports `FRAMEWORK_CORPUS` (>=3 files, contains the literal token `authenticate`), `SMOKE_QUERY`, `SMOKE_TOOL == "search_documents"`, `SMOKE_ARGS == {"query": SMOKE_QUERY}`, `stdio_server_params`, `assert_non_empty_search`, `assert_no_orphans`.
    - `assert_non_empty_search` is unit-exercised by the verify command against a synthetic MCP `CallToolResult`-like object with `structuredContent={"results":[{"score":0.9}]}` AND against one with `content=[{"type":"text","text":'{"results":[{"score":0.9}]}'}]` — both MUST assert non-empty (count>=1) without raising. The helper handles all 5 enumerated shapes (CallToolResult structuredContent/content, LangChain ToolMessage/str, LlamaIndex ToolOutput, Pydantic AI content-part list, Autogen ToolResult.result).
    - `framework-matrix/conftest.py` parses and defines a session-scoped `seeded_mcp_server` fixture that calls `pytest.skip` when prerequisites missing (grep `pytest.skip`) and an `http_mcp_listener` factory fixture that Popens the real `agent-brain-mcp --transport http` binary, polls `/healthz` (grep `healthz` and `--transport`), and tears down with `SIGTERM`→`SIGKILL` (grep `SIGTERM` AND `SIGKILL`; grep proves NO `SIGINT`).
    - `conftest.py` contains a `pytest_collection_modifyitems` that adds `pytest.mark.framework` (grep `pytest.mark.framework`) and an autouse orphan-guard calling `assert_no_orphans`.
    - No cross-package import of `tests.integration._corpus` (grep returns 0 — the logic is inlined, since framework-matrix has no package root). No reference to `tests.e2e.conftest` or its `indexed_server` stub.
  </acceptance_criteria>
  <done>The shared harness exposes a session-scoped seeded real server (mirroring _corpus.py, not the e2e stub), a real-binary HTTP listener factory with SIGTERM→SIGKILL teardown, a keyless connect→call→assert helper normalizing all 5 framework result shapes, and a session orphan-guard — the contract every per-framework plan builds against.</done>
</task>

</tasks>

<verification>
- `framework-matrix/` exists with README, conftest, _harness, pytest.ini, bootstrap_venv.sh.
- `framework` marker registered in framework-matrix/pytest.ini, opt-in, NOT in any package collection path nor root Taskfile.
- `task before-push` still passes (run from repo root) — framework tests are not collected.
- Harness exports the canonical SMOKE_TOOL/SMOKE_ARGS contract + assert_non_empty_search (5-shape normalization, unit-exercised against a synthetic CallToolResult) + assert_no_orphans + seeded_mcp_server + real-binary http_mcp_listener (SIGTERM teardown).
</verification>

<success_criteria>
- A per-framework plan can `import _harness` and use `seeded_mcp_server` / `http_mcp_listener` / `stdio_server_params` / `assert_non_empty_search` without re-deriving any server-spawn logic.
- Running `pytest framework-matrix/` with no per-framework tests yet collects 0 framework tests and does not error (skeleton is import-clean).
- `task before-push` from repo root exits 0 with framework tests uncollected.
</success_criteria>

<output>
After completion, create `.planning/phases/61-python-framework-adapter-matrix/61-01-SUMMARY.md`
</output>
</content>
</invoke>
