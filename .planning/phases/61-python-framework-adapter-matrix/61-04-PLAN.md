---
phase: 61-python-framework-adapter-matrix
plan: 04
type: execute
wave: 2
depends_on: ["61-01"]
files_modified:
  - framework-matrix/pydantic-ai/requirements.txt
  - framework-matrix/pydantic-ai/test_pydantic_ai_smoke.py
  - framework-matrix/pydantic-ai/README.md
  - framework-matrix/autogen/requirements.txt
  - framework-matrix/autogen/test_autogen_smoke.py
  - framework-matrix/autogen/README.md
autonomous: true
requirements: [FRAME-04, FRAME-05]
user_setup: []
must_haves:
  truths:
    - "Pydantic AI (via MCPServerStdio) connects to agent-brain-mcp, lists tools, calls search_documents, and gets a non-empty result"
    - "Autogen/AG2 (via McpWorkbench) connects to agent-brain-mcp, lists tools, calls search_documents, and gets a non-empty result"
    - "Both SDKs + their MCP adapters are pinned in isolated requirements files with source URL + pin-date comments"
    - "Both tests are keyless, offline against the seeded corpus, <30s, orphan-free"
  artifacts:
    - path: "framework-matrix/pydantic-ai/requirements.txt"
      provides: "pinned pydantic-ai (ships MCPServerStdio) with source + pin-date comments"
      contains: "pydantic-ai=="
    - path: "framework-matrix/pydantic-ai/test_pydantic_ai_smoke.py"
      provides: "one @pytest.mark.framework test asserting non-empty search_documents via Pydantic AI MCPServerStdio"
      min_lines: 40
    - path: "framework-matrix/autogen/requirements.txt"
      provides: "pinned autogen-ext[mcp] (ships McpWorkbench) with source + pin-date comments"
      contains: "autogen-ext"
    - path: "framework-matrix/autogen/test_autogen_smoke.py"
      provides: "one @pytest.mark.framework test asserting non-empty search_documents via McpWorkbench"
      min_lines: 40
  key_links:
    - from: "framework-matrix/pydantic-ai/test_pydantic_ai_smoke.py"
      to: "pydantic_ai.mcp.MCPServerStdio"
      via: "stdio connect -> list_tools -> call_tool('search_documents', {...}) -> assert non-empty"
      pattern: "MCPServerStdio"
    - from: "framework-matrix/autogen/test_autogen_smoke.py"
      to: "autogen_ext.tools.mcp.McpWorkbench"
      via: "stdio connect -> list_tools -> call_tool('search_documents', {...}) -> assert non-empty"
      pattern: "McpWorkbench"
---

<objective>
FRAME-04 + FRAME-05: smoke-test Pydantic AI (via `MCPServerStdio`) and Autogen/AG2
(via `McpWorkbench`) against `agent-brain-mcp` over stdio. Each test connects,
lists tools, calls `search_documents` with a query that hits the seeded corpus,
and asserts a non-empty result. Both are stdio-only, keyless (no LLM agent loop),
offline, <30s, orphan-free.

Purpose: these two stdio frameworks have no cross-dependency and pair into one
plan, running in parallel with 61-02 and 61-03. Each gets its OWN isolated venv +
pinned requirements per the dependency-isolation decision.

Output: `framework-matrix/pydantic-ai/*` and `framework-matrix/autogen/*`.
</objective>

<execution_context>
@/Users/richardhightower/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardhightower/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/61-python-framework-adapter-matrix/61-CONTEXT.md

# Shared harness this plan consumes (created in Plan 61-01)
@framework-matrix/conftest.py
@framework-matrix/_harness.py
@framework-matrix/bootstrap_venv.sh

# stdio connect reference
@agent-brain-mcp/tests/test_e2e_stdio.py

<interfaces>
<!-- Both adapters churn; the executor MUST fetch current docs via ctx7 before
     pinning + writing connect calls. -->
Pydantic AI MCP client. PyPI: pydantic-ai (or pydantic-ai-slim[mcp]). Surface:
  from pydantic_ai.mcp import MCPServerStdio
  server = MCPServerStdio(command, args=[...], env={...})
  async with server:                     # runs the stdio handshake
      tools = await server.list_tools()  # MCP tool defs
      result = await server.call_tool("search_documents", {"query": ...})
(NO Agent(...).run loop - the MCPServerStdio object speaks MCP directly.)

Autogen / AG2 MCP workbench. PyPI: autogen-ext[mcp] (autogen-ext) for the
McpWorkbench primitive. Surface:
  from autogen_ext.tools.mcp import McpWorkbench, StdioServerParams
  params = StdioServerParams(command=..., args=[...], env={...})
  async with McpWorkbench(server_params=params) as wb:
      tools = await wb.list_tools()
      result = await wb.call_tool("search_documents", {"query": ...})
(NB: "Autogen/AG2" in REQUIREMENTS maps to the autogen-ext McpWorkbench; the
exact distribution name - autogen-ext vs ag2 vs pyautogen - MUST be confirmed via
ctx7 before pinning, since the AG2/Autogen fork split renamed packages.)

From Plan 61-01 (_harness.py): SMOKE_TOOL="search_documents",
SMOKE_QUERY, SMOKE_ARGS={"query": SMOKE_QUERY}, stdio_server_params(state_dir),
assert_non_empty_search(result).
From Plan 61-01 (conftest.py): seeded_mcp_server (session state_dir fixture).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FRAME-04 Pydantic AI - pin pydantic-ai + MCPServerStdio smoke test</name>
  <files>framework-matrix/pydantic-ai/requirements.txt, framework-matrix/pydantic-ai/README.md, framework-matrix/pydantic-ai/test_pydantic_ai_smoke.py</files>
  <read_first>
    - framework-matrix/_harness.py (SMOKE_TOOL, SMOKE_ARGS, stdio_server_params, assert_non_empty_search)
    - framework-matrix/conftest.py (seeded_mcp_server fixture)
    - framework-matrix/bootstrap_venv.sh (venv this requirements file feeds)
    - .planning/phases/61-python-framework-adapter-matrix/61-CONTEXT.md (FRAME-04 = Pydantic AI MCPServerStdio)
  </read_first>
  <behavior>
    - test_pydantic_ai_search_returns_results: Pydantic AI MCPServerStdio connects
      to agent-brain-mcp over stdio (using stdio_server_params) -> list_tools()
      includes search_documents -> call_tool("search_documents", {"query":
      SMOKE_QUERY}) -> assert_non_empty_search passes (>=1 result).
    - Skips cleanly via seeded_mcp_server when prerequisites absent.
  </behavior>
  <action>
    Fetch current docs:
    `npx ctx7@latest library "Pydantic AI" "MCPServerStdio list_tools call_tool stdio"`
    then `npx ctx7@latest docs <id> "MCPServerStdio command args env list_tools call_tool async with"`.

    Create `framework-matrix/pydantic-ai/requirements.txt` pinning the EXACT
    version of the distribution that ships `pydantic_ai.mcp.MCPServerStdio`
    (confirm via ctx7 whether it is `pydantic-ai` or `pydantic-ai-slim[mcp]`),
    using an EXACT `==` pin (NOT a range like `>=` — the locked no-pip-upgrade /
    exact-pin decision requires `pydantic-ai==X.Y.Z`), with a
    `# source: <pypi-url>  pinned: <YYYY-MM-DD>` comment. No model-provider API
    keys / extras (keyless).

    Create `framework-matrix/pydantic-ai/README.md` (3-4 lines: FRAME-04, bootstrap
    + run commands).

    Create `framework-matrix/pydantic-ai/test_pydantic_ai_smoke.py`:
    - `pytestmark = pytest.mark.framework`.
    - `async def test_pydantic_ai_search_returns_results(seeded_mcp_server)`:
      - `command, args, env = stdio_server_params(seeded_mcp_server)`.
      - `server = MCPServerStdio(command, args=args, env=env)` (match ctx7's actual
        signature - some versions take `command` positional + `args`/`env` kwargs).
      - `async with server:` then `tools = await server.list_tools()`; assert a tool
        named SMOKE_TOOL is present.
      - `result = await server.call_tool(SMOKE_TOOL, SMOKE_ARGS)`.
      - `assert_non_empty_search(result)` (helper normalizes Pydantic AI's return;
        if it returns a list of content parts or a structured object, pass it
        through or extract the JSON/structured payload).
    Keep <30s. Rely on the session orphan-guard.
  </action>
  <verify>
    <automated>grep -Eq "pydantic-ai==[0-9]" framework-matrix/pydantic-ai/requirements.txt && grep -Eq "pinned: 20[0-9]{2}-[0-9]{2}-[0-9]{2}" framework-matrix/pydantic-ai/requirements.txt && grep -q "source:" framework-matrix/pydantic-ai/requirements.txt && cd framework-matrix && python -c "import ast; ast.parse(open('pydantic-ai/test_pydantic_ai_smoke.py').read())" && grep -q "MCPServerStdio" pydantic-ai/test_pydantic_ai_smoke.py && grep -q "search_documents" pydantic-ai/test_pydantic_ai_smoke.py && grep -q "assert_non_empty_search" pydantic-ai/test_pydantic_ai_smoke.py && grep -q "seeded_mcp_server" pydantic-ai/test_pydantic_ai_smoke.py</automated>
  </verify>
  <acceptance_criteria>
    - `framework-matrix/pydantic-ai/requirements.txt` pins the pydantic-ai distribution EXACTLY (`pydantic-ai==<version>` — grep `pydantic-ai==[0-9]`; a range like `>=` MUST fail this) with `# source:` URL + `pinned: YYYY-MM-DD` comment; no provider extras.
    - `framework-matrix/pydantic-ai/test_pydantic_ai_smoke.py` parses, is `framework`-marked, consumes `seeded_mcp_server`, uses `MCPServerStdio`, lists tools, calls `search_documents`, and runs `assert_non_empty_search`.
    - `framework-matrix/pydantic-ai/README.md` documents bootstrap + run.
    - After `sh framework-matrix/bootstrap_venv.sh pydantic-ai`, `framework-matrix/pydantic-ai/.venv/bin/pytest framework-matrix/pydantic-ai/ -m framework` exits 0 (or SKIPs without OPENAI_API_KEY) in <30s.
  </acceptance_criteria>
  <done>Pydantic AI connects to agent-brain-mcp via MCPServerStdio, calls search_documents, and asserts a non-empty result against the seeded corpus; pydantic-ai is exact-pinned (==).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FRAME-05 Autogen/AG2 - pin autogen-ext[mcp] + McpWorkbench smoke test</name>
  <files>framework-matrix/autogen/requirements.txt, framework-matrix/autogen/README.md, framework-matrix/autogen/test_autogen_smoke.py</files>
  <read_first>
    - framework-matrix/_harness.py (SMOKE_TOOL, SMOKE_ARGS, stdio_server_params, assert_non_empty_search)
    - framework-matrix/conftest.py (seeded_mcp_server fixture)
    - framework-matrix/bootstrap_venv.sh (venv this requirements file feeds)
    - .planning/phases/61-python-framework-adapter-matrix/61-CONTEXT.md (FRAME-05 = Autogen/AG2 McpWorkbench)
  </read_first>
  <behavior>
    - test_autogen_search_returns_results: McpWorkbench connects to agent-brain-mcp
      over stdio (using stdio_server_params via StdioServerParams) -> list_tools()
      includes search_documents -> call_tool("search_documents", {"query":
      SMOKE_QUERY}) -> assert_non_empty_search passes (>=1 result).
    - Skips cleanly via seeded_mcp_server when prerequisites absent.
  </behavior>
  <action>
    Fetch current docs FIRST to resolve the package split (autogen-ext vs ag2 vs
    pyautogen) and the exact McpWorkbench import path:
    `npx ctx7@latest library "AutoGen" "McpWorkbench StdioServerParams mcp tools list_tools call_tool"`
    then `npx ctx7@latest docs <id> "McpWorkbench StdioServerParams command args list_tools call_tool"`.

    Create `framework-matrix/autogen/requirements.txt` pinning the EXACT
    distribution that ships `autogen_ext.tools.mcp.McpWorkbench` (most likely
    `autogen-ext[mcp]==<version>` plus `autogen-core==<version>`) — use EXACT `==`
    pins (NOT ranges; the locked exact-pin decision), each with a
    `# source: <pypi-url>  pinned: <YYYY-MM-DD>` comment. No model-client API keys
    / extras (keyless - we only use the workbench, not a model client).

    Create `framework-matrix/autogen/README.md` (3-4 lines: FRAME-05, bootstrap +
    run commands; note which distribution the McpWorkbench came from since the
    AG2/Autogen fork split is confusing).

    Create `framework-matrix/autogen/test_autogen_smoke.py`:
    - `pytestmark = pytest.mark.framework`.
    - `async def test_autogen_search_returns_results(seeded_mcp_server)`:
      - `command, args, env = stdio_server_params(seeded_mcp_server)`.
      - `params = StdioServerParams(command=command, args=args, env=env)`
        (match ctx7's actual field names - some versions use `read_timeout_seconds`
        etc.; only command/args/env are required here).
      - `async with McpWorkbench(server_params=params) as wb:` then
        `tools = await wb.list_tools()`; assert SMOKE_TOOL present (tool entries may
        be dicts or objects - match on the `name` field/attr).
      - `result = await wb.call_tool(SMOKE_TOOL, SMOKE_ARGS)`.
      - `assert_non_empty_search(result)` (helper normalizes the Autogen
        ToolResult; if it carries `.result`/`.content` list of TextResultContent,
        extract and pass that).
    Keep <30s. Rely on the session orphan-guard.
  </action>
  <verify>
    <automated>grep -Eq "autogen-ext(\[mcp\])?==[0-9]" framework-matrix/autogen/requirements.txt && grep -Eq "pinned: 20[0-9]{2}-[0-9]{2}-[0-9]{2}" framework-matrix/autogen/requirements.txt && grep -q "source:" framework-matrix/autogen/requirements.txt && cd framework-matrix && python -c "import ast; ast.parse(open('autogen/test_autogen_smoke.py').read())" && grep -q "McpWorkbench" autogen/test_autogen_smoke.py && grep -q "search_documents" autogen/test_autogen_smoke.py && grep -q "assert_non_empty_search" autogen/test_autogen_smoke.py && grep -q "seeded_mcp_server" autogen/test_autogen_smoke.py</automated>
  </verify>
  <acceptance_criteria>
    - `framework-matrix/autogen/requirements.txt` pins the autogen-ext distribution EXACTLY (`autogen-ext[mcp]==<version>` — grep `autogen-ext(\[mcp\])?==[0-9]`) with `# source:` URL + `pinned: YYYY-MM-DD` comment; no model-client extras.
    - `framework-matrix/autogen/test_autogen_smoke.py` parses, is `framework`-marked, consumes `seeded_mcp_server`, uses `McpWorkbench` + `StdioServerParams`, lists tools, calls `search_documents`, and runs `assert_non_empty_search`.
    - `framework-matrix/autogen/README.md` documents bootstrap + run and names the source distribution.
    - After `sh framework-matrix/bootstrap_venv.sh autogen`, `framework-matrix/autogen/.venv/bin/pytest framework-matrix/autogen/ -m framework` exits 0 (or SKIPs without OPENAI_API_KEY) in <30s.
  </acceptance_criteria>
  <done>Autogen/AG2 connects to agent-brain-mcp via McpWorkbench, calls search_documents, and asserts a non-empty result against the seeded corpus; autogen-ext is exact-pinned (==).</done>
</task>

</tasks>

<verification>
- Both `requirements.txt` files pin their MCP adapter EXACTLY (`==`) with source URL + pin date in isolated per-framework dirs.
- Both smoke tests connect via the framework's MCP primitive, call `search_documents`, and assert non-empty results.
- Both tests are `framework`-marked (opt-in) and NOT collected by `task before-push`.
- With prerequisites present, both pass <30s with zero orphan subprocesses.
</verification>

<success_criteria>
- ROADMAP Phase 61 SC2 met for Pydantic AI + Autogen/AG2: each connects to agent-brain-mcp, calls search_documents, asserts non-empty results.
- Each isolated venv resolves with no pip upgrade messages (bootstrap pin-freshness check passes).
</success_criteria>

<output>
After completion, create `.planning/phases/61-python-framework-adapter-matrix/61-04-SUMMARY.md`
</output>
</content>
