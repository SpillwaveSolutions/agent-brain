---
phase: 61-python-framework-adapter-matrix
plan: 02
type: execute
wave: 2
depends_on: ["61-01"]
files_modified:
  - framework-matrix/openai-agents/requirements.txt
  - framework-matrix/openai-agents/test_openai_agents_smoke.py
  - framework-matrix/openai-agents/README.md
autonomous: true
requirements: [FRAME-01]
user_setup: []
must_haves:
  truths:
    - "OpenAI Agents SDK connects to agent-brain-mcp via MCPServerStdio, calls search_documents, and gets a non-empty result list"
    - "OpenAI Agents SDK connects to agent-brain-mcp via MCPServerStreamableHttp, calls search_documents, and gets a non-empty result list"
    - "The OpenAI Agents SDK + its MCP adapter are pinned with a source URL + pin-date comment"
    - "Both legs run keyless (no agent.run loop), offline against the seeded corpus, in <30s each, with zero orphan subprocesses"
  artifacts:
    - path: "framework-matrix/openai-agents/requirements.txt"
      provides: "pinned openai-agents SDK (ships MCPServerStdio + MCPServerStreamableHttp) with source + pin-date comment"
      contains: "openai-agents=="
    - path: "framework-matrix/openai-agents/test_openai_agents_smoke.py"
      provides: "two @pytest.mark.framework tests — stdio leg + streamable-http leg — each asserting non-empty search_documents results"
      min_lines: 70
  key_links:
    - from: "framework-matrix/openai-agents/test_openai_agents_smoke.py"
      to: "agents.mcp.MCPServerStdio / MCPServerStreamableHttp"
      via: "connect -> list_tools -> call_tool('search_documents', {'query': ...}) -> assert non-empty"
      pattern: "MCPServerStdio|MCPServerStreamableHttp"
    - from: "framework-matrix/openai-agents/test_openai_agents_smoke.py"
      to: "framework-matrix/conftest.py:seeded_mcp_server + http_mcp_listener"
      via: "consumes the shared real-server fixtures from Plan 61-01"
      pattern: "seeded_mcp_server|http_mcp_listener"
---

<objective>
FRAME-01: smoke-test the OpenAI Agents SDK against `agent-brain-mcp` over BOTH
MCP transports it supports — `MCPServerStdio` AND `MCPServerStreamableHttp`. Each
leg connects to the server, calls the `search_documents` tool with a query that
hits the seeded corpus, and asserts a non-empty result list. No LLM `agent.run()`
loop, no API keys, offline, <30s, orphan-free.

Purpose: this is the heaviest framework (two transports) so it gets its own plan.
It validates both the stdio hygiene path (Phase 60 contract) and the loopback
HTTP listener (Plan 61-01 `http_mcp_listener`).

Output: `framework-matrix/openai-agents/{requirements.txt, test_openai_agents_smoke.py, README.md}`.
</objective>

<execution_context>
@/Users/richardhightower/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardhightower/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/61-python-framework-adapter-matrix/61-CONTEXT.md

# Shared harness this plan consumes (created in Plan 61-01)
@framework-matrix/conftest.py
@framework-matrix/_harness.py

# stdio connect + streamablehttp connect reference patterns from the codebase
@agent-brain-mcp/tests/test_e2e_stdio.py
@agent-brain-mcp/tests/contract/test_http_transport_contract.py

<interfaces>
<!-- The OpenAI Agents SDK MCP primitives. The executor MUST fetch current docs
     via ctx7 before pinning + writing the connect calls, since the SDK churns. -->
Library: OpenAI Agents SDK (Python). PyPI package: openai-agents.
MCP primitives live under agents.mcp:
  - MCPServerStdio(params={"command": ..., "args": [...], "env": {...}})
  - MCPServerStreamableHttp(params={"url": "http://127.0.0.1:<port>/mcp"})
Both expose async connect(), list_tools(), call_tool(name, arguments) and work as
async context managers. The connect -> list_tools -> call_tool path is the keyless
MCP-adapter-layer test (NO Runner.run / agent loop).

From Plan 61-01 (_harness.py): SMOKE_TOOL="search_documents",
SMOKE_ARGS={"query": SMOKE_QUERY}, stdio_server_params(state_dir),
assert_non_empty_search(result), assert_no_orphans(pid, baseline).
From Plan 61-01 (conftest.py): seeded_mcp_server (session state_dir),
http_mcp_listener (factory — MUST be CALLED with parens, `http_mcp_listener()`,
to start the real agent-brain-mcp --transport http binary and get back the
http://127.0.0.1:<port>/mcp URL).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin openai-agents SDK in an isolated requirements file + README</name>
  <files>framework-matrix/openai-agents/requirements.txt, framework-matrix/openai-agents/README.md</files>
  <read_first>
    - framework-matrix/README.md (per-framework subdir convention from Plan 61-01)
    - framework-matrix/bootstrap_venv.sh (the venv this requirements file feeds)
    - .planning/phases/61-python-framework-adapter-matrix/61-CONTEXT.md (per-framework venv + pin-with-source-URL+date decision)
  </read_first>
  <action>
    First, fetch current docs to get the exact current version + the MCP import
    path (the SDK renames agents.mcp members between releases):
    `npx ctx7@latest library "OpenAI Agents SDK Python" "MCPServerStdio MCPServerStreamableHttp connect call_tool"`
    then `npx ctx7@latest docs <id> "MCPServerStreamableHttp params url call_tool list_tools"`.

    Create `framework-matrix/openai-agents/requirements.txt` pinning the EXACT
    resolved version of openai-agents (the package that ships the agents.mcp
    transports). Each pinned line MUST carry a comment with the source URL and the
    pin date, e.g.:
      # openai-agents - OpenAI Agents SDK for Python (ships agents.mcp.MCPServerStdio + MCPServerStreamableHttp)
      # source: https://pypi.org/project/openai-agents/  pinned: 2026-06-11
      openai-agents==X.Y.Z
    Pin any MCP-adapter transitive dep the SDK does NOT itself pin tightly (e.g.
    the `mcp` SDK) on its own commented line if a floating range would let it
    drift. Do NOT add OpenAI/Anthropic API-key-gated extras - the test is keyless.

    Create `framework-matrix/openai-agents/README.md` - 3-4 lines: what this dir
    tests (FRAME-01, both transports), how to bootstrap
    (`sh framework-matrix/bootstrap_venv.sh openai-agents`), how to run
    (`framework-matrix/openai-agents/.venv/bin/pytest framework-matrix/openai-agents/ -m framework`).
  </action>
  <verify>
    <automated>grep -Eq "openai-agents==[0-9]" framework-matrix/openai-agents/requirements.txt && grep -q "source:" framework-matrix/openai-agents/requirements.txt && grep -Eq "pinned: 20[0-9]{2}-[0-9]{2}-[0-9]{2}" framework-matrix/openai-agents/requirements.txt && test -f framework-matrix/openai-agents/README.md</automated>
  </verify>
  <acceptance_criteria>
    - framework-matrix/openai-agents/requirements.txt contains a line matching `openai-agents==<version>` (exact pin, not a range).
    - The file contains a `# source: <url>` comment AND a `# ... pinned: YYYY-MM-DD` date comment.
    - No API-key-gated extras are added (grep shows no OPENAI_API_KEY/auth extras lines).
    - README documents the bootstrap + run commands for this framework.
  </acceptance_criteria>
  <done>openai-agents is pinned exactly with source+date in an isolated requirements file; the README explains bootstrap + run.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Smoke tests - MCPServerStdio leg AND MCPServerStreamableHttp leg</name>
  <files>framework-matrix/openai-agents/test_openai_agents_smoke.py</files>
  <read_first>
    - framework-matrix/_harness.py (SMOKE_TOOL, SMOKE_ARGS, stdio_server_params, assert_non_empty_search, assert_no_orphans)
    - framework-matrix/conftest.py (seeded_mcp_server + http_mcp_listener fixtures — http_mcp_listener is a FACTORY, call it with parens)
    - agent-brain-mcp/tests/test_e2e_stdio.py (stdio ClientSession connect->initialize->call_tool reference)
    - agent-brain-mcp/tests/contract/test_http_transport_contract.py (streamablehttp connect->initialize->call_tool reference; mount path /mcp)
  </read_first>
  <behavior>
    - test_stdio_search_returns_results: MCPServerStdio connected to agent-brain-mcp
      (via stdio_server_params from the seeded server) -> list_tools() includes
      "search_documents" -> call_tool("search_documents", {"query": SMOKE_QUERY})
      -> assert_non_empty_search passes (>=1 result).
    - test_streamable_http_search_returns_results: MCPServerStreamableHttp connected
      to the http_mcp_listener() URL (http://127.0.0.1:<port>/mcp) -> same list_tools
      -> call_tool -> non-empty assertion.
    - Both tests skip (via the shared seeded_mcp_server fixture) when prerequisites
      are absent; both leave zero orphan agent-brain subprocesses (session guard).
  </behavior>
  <action>
    Create `framework-matrix/openai-agents/test_openai_agents_smoke.py`. Import the
    SDK MCP primitives from agents.mcp (confirm exact import path against the ctx7
    docs from Task 1). Set `pytestmark = pytest.mark.framework` (the conftest
    auto-tag also covers it - belt and suspenders). asyncio_mode=auto is set in
    pytest.ini so plain `async def` tests work; add `@pytest.mark.asyncio` if the
    installed plugin requires it.

    Two tests:

    1. `async def test_stdio_search_returns_results(seeded_mcp_server)`:
       - `command, args, env = stdio_server_params(seeded_mcp_server)`
       - construct `MCPServerStdio(params={"command": command, "args": args,
         "env": env})` (match the SDK's actual params schema from ctx7 - newer
         versions accept a plain dict or an MCPServerStdioParams object).
       - `async with server:` (the SDK context manager runs connect).
       - `tools = await server.list_tools()`; assert any tool name == SMOKE_TOOL.
       - `result = await server.call_tool(SMOKE_TOOL, SMOKE_ARGS)`
       - `assert_non_empty_search(result)`.

    2. `async def test_streamable_http_search_returns_results(http_mcp_listener)`:
       - `url = http_mcp_listener()` — CALL the factory WITH PARENS; this starts
         the real `agent-brain-mcp --transport http` binary and yields
         http://127.0.0.1:<port>/mcp. Injecting the bare fixture without calling
         it never starts the HTTP listener, so the leg silently never runs.
       - `MCPServerStreamableHttp(params={"url": url})`; `async with server:`;
         `list_tools()` -> assert SMOKE_TOOL present; `call_tool(SMOKE_TOOL,
         SMOKE_ARGS)` -> `assert_non_empty_search(result)`.

    Translation note for assert_non_empty_search: OpenAI Agents `call_tool` returns
    the MCP SDK `CallToolResult` (has `.content` + `.structuredContent`). The
    Plan-61-01 helper normalizes list / dict / CallToolResult shapes - pass the raw
    result through; if the SDK wraps it differently, extract `.structuredContent` /
    `.content` and pass that.

    Keep each test well under 30s - the seeded server is session-scoped (started
    once in conftest), so per-test cost is one connect + one tool call. Rely on the
    Plan-61-01 session orphan-guard for the zero-orphan assertion.
  </action>
  <verify>
    <automated>cd framework-matrix && python -c "import ast; ast.parse(open('openai-agents/test_openai_agents_smoke.py').read())" && grep -q "MCPServerStdio" openai-agents/test_openai_agents_smoke.py && grep -q "MCPServerStreamableHttp" openai-agents/test_openai_agents_smoke.py && grep -q "search_documents" openai-agents/test_openai_agents_smoke.py && grep -q "http_mcp_listener()" openai-agents/test_openai_agents_smoke.py</automated>
  </verify>
  <acceptance_criteria>
    - `framework-matrix/openai-agents/test_openai_agents_smoke.py` parses and defines exactly two test functions covering the stdio + streamable-http legs.
    - Source contains BOTH `MCPServerStdio` and `MCPServerStreamableHttp` (both transports exercised).
    - Each test calls the tool named `search_documents` with `{"query": ...}` and runs `assert_non_empty_search` on the result (grep `assert_non_empty_search`).
    - The streamable-http test CALLS the `http_mcp_listener()` factory WITH PARENS (grep `http_mcp_listener()`) — proving the real HTTP binary is started, not just injected as a bare fixture arg; the stdio test consumes `seeded_mcp_server` (grep both names).
    - After `sh framework-matrix/bootstrap_venv.sh openai-agents`, running `framework-matrix/openai-agents/.venv/bin/pytest framework-matrix/openai-agents/ -m framework` exits 0 (or SKIPs cleanly without OPENAI_API_KEY) with each test <30s.
  </acceptance_criteria>
  <done>Both OpenAI Agents transports connect to agent-brain-mcp, call search_documents, and assert non-empty results against the seeded corpus, keyless and orphan-free; the HTTP leg provably starts the real listener via http_mcp_listener().</done>
</task>

</tasks>

<verification>
- `framework-matrix/openai-agents/requirements.txt` pins openai-agents exactly with source URL + pin date.
- The smoke test exercises BOTH MCPServerStdio and MCPServerStreamableHttp and asserts non-empty search_documents results on each.
- The streamable-http test CALLS `http_mcp_listener()` (parens) so the real HTTP binary actually starts.
- Tests are `framework`-marked (opt-in) and NOT collected by `task before-push`.
- With prerequisites present, both legs pass <30s with zero orphan subprocesses.
</verification>

<success_criteria>
- ROADMAP Phase 61 SC1 met: OpenAI Agents SDK connects via both transports, calls search_documents, asserts non-empty results.
- The isolated openai-agents venv resolves with no pip upgrade messages (Plan 61-01 bootstrap pin-freshness check passes).
</success_criteria>

<output>
After completion, create `.planning/phases/61-python-framework-adapter-matrix/61-02-SUMMARY.md`
</output>
</content>
