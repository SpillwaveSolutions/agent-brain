---
phase: 29-cli-api-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/API_REFERENCE.md
autonomous: true
requirements:
  - CLIDOC-03
must_haves:
  truths:
    - "Every API endpoint documented in API_REFERENCE.md matches the OpenAPI spec from the running server"
    - "Request/response schemas in docs match actual Pydantic models"
    - "No stale endpoints remain in docs and no undocumented endpoints exist"
  artifacts:
    - path: "docs/API_REFERENCE.md"
      provides: "Accurate API endpoint documentation"
      contains: "POST /query"
  key_links:
    - from: "docs/API_REFERENCE.md"
      to: "agent-brain-server/agent_brain_server/api/routers/"
      via: "OpenAPI spec comparison"
      pattern: "/health|/query|/index|/jobs|/folders|/cache"
---

<objective>
Audit and fix API endpoint documentation so it accurately reflects the OpenAPI spec and source code.

Purpose: API docs are the contract developers code against. Inaccurate docs cause integration failures.
Output: Updated API_REFERENCE.md matching the actual server endpoints, methods, parameters, and response schemas.
</objective>

<execution_context>
@/Users/richardhightower/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardhightower/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract OpenAPI spec and compare against docs</name>
  <files>docs/API_REFERENCE.md</files>
  <action>
1. Read ALL router files to extract the actual endpoints:
   - agent-brain-server/agent_brain_server/api/routers/health.py
   - agent-brain-server/agent_brain_server/api/routers/query.py
   - agent-brain-server/agent_brain_server/api/routers/index.py
   - agent-brain-server/agent_brain_server/api/routers/jobs.py
   - agent-brain-server/agent_brain_server/api/routers/folders.py
   - agent-brain-server/agent_brain_server/api/routers/cache.py
   - agent-brain-server/agent_brain_server/api/main.py (for router registration and any root endpoints)

2. For each router file, extract:
   - HTTP method (GET, POST, PUT, DELETE)
   - Path (including path parameters)
   - Request body model (Pydantic class name and fields)
   - Response model (Pydantic class name and fields)
   - Query parameters
   - Description/docstring

3. Read the request/response model files:
   - agent-brain-server/agent_brain_server/models/ directory — all Pydantic models used in endpoints

4. Read current docs/API_REFERENCE.md.

5. Compare endpoint-by-endpoint:
   - Endpoints in source but missing from docs
   - Endpoints in docs but removed from source
   - Wrong HTTP methods
   - Wrong request/response fields
   - Missing query parameters
   - Stale descriptions

6. Build a complete discrepancy list.
  </action>
  <verify>
    <automated>cd /Users/richardhightower/clients/spillwave/src/agent-brain && grep -r "@router\.\(get\|post\|put\|delete\|patch\)" agent-brain-server/agent_brain_server/api/routers/ | wc -l</automated>
  </verify>
  <done>Complete list of actual endpoints extracted from source. Discrepancy list between docs and source is documented.</done>
</task>

<task type="auto">
  <name>Task 2: Fix API_REFERENCE.md to match source code</name>
  <files>docs/API_REFERENCE.md</files>
  <action>
Using the discrepancy list from Task 1, update docs/API_REFERENCE.md:

1. For each endpoint in the source code:
   - Verify it appears in API_REFERENCE.md with correct method, path, description
   - Verify request body fields match the Pydantic model (field names, types, required/optional)
   - Verify response fields match the Pydantic response model
   - Verify query parameters are documented with correct types and defaults

2. Add any missing endpoints (from v7-v9 features):
   - Folder management endpoints (folders router)
   - Cache management endpoints (cache router)
   - Job queue endpoints (jobs router)
   - Any new query/index parameters added in recent versions

3. Remove any stale endpoints that no longer exist in source code.

4. For each endpoint section, ensure:
   - Example request shows valid JSON matching current schema
   - Example response shows realistic output matching current response model
   - Error responses are documented if the router handles specific error cases

**Rules:**
- Only fix inaccuracies. Do NOT restructure the document.
- Match field names exactly to Pydantic model attribute names.
- If a field has a default value in the model, document it as optional with the default.
- Preserve the existing documentation style and formatting conventions.
  </action>
  <verify>
    <automated>cd /Users/richardhightower/clients/spillwave/src/agent-brain && grep -c "###\|##" docs/API_REFERENCE.md && grep -oP '`(GET|POST|PUT|DELETE|PATCH) /[^`]+`' docs/API_REFERENCE.md | sort</automated>
  </verify>
  <done>
- Every endpoint from source code routers is documented in API_REFERENCE.md
- Request/response schemas match Pydantic models exactly
- No stale endpoints remain
- Example requests/responses use valid current schemas
  </done>
</task>

</tasks>

<verification>
Count endpoints in router source files. Count endpoints in API_REFERENCE.md. Numbers must match.
Spot-check 3 endpoints: verify field names in docs match Pydantic model field names exactly.
Verify folders, cache, and jobs endpoints are all present (these are v7+ additions).
</verification>

<success_criteria>
1. Zero discrepancies between source code routers and API_REFERENCE.md
2. All request/response schemas match Pydantic models
3. Folder, cache, and job endpoints fully documented
4. No stale endpoints remain in docs
</success_criteria>

<output>
After completion, create `.planning/phases/29-cli-api-documentation/29-02-SUMMARY.md`
</output>
