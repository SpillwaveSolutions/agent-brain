---
phase: 29-cli-api-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/USER_GUIDE.md
  - CLAUDE.md
  - .claude/CLAUDE.md
autonomous: true
requirements:
  - CLIDOC-01
  - CLIDOC-02
  - CLIDOC-04
must_haves:
  truths:
    - "Every CLI subcommand listed in docs matches actual `agent-brain --help` output"
    - "All 5 install-agent runtime commands documented with correct syntax and options"
    - "Job queue commands (jobs, jobs --watch, jobs JOB_ID, jobs --cancel) documented accurately"
    - "CLI command tables in CLAUDE.md and .claude/CLAUDE.md match reality"
  artifacts:
    - path: "docs/USER_GUIDE.md"
      provides: "Accurate CLI usage documentation"
      contains: "install-agent"
    - path: "CLAUDE.md"
      provides: "Accurate CLI commands table"
      contains: "agent-brain"
    - path: ".claude/CLAUDE.md"
      provides: "Accurate CLI commands table"
      contains: "agent-brain"
  key_links:
    - from: "docs/USER_GUIDE.md"
      to: "agent-brain-cli/agent_brain_cli/cli.py"
      via: "CLI --help output"
      pattern: "agent-brain.*--help"
---

<objective>
Audit and fix all CLI command documentation so it accurately reflects the actual CLI behavior.

Purpose: CLI docs are the primary reference for users. Stale docs cause confusion and support burden.
Output: Updated USER_GUIDE.md, CLAUDE.md, and .claude/CLAUDE.md with accurate CLI command info.
</objective>

<execution_context>
@/Users/richardhightower/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardhightower/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture CLI source of truth and identify discrepancies</name>
  <files>docs/USER_GUIDE.md, CLAUDE.md, .claude/CLAUDE.md</files>
  <action>
1. Run `agent-brain --help` and capture the full output. This is the root-level source of truth.
2. Run `agent-brain SUBCOMMAND --help` for EVERY subcommand listed in the root help output. Capture each output.
3. Pay special attention to:
   - `agent-brain install-agent --help` — capture all 5 runtime options (claude, opencode, gemini, codex, skill-runtime)
   - `agent-brain jobs --help` — capture all job queue options (--watch, --cancel, etc.)
   - `agent-brain init --help`, `agent-brain start --help`, `agent-brain stop --help`, `agent-brain list --help`
   - `agent-brain index --help`, `agent-brain query --help`, `agent-brain status --help`, `agent-brain reset --help`
   - `agent-brain config --help`, `agent-brain cache --help`, `agent-brain folders --help`
   - `agent-brain inject --help`, `agent-brain types --help`, `agent-brain uninstall --help`
4. Read the current docs/USER_GUIDE.md, CLAUDE.md, and .claude/CLAUDE.md.
5. For each doc, compare the CLI commands table against the captured --help output.
6. Build a discrepancy list: commands missing from docs, commands in docs but not in CLI, wrong syntax, wrong descriptions, missing options.
  </action>
  <verify>
    <automated>cd /Users/richardhightower/clients/spillwave/src/agent-brain && agent-brain --help 2>/dev/null | head -5 || echo "CLI not available - check install"</automated>
  </verify>
  <done>Full --help output captured for every subcommand. Discrepancy list between docs and actual CLI is documented.</done>
</task>

<task type="auto">
  <name>Task 2: Fix CLI documentation in all affected files</name>
  <files>docs/USER_GUIDE.md, CLAUDE.md, .claude/CLAUDE.md</files>
  <action>
Using the discrepancy list from Task 1, update all three files:

**CLAUDE.md (root):**
- Update the "CLI Commands" table to list every subcommand from `agent-brain --help`
- For multi-instance commands section: verify init, start, stop, list are accurate
- For data commands section: verify status, query, index, reset are accurate
- Add any missing command sections (e.g., install-agent, config, cache, folders, inject, types, uninstall)
- Update "Job Queue Commands" section to match `agent-brain jobs --help` exactly

**.claude/CLAUDE.md:**
- Mirror the same CLI command corrections as root CLAUDE.md (this file has a subset)

**docs/USER_GUIDE.md:**
- Update all CLI usage sections to match actual --help output
- Ensure install-agent section documents ALL 5 runtimes: claude, opencode, gemini, codex, skill-runtime
- For each runtime, include: command syntax, required options (--dir for skill-runtime), optional flags (--dry-run)
- Ensure job queue section accurately documents: `agent-brain jobs`, `agent-brain jobs --watch`, `agent-brain jobs JOB_ID`, `agent-brain jobs JOB_ID --cancel`
- Fix any stale command names, option flags, or descriptions

**Rules:**
- Only fix inaccuracies. Do NOT rewrite prose or restructure docs.
- If a command exists in CLI but is not documented anywhere, add it to the appropriate section.
- If a command is documented but does not exist in CLI, remove it.
- Preserve existing doc structure and formatting style.
  </action>
  <verify>
    <automated>cd /Users/richardhightower/clients/spillwave/src/agent-brain && grep -c "install-agent" docs/USER_GUIDE.md && grep -c "jobs" docs/USER_GUIDE.md && grep -c "install-agent\|jobs\|folders\|cache\|config\|inject\|types\|uninstall" CLAUDE.md</automated>
  </verify>
  <done>
- Every subcommand from `agent-brain --help` appears in docs
- install-agent documents all 5 runtimes with correct syntax
- Job queue commands match actual --help output
- No stale/removed commands remain in docs
- CLAUDE.md, .claude/CLAUDE.md, and USER_GUIDE.md are all consistent
  </done>
</task>

</tasks>

<verification>
Run `agent-brain --help` and confirm every listed subcommand appears in at least one doc file.
Run `agent-brain install-agent --help` and confirm all runtime options appear in USER_GUIDE.md.
Run `agent-brain jobs --help` and confirm all flags appear in docs.
Spot-check 3 random subcommands: verify their --help description matches doc description.
</verification>

<success_criteria>
1. Zero discrepancies between `agent-brain --help` output and documented commands
2. All 5 install-agent runtimes documented with correct syntax
3. Job queue commands fully documented
4. CLAUDE.md, .claude/CLAUDE.md, and USER_GUIDE.md are mutually consistent
</success_criteria>

<output>
After completion, create `.planning/phases/29-cli-api-documentation/29-01-SUMMARY.md`
</output>
