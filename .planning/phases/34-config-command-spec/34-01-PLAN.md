---
phase: 34-config-command-spec
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/34-config-command-spec/SPEC.md
  - agent-brain-plugin/commands/agent-brain-config.md
autonomous: true
requirements:
  - SPEC-AUDIT-01
  - SPEC-FIX-01
  - SPEC-FIX-02

must_haves:
  truths:
    - "SPEC.md title reflects 12-step wizard (not 9-step)"
    - "Every SPEC step has a corresponding command section with matching AskUserQuestion options"
    - "Config keys documented in SPEC match config keys written in command"
    - "Error states listed in SPEC are handled in command"
  artifacts:
    - path: ".planning/phases/34-config-command-spec/SPEC.md"
      provides: "Updated spec with corrected title and version info"
      contains: "12-Step Wizard"
    - path: "agent-brain-plugin/commands/agent-brain-config.md"
      provides: "Command file reconciled with spec"
      contains: "Step 9: Configure File Watcher"
  key_links:
    - from: ".planning/phases/34-config-command-spec/SPEC.md"
      to: "agent-brain-plugin/commands/agent-brain-config.md"
      via: "12-step wizard spec/implementation alignment"
      pattern: "Step [0-9]+:"
---

<objective>
Audit and reconcile the 12-step config wizard SPEC with the command implementation.

Purpose: The SPEC.md defines the contract for the agent-brain-config wizard. The command file (agent-brain-config.md) is the implementation. Drift between the two is a bug. This plan audits both files, fixes any drift found, and ensures spec title/version are correct.

Output: Updated SPEC.md with corrected title, and agent-brain-config.md with any missing content filled in to match spec.
</objective>

<execution_context>
@/Users/richardhightower/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardhightower/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-config-command-spec/34-CONTEXT.md
@.planning/phases/34-config-command-spec/SPEC.md
@agent-brain-plugin/commands/agent-brain-config.md
@agent-brain-plugin/scripts/ab-setup-check.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit SPEC vs Command and fix SPEC.md title/discrepancies</name>
  <files>.planning/phases/34-config-command-spec/SPEC.md</files>
  <read_first>
    - .planning/phases/34-config-command-spec/SPEC.md
    - agent-brain-plugin/commands/agent-brain-config.md
    - agent-brain-plugin/scripts/ab-setup-check.sh
    - .planning/phases/34-config-command-spec/34-CONTEXT.md
  </read_first>
  <action>
Perform a systematic step-by-step comparison of SPEC.md against agent-brain-config.md for all 12 steps. For each step, check:
- AskUserQuestion options (count, labels, descriptions)
- Config keys written (YAML keys and env vars)
- Error states and fallbacks
- Pre-flight script output keys vs what Step 2 documents

Then update SPEC.md with these specific fixes:

1. **Title fix**: Change line `This document is the **source of truth** for the 9-step wizard behavior.` to `This document is the **source of truth** for the 12-step wizard behavior.`

2. **Step 2 output keys**: The SPEC lists output keys `ollama_running`, `docker_available`, `config_file_path`, `available_postgres_port`, `large_dirs`. Verify these match ab-setup-check.sh actual output. The script also emits `agent_brain_installed`, `agent_brain_version`, `config_file_found`, `ollama_models`, `docker_compose_available`, `python_version`, `api_keys`. Add any missing keys to the SPEC Step 2 table that are used by later wizard steps.

3. **Step 7 GraphRAG**: The SPEC lists options: 1) Disabled, 2) Enabled JSON, 3) Enabled Kuzu. The command file has 4 options: 1) Disabled, 2) AST+LangExtract, 3) Kuzu+AST+LangExtract, 4) AST only. Update the SPEC Step 7 AskUserQuestion table to match the command's 4 options exactly:
   - Option 1: Disabled (Default)
   - Option 2: AST + LangExtract (Recommended for mixed repos) - JSON persistence
   - Option 3: Kuzu + AST + LangExtract - Kuzu persistent graph store
   - Option 4: AST only - JSON persistence, code metadata only

4. **Step 7 config keys**: The SPEC shows `use_llm_extraction: false` and `use_code_metadata: true`. The command file uses `doc_extractor: "langextract"` instead of `use_llm_extraction`. Update the SPEC config keys block to include `doc_extractor` key and match the command's actual YAML output for each option.

5. **Step 7 extraction mode sub-question**: The SPEC has a separate "extraction mode" AskUserQuestion after the GraphRAG enable question. The command file integrates extraction mode into the main question (options 2-4 encode the extractor choice). Remove or revise the SPEC's separate extraction mode sub-question to match the combined approach in the command.

6. **Step 12 port auto-discovery**: The SPEC says options are "Local (Default) 127.0.0.1:8000", "Network 0.0.0.0:8000", "Custom port". The command has port auto-discovery scanning 8000-8300. Update the SPEC Step 12 option 1 description to mention auto-discovered port from 8000-8300 instead of hardcoded 8000.

7. **Error states table**: The SPEC's error table lists 7 conditions. Verify the command handles all 7. Add any error conditions present in the command but missing from the SPEC table.

8. **Version section**: Update the version note at the bottom to say `Current spec version: v9.3.0 (Phase 34)`.

Do NOT change anything that is already aligned. Only fix actual drift.
  </action>
  <acceptance_criteria>
    - SPEC.md contains "12-step wizard behavior" (not "9-step")
    - SPEC.md Step 2 output keys table includes `agent_brain_installed`, `ollama_models`, `python_version`, `api_keys` rows
    - SPEC.md Step 7 AskUserQuestion table has exactly 4 options matching command: Disabled, AST+LangExtract, Kuzu+AST+LangExtract, AST only
    - SPEC.md Step 7 config keys block includes `doc_extractor` key
    - SPEC.md Step 12 option 1 mentions port auto-discovery in 8000-8300 range
    - SPEC.md version section contains "v9.3.0"
  </acceptance_criteria>
  <verify>
    <automated>grep -c "12-step wizard" .planning/phases/34-config-command-spec/SPEC.md && grep -c "doc_extractor" .planning/phases/34-config-command-spec/SPEC.md && grep -c "8000-8300" .planning/phases/34-config-command-spec/SPEC.md && grep -c "v9.3.0" .planning/phases/34-config-command-spec/SPEC.md</automated>
  </verify>
  <done>SPEC.md title says "12-step wizard", all 12 steps match command implementation options, config keys are aligned, version updated to v9.3.0</done>
</task>

<task type="auto">
  <name>Task 2: Fix command file drift to match updated SPEC</name>
  <files>agent-brain-plugin/commands/agent-brain-config.md</files>
  <read_first>
    - .planning/phases/34-config-command-spec/SPEC.md (updated from Task 1)
    - agent-brain-plugin/commands/agent-brain-config.md
    - .planning/phases/34-config-command-spec/34-CONTEXT.md
  </read_first>
  <action>
After Task 1 updated the SPEC, review the command file for any remaining drift FROM the spec TO the command. Fix these specific items:

1. **Frontmatter description**: The command frontmatter line `description: 12-step wizard to configure all Agent Brain settings...` — verify it says "12-step" (it already does, confirm no change needed).

2. **Purpose section**: The command's Purpose section says it guides users through configuring Agent Brain with just 2 bullet points (Providers, Indexing). Expand to list all 12 wizard areas matching SPEC steps:
   - Providers (embedding + summarization)
   - Storage backend (ChromaDB or PostgreSQL)
   - Indexing excludes
   - GraphRAG (knowledge graph)
   - Caching (embedding + query)
   - File watcher (auto-reindex)
   - Reranking (two-stage search)
   - Chunking and search tuning
   - Server and deployment

3. **Step 3 Gemini config.yaml**: The command shows Gemini setup with env vars only (Step 4, Option 3). The SPEC Step 3 shows config.yaml format for all providers. Add a config.yaml snippet for Gemini in the command's Option 3 section:
   ```yaml
   embedding:
     provider: "gemini"
     model: "text-embedding-004"
     api_key: "AIza..."

   summarization:
     provider: "gemini"
     model: "gemini-2.0-flash"
     api_key: "AIza..."
   ```

4. **Ollama batch_size prompt**: Per Phase 38 decision (STATE.md), the wizard should "Expose batch_size and request_delay_ms prompts only when embedding provider is ollama." Check if Step 3/4 includes an Ollama-specific sub-question for batch_size (default 10) and request_delay_ms. If not present, add after the Ollama config YAML in Step 4 Option 1:
   ```
   Ollama performance tuning (optional):
     EMBEDDING_BATCH_SIZE=10       # default, reduce if Ollama OOMs
     OLLAMA_REQUEST_DELAY_MS=0     # add delay between batches if needed
   ```

5. **Step 9 env var placement**: Verify the file watcher step tells users WHERE to set the env var. The command should mention adding `AGENT_BRAIN_WATCH_DEBOUNCE_SECONDS=30` to `.env` or shell profile, and note it is NOT a config.yaml key (env-only). This is already mostly there but verify the "env-only" distinction is explicit.

6. **Advanced Configuration Reference table**: Verify the command's advanced config table at the bottom matches the SPEC's advanced config list. Ensure these keys are present in the command table: `CHROMA_PERSIST_DIR`, `BM25_INDEX_PATH`, `COLLECTION_NAME`, `EMBEDDING_DIMENSIONS`, `EMBEDDING_BATCH_SIZE`, `MAX_CHUNK_SIZE`, `MIN_CHUNK_SIZE`, `MAX_TOP_K`, `AGENT_BRAIN_MAX_QUEUE`, `AGENT_BRAIN_JOB_TIMEOUT`, `AGENT_BRAIN_MAX_RETRIES`, `AGENT_BRAIN_CHECKPOINT_INTERVAL`, `EMBEDDING_CACHE_PERSIST_STATS`, `AGENT_BRAIN_STRICT_MODE`, `GRAPH_EXTRACTION_MODEL`, `GRAPH_RRF_K`.

Do NOT rewrite working sections. Only add/fix what is missing or misaligned.
  </action>
  <acceptance_criteria>
    - Command Purpose section lists at least 7 wizard areas (not just 2)
    - Command Step 4 Option 3 (Gemini) has a config.yaml snippet with `provider: "gemini"` and `model: "text-embedding-004"`
    - Command Step 4 Option 1 (Ollama) mentions `EMBEDDING_BATCH_SIZE` and `OLLAMA_REQUEST_DELAY_MS`
    - Command Advanced Configuration Reference table contains at least 14 rows (14+ env vars)
    - Command file grep for "EMBEDDING_BATCH_SIZE" returns at least 1 match
    - Command file grep for "gemini" returns matches in both Step 4 and config.yaml snippet
  </acceptance_criteria>
  <verify>
    <automated>grep -c "EMBEDDING_BATCH_SIZE" agent-brain-plugin/commands/agent-brain-config.md && grep -c 'provider: "gemini"' agent-brain-plugin/commands/agent-brain-config.md && grep -c "OLLAMA_REQUEST_DELAY_MS" agent-brain-plugin/commands/agent-brain-config.md</automated>
  </verify>
  <done>Command file Purpose expanded to cover all wizard areas, Gemini config.yaml added, Ollama batch tuning documented, advanced reference table complete</done>
</task>

</tasks>

<verification>
1. SPEC.md and command file are in sync on all 12 steps
2. SPEC title says "12-step wizard" not "9-step"
3. All AskUserQuestion options match between spec and command
4. All config keys (YAML + env vars) match between spec and command
5. Error states table covers all conditions
6. No deferred features implemented (no config validate, no migration tool, no interactive diff)
</verification>

<success_criteria>
- SPEC.md accurately documents all 12 wizard steps with correct options and config keys
- Command file implements all 12 steps with options matching the spec
- Zero drift between spec and command on AskUserQuestion options, config keys, and error states
- Both files reference the same version (v9.3.0)
</success_criteria>

<output>
After completion, create `.planning/phases/34-config-command-spec/34-01-SUMMARY.md`
</output>
