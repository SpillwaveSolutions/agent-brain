---
phase: 67-co-located-as-rs-middleware
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agent-brain-mcp/pyproject.toml
  - agent-brain-mcp/poetry.lock
  - agent-brain-mcp/tests/test_oauth_deps_smoke.py
autonomous: true
requirements: [OAUTH-04]
must_haves:
  truths:
    - "The mcp SDK OAuth machinery (OAuthAuthorizationServerProvider, create_auth_routes, RequireAuthMiddleware, BearerAuthBackend, TokenVerifier) imports successfully under the bumped pin"
    - "PyJWT[crypto], authlib, and pwdlib[argon2] are installed and import"
    - "The existing MCP test suite still passes after the SDK bump 1.12->1.27.2 (no breaking API drift in server.py / http.py / client.py / subscriptions)"
  artifacts:
    - path: "agent-brain-mcp/pyproject.toml"
      provides: "Bumped mcp pin + new OAuth runtime deps"
      contains: 'mcp = "^1.27.2"'
    - path: "agent-brain-mcp/tests/test_oauth_deps_smoke.py"
      provides: "Import-smoke test for all new OAuth deps + SDK symbols"
      contains: "def test_"
  key_links:
    - from: "agent-brain-mcp/pyproject.toml"
      to: "mcp.server.auth"
      via: "mcp >= 1.27.2 pin enables OAuth submodule"
      pattern: 'mcp = "\^1\.27\.2"'
---

<objective>
Bump the `mcp` SDK pin from `^1.12.0` to `^1.27.2` (the floor that ships the OAuth
machinery) and add the OAuth runtime dependencies (`PyJWT[crypto]`, `authlib`,
`pwdlib[argon2]`), then PROVE the bump introduced no breaking SDK API drift by running
the existing MCP test suite. This plan is the prerequisite gate for the entire phase —
every other Phase 67 plan imports from `mcp.server.auth` and/or the new crypto libs.

Purpose: De-risk the single biggest integration hazard in Phase 67 (a 15-minor-version
SDK jump across `server.py`, `http.py`, `client.py`, and the subscriptions module) in
isolation, before any OAuth implementation code lands on top of it.
Output: Updated `pyproject.toml` + `poetry.lock`, a dep-import smoke test, and a green
existing suite.
</objective>

<execution_context>
@/Users/richardhightower/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardhightower/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-co-located-as-rs-middleware/67-CONTEXT.md

@agent-brain-mcp/pyproject.toml

<interfaces>
<!-- SDK OAuth symbols confirmed present at mcp 1.27.1 (installed); pin floor is 1.27.2.
     Executor should import these directly — they are the contracts later plans build on. -->

From mcp.server.auth.provider:
  OAuthAuthorizationServerProvider  # abstract; 9 methods: authorize, exchange_authorization_code,
                                    # exchange_refresh_token, get_client, load_access_token,
                                    # load_authorization_code, load_refresh_token, register_client, revoke_token
  TokenVerifier                     # Protocol with async verify_token(token: str) -> AccessToken | None
  AccessToken                       # fields: token, client_id, scopes, expires_at, resource
  AuthorizationCode                 # fields: code, scopes, expires_at, client_id, code_challenge,
                                    #         redirect_uri, redirect_uri_provided_explicitly, resource
  RefreshToken                      # fields: token, client_id, scopes, expires_at
  AuthorizationParams               # fields: state, scopes, code_challenge, redirect_uri,
                                    #         redirect_uri_provided_explicitly, resource

From mcp.server.auth.routes:
  create_auth_routes(provider, issuer_url: AnyHttpUrl, service_documentation_url=None,
                     client_registration_options: ClientRegistrationOptions | None = None,
                     revocation_options: RevocationOptions | None = None) -> list[Route]
  ClientRegistrationOptions(*, enabled=False, client_secret_expiry_seconds=None,
                            valid_scopes=None, default_scopes=None)

From mcp.server.auth.middleware.bearer_auth:
  RequireAuthMiddleware(app, required_scopes: list[str], resource_metadata_url: AnyHttpUrl | None = None)
  BearerAuthBackend(token_verifier: TokenVerifier)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump mcp pin to ^1.27.2 and add OAuth runtime dependencies</name>
  <files>agent-brain-mcp/pyproject.toml, agent-brain-mcp/poetry.lock</files>
  <read_first>
    - agent-brain-mcp/pyproject.toml (current pins; mcp = "^1.12.0" at line 28; the path
      deps agent-brain-rag / agent-brain-uds at lines 41-42 must NOT change)
    - .planning/phases/67-co-located-as-rs-middleware/67-CONTEXT.md §"Dependency additions"
    - docs/plans/2026-06-14-mcp-v4-oauth-design.md §"Library Additions to Record" (lines 94-112:
      the version table + the do-NOT-use list)
  </read_first>
  <action>
    Edit `agent-brain-mcp/pyproject.toml` `[tool.poetry.dependencies]`:
    1. Change `mcp = "^1.12.0"` to `mcp = "^1.27.2"`.
    2. Add these new runtime dependencies (Poetry extras syntax):
       - `pyjwt = {extras = ["crypto"], version = "^2.13"}`
       - `authlib = "^1.7.2"`
       - `pwdlib = {extras = ["argon2"], version = ">=0.2"}`
    Do NOT add `itsdangerous` yet — Task 2 verifies whether it is already a Starlette
    transitive dep (run `poetry run python -c "import itsdangerous"` and check
    `poetry show itsdangerous`); only add `itsdangerous = "^2.2"` explicitly if it is NOT
    already resolvable. Do NOT use python-jose, passlib, or fastapi-users (design doc
    do-not-use list). Do NOT touch the `agent-brain-rag` / `agent-brain-uds` path pins or
    the dev-group deps.
    Then resolve the lockfile from the REPO ROOT package dir:
    `cd agent-brain-mcp && poetry lock` (do NOT pass --no-update unless lock fails; the
    point is to actually resolve the new graph). Commit both pyproject.toml and poetry.lock.
  </action>
  <verify>
    <automated>cd agent-brain-mcp && poetry run python -c "import mcp; from mcp.server.auth.provider import OAuthAuthorizationServerProvider, TokenVerifier, AccessToken; from mcp.server.auth.routes import create_auth_routes, ClientRegistrationOptions; from mcp.server.auth.middleware.bearer_auth import RequireAuthMiddleware, BearerAuthBackend; import jwt, authlib, pwdlib; print('all imports OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `agent-brain-mcp/pyproject.toml` contains `mcp = "^1.27.2"`
    - `agent-brain-mcp/pyproject.toml` contains `pyjwt` with extras `["crypto"]`
    - `agent-brain-mcp/pyproject.toml` contains `authlib = "^1.7.2"`
    - `agent-brain-mcp/pyproject.toml` contains `pwdlib` with extras `["argon2"]`
    - `agent-brain-mcp/pyproject.toml` does NOT contain `python-jose`, `passlib`, or `fastapi-users`
    - The verify import command exits 0 (`all imports OK`)
    - `poetry.lock` is regenerated (its `mcp` entry resolves to >= 1.27.2)
  </acceptance_criteria>
  <done>The SDK is pinned at ^1.27.2 with all OAuth symbols importable; PyJWT[crypto], authlib, pwdlib[argon2] install and import; the lockfile is regenerated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Dep-import smoke test + full existing-suite regression run (SDK drift gate)</name>
  <files>agent-brain-mcp/tests/test_oauth_deps_smoke.py</files>
  <read_first>
    - agent-brain-mcp/tests/ (existing test conventions — pick any test_*.py to mirror
      import + assertion style; asyncio_mode is "auto" per pyproject)
    - agent-brain-mcp/agent_brain_mcp/server.py imports (mcp.server.lowlevel, mcp.server.session,
      mcp.shared.session, mcp.shared.version — the surfaces most at risk from the bump)
    - agent-brain-mcp/agent_brain_mcp/http.py (StreamableHTTPSessionManager, TransportSecuritySettings)
    - agent-brain-mcp/agent_brain_mcp/client.py (mcp.client.streamable_http, mcp.client.stdio)
    - .planning/phases/67-co-located-as-rs-middleware/67-CONTEXT.md §2 (dep-bump is highest risk)
  </read_first>
  <behavior>
    - Test: `from mcp.server.auth.provider import OAuthAuthorizationServerProvider, TokenVerifier, AccessToken, AuthorizationCode, RefreshToken, AuthorizationParams` succeeds.
    - Test: `from mcp.server.auth.routes import create_auth_routes, ClientRegistrationOptions, RevocationOptions` succeeds.
    - Test: `from mcp.server.auth.middleware.bearer_auth import RequireAuthMiddleware, BearerAuthBackend` succeeds.
    - Test: `import jwt; jwt.algorithms.RSAAlgorithm` is available (crypto extra installed).
    - Test: `import authlib` and `import pwdlib` succeed; `from pwdlib.hashers.argon2 import Argon2Hasher` succeeds (argon2 extra installed).
    - Test: importing the project's own modules still works post-bump:
      `import agent_brain_mcp.server, agent_brain_mcp.http, agent_brain_mcp.client, agent_brain_mcp.subscriptions`.
    - Test: `create_auth_routes` signature still has params `provider` and `issuer_url`
      (guard against SDK signature drift) via `inspect.signature`.
  </behavior>
  <action>
    Create `agent-brain-mcp/tests/test_oauth_deps_smoke.py` implementing the behaviors above
    as individual `test_*` functions (plain sync functions; no network, no subprocess). The
    `create_auth_routes` signature guard should assert `"provider" in params and "issuer_url"
    in params` from `inspect.signature(create_auth_routes).parameters`.
    Then run the FULL existing MCP suite to prove the bump broke nothing:
    `cd agent-brain-mcp && poetry run pytest` (the default addopts excludes e2e/contract/stress).
    If any pre-existing test fails as a result of SDK API drift, FIX the drift in the
    affected source file (server.py / http.py / client.py / subscriptions) in this task and
    document the change in the SUMMARY — do NOT leave a broken suite for downstream plans.
  </action>
  <verify>
    <automated>cd agent-brain-mcp && poetry run pytest tests/test_oauth_deps_smoke.py -q && poetry run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `agent-brain-mcp/tests/test_oauth_deps_smoke.py` exists and contains `def test_`
    - `pytest tests/test_oauth_deps_smoke.py -q` exits 0
    - The full default suite `pytest -q` exits 0 (no SDK-drift regressions)
    - `task before-push` exits 0 (run from REPO ROOT): `cd .. && task before-push`
  </acceptance_criteria>
  <done>The dep smoke test passes, the full existing suite is green under mcp 1.27.2, and any SDK API drift is fixed in-place. The phase's foundation is verified safe to build on.</done>
</task>

</tasks>

<verification>
- `cd agent-brain-mcp && poetry run pytest -q` exits 0 (existing suite green post-bump)
- All `mcp.server.auth` OAuth symbols + PyJWT[crypto] + authlib + pwdlib[argon2] import
- `task before-push` from repo root exits 0
</verification>

<success_criteria>
mcp pinned at `^1.27.2`; OAuth deps added; SDK OAuth surface + crypto libs import; existing
MCP test suite passes with zero regressions; the dep-bump risk is closed before any AS/RS
code is written.
</success_criteria>

<output>
After completion, create `.planning/phases/67-co-located-as-rs-middleware/67-01-SUMMARY.md`
documenting: final pin versions, whether itsdangerous was added explicitly, any SDK API
drift found + fixed, and the post-bump test count.
</output>
