---
phase: 39-plugin-and-setup-wizard-ux
plan: 02
type: execute
wave: 2
depends_on: ["39-01"]
files_modified:
  - agent-brain-plugin/commands/agent-brain-config.md
  - agent-brain-plugin/tests/test_plugin_wizard_spec.py
  - agent-brain-cli/agent_brain_cli/commands/config.py
  - agent-brain-cli/tests/commands/test_config_wizard.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "Wizard Step 7 includes a first-class AST+LangExtract option for mixed code/doc repositories."
    - "Wizard Step 12 suggests and uses an available server port instead of assuming 8000."
    - "CLI config wizard persists the selected GraphRAG extraction mode and discovered port in generated config output."
  artifacts:
    - path: agent-brain-plugin/commands/agent-brain-config.md
      provides: Updated 12-step wizard contract for Step 7 and Step 12 behavior
    - path: agent-brain-cli/agent_brain_cli/commands/config.py
      provides: Executable config wizard implementation with extraction mode and port discovery
    - path: agent-brain-cli/tests/commands/test_config_wizard.py
      provides: Regression coverage for extractor option and port suggestion behavior
  key_links:
    - from: agent-brain-cli/agent_brain_cli/commands/config.py
      to: agent-brain-cli/tests/commands/test_config_wizard.py
      via: Click wizard prompt/output assertions
      pattern: "test_.*(extract|port|wizard)"
    - from: agent-brain-plugin/commands/agent-brain-config.md
      to: agent-brain-plugin/tests/test_plugin_wizard_spec.py
      via: Markdown spec assertions for Step 7/12 content
      pattern: "GraphRAG|LangExtract|Step 12|port"
---

<objective>
Upgrade the config wizard UX so GraphRAG mixed-repo extraction is explicit in Step 7 and API port conflict avoidance is automatic in Step 12.

Purpose: Users should get the best default GraphRAG setup and avoid multi-project port collisions without manual tuning.
Output: Updated wizard spec + CLI implementation + tests proving Step 7 and Step 12 behavior.
</objective>

<execution_context>
@/Users/richardhightower/clients/spillwave/src/agent-brain/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/richardhightower/clients/spillwave/src/agent-brain/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/todos/pending/2026-03-19-add-ast-for-code-plus-langextract-for-docs-as-first-class-graphrag-option-in-agent-brain-config-wizard.md
@.planning/todos/pending/2026-03-19-auto-discover-available-port-in-agent-brain-config-step-12-deployment-wizard-to-prevent-multi-project-port-conflicts.md
@.planning/phases/25-setup-wizard-coverage-gaps-graphrag-opt-in-bm25-postgresql-awareness-search-mode-education/25-01-SUMMARY.md
@.planning/phases/38-server-reliability-and-provider-fixes/38-04-SUMMARY.md

<interfaces>
From agent-brain-cli/agent_brain_cli/commands/config.py:
```python
@config_group.command("wizard")
def wizard() -> None: ...

def _resolve_wizard_config_path() -> Path: ...
```

From agent-brain-cli/tests/commands/test_config_wizard.py:
```python
runner.invoke(config_group, ["wizard"], input="...")
assert config["embedding"]["params"]["batch_size"] == 20
```

From agent-brain-plugin/commands/agent-brain-config.md:
```markdown
## Step 7: Configure GraphRAG (Knowledge Graph)
## Step 12: Server & Deployment Configuration
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing tests for mixed GraphRAG mode and automatic API port suggestion</name>
  <files>agent-brain-cli/tests/commands/test_config_wizard.py, agent-brain-plugin/tests/test_plugin_wizard_spec.py</files>
  <read_first>
    - agent-brain-cli/tests/commands/test_config_wizard.py
    - agent-brain-plugin/tests/test_plugin_wizard_spec.py
    - agent-brain-cli/agent_brain_cli/commands/config.py
    - agent-brain-plugin/commands/agent-brain-config.md
  </read_first>
  <behavior>
    - Test 1: wizard flow includes explicit mixed extraction option equivalent to `use_code_metadata=true` + `GRAPH_DOC_EXTRACTOR=langextract`.
    - Test 2: wizard flow discovers a free API port in the 8000-8300 range and surfaces it as default/suggested value.
    - Test 3: generated config output contains selected port and GraphRAG extraction fields.
  </behavior>
  <action>Extend `test_config_wizard.py` with RED tests that validate new prompts and persisted values for GraphRAG extraction mode and server port selection. Extend `test_plugin_wizard_spec.py` with content checks for Step 7 option text that explicitly calls out AST+LangExtract and Step 12 instructions that include available-port discovery and range 8000-8300. Keep tests narrowly scoped and deterministic (mock or isolate port detection as needed).</action>
  <acceptance_criteria>
    - `test_config_wizard.py` contains at least one test asserting GraphRAG extractor selection persistence.
    - `test_config_wizard.py` contains at least one test asserting suggested/discovered API port behavior.
    - `test_plugin_wizard_spec.py` contains assertions for "AST + LangExtract" wording and Step 12 auto-port text.
  </acceptance_criteria>
  <verify>
    <automated>poetry run pytest agent-brain-cli/tests/commands/test_config_wizard.py -q</automated>
  </verify>
  <done>Test suite captures required Step 7 and Step 12 UX behavior and fails before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement wizard behavior in CLI command for extraction mode and auto-port discovery</name>
  <files>agent-brain-cli/agent_brain_cli/commands/config.py, agent-brain-cli/tests/commands/test_config_wizard.py</files>
  <read_first>
    - agent-brain-cli/agent_brain_cli/commands/config.py
    - agent-brain-cli/tests/commands/test_config_wizard.py
    - .planning/todos/pending/2026-03-19-add-ast-for-code-plus-langextract-for-docs-as-first-class-graphrag-option-in-agent-brain-config-wizard.md
    - .planning/todos/pending/2026-03-19-auto-discover-available-port-in-agent-brain-config-step-12-deployment-wizard-to-prevent-multi-project-port-conflicts.md
  </read_first>
  <behavior>
    - Test 1: selecting mixed extraction writes GraphRAG config with code metadata enabled and doc extractor set to langextract.
    - Test 2: when no explicit port provided, wizard proposes/discovers a free port in 8000-8300 and writes it.
    - Test 3: non-GraphRAG flows and existing ollama batch/delay behavior remain unchanged.
  </behavior>
  <action>Refactor `wizard()` in `config.py` into staged prompts aligned with Step 7/12 concepts while preserving current provider prompts. Add GraphRAG prompt options that include a first-class mixed mode label (`AST for code + LangExtract for docs`) and map to concrete config values (`graphrag.enabled`, `graphrag.store_type`, `graphrag.use_code_metadata`, `graphrag.doc_extractor`). Add available-port discovery helper scanning 8000-8300 using bind/listen checks (or equivalent safe socket probe), then prompt with discovered port as default and persist under server config (`api.host`/`api.port` or existing schema key used by CLI). Keep output path resolution unchanged and ensure YAML order remains stable.</action>
  <acceptance_criteria>
    - `config.py` contains GraphRAG mixed-mode prompt text that explicitly mentions AST + LangExtract.
    - `config.py` contains a helper or inline logic that scans 8000-8300 for an available port before final port prompt.
    - `poetry run pytest agent-brain-cli/tests/commands/test_config_wizard.py -q` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>poetry run pytest agent-brain-cli/tests/commands/test_config_wizard.py -q</automated>
  </verify>
  <done>CLI wizard implements and persists Step 7 mixed extraction mode and Step 12 auto-port behavior with passing tests.</done>
</task>

<task type="auto">
  <name>Task 3: Update plugin command spec text for Step 7 and Step 12 to match implemented wizard behavior</name>
  <files>agent-brain-plugin/commands/agent-brain-config.md, agent-brain-plugin/tests/test_plugin_wizard_spec.py</files>
  <read_first>
    - agent-brain-plugin/commands/agent-brain-config.md
    - agent-brain-plugin/tests/test_plugin_wizard_spec.py
    - agent-brain-cli/agent_brain_cli/commands/config.py
  </read_first>
  <action>Update Step 7 in `agent-brain-config.md` so GraphRAG options present AST+LangExtract as a first-class selectable path (not only an implicit sub-question). Update Step 12 text so deployment mode instructions explicitly auto-discover an available API port in 8000-8300 and use it as default/suggestion to avoid conflicts. Keep existing security notes and deployment guidance. Align spec wording with implemented CLI prompt names and resulting config keys.</action>
  <acceptance_criteria>
    - Step 7 section includes a top-level option explicitly labeled AST+LangExtract for mixed repos.
    - Step 12 section includes available-port discovery instructions and the 8000-8300 scan range.
    - `test_plugin_wizard_spec.py` passes with assertions covering both additions.
  </acceptance_criteria>
  <verify>
    <automated>python3 -m pytest agent-brain-plugin/tests/test_plugin_wizard_spec.py -q</automated>
  </verify>
  <done>Plugin wizard contract is synchronized with CLI behavior for GraphRAG extraction selection and automatic port suggestion.</done>
</task>

</tasks>

<verification>
Run both suites: `poetry run pytest agent-brain-cli/tests/commands/test_config_wizard.py -q` and `python3 -m pytest agent-brain-plugin/tests/test_plugin_wizard_spec.py -q`.</verification>

<success_criteria>
1. Step 7 exposes AST+LangExtract as an explicit primary option.
2. Step 12 auto-discovers and suggests a free server port in the configured range.
3. CLI implementation and plugin command spec agree, validated by automated tests.
</success_criteria>

<output>
After completion, create `.planning/phases/39-plugin-and-setup-wizard-ux/39-02-SUMMARY.md`
</output>
