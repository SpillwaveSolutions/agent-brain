---
phase: 39-plugin-and-setup-wizard-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agent-brain-plugin/agents/setup-assistant.md
  - agent-brain-plugin/commands/agent-brain-config.md
  - agent-brain-plugin/commands/agent-brain-install.md
  - agent-brain-plugin/commands/agent-brain-setup.md
  - agent-brain-plugin/commands/agent-brain-init.md
  - agent-brain-plugin/commands/agent-brain-start.md
  - agent-brain-plugin/commands/agent-brain-verify.md
  - agent-brain-plugin/scripts/ab-pypi-version.sh
  - agent-brain-plugin/scripts/ab-uv-check.sh
  - agent-brain-plugin/tests/test_setup_permissions_spec.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "Running setup/install/config commands no longer triggers repeated runtime permission prompts for expected script execution and config-file edits."
    - "The setup assistant has scoped permissions for plugin setup scripts and config file writes in all supported config locations."
    - "Setup commands route through the setup assistant policy island instead of ad-hoc command-level permissions."
  artifacts:
    - path: agent-brain-plugin/agents/setup-assistant.md
      provides: Setup assistant tool permission contract
    - path: agent-brain-plugin/commands/agent-brain-config.md
      provides: Script-first setup flow without broad bash invocation
    - path: agent-brain-plugin/commands/agent-brain-install.md
      provides: Script-based version/install checks with reduced inline shell complexity
    - path: agent-brain-plugin/scripts/ab-pypi-version.sh
      provides: Canonical PyPI version resolver used by install flow
    - path: agent-brain-plugin/scripts/ab-uv-check.sh
      provides: Canonical uv presence/installability checker
  key_links:
    - from: agent-brain-plugin/commands/agent-brain-config.md
      to: agent-brain-plugin/scripts/ab-setup-check.sh
      via: Direct script invocation path (no `bash "$SCRIPT"`)
      pattern: "SETUP_STATE=\$\([^)]*ab-setup-check\.sh\)"
    - from: agent-brain-plugin/commands/agent-brain-install.md
      to: agent-brain-plugin/agents/setup-assistant.md
      via: frontmatter `context: fork` + `agent: setup-assistant`
      pattern: "context:\s*fork"
---

<objective>
Lock down setup-assistant permissions and remove setup-command approval fatigue by switching to a policy-island flow (agent-scoped tools + script-first command execution).

Purpose: The setup experience must be uninterrupted for normal install/config/setup flows.
Output: Updated setup assistant policy, command frontmatter binding, and script-backed setup/install checks with regression coverage.
</objective>

<execution_context>
@/Users/richardhightower/clients/spillwave/src/agent-brain/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/richardhightower/clients/spillwave/src/agent-brain/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/todos/pending/2026-03-19-fix-two-permission-gaps-in-setup-assistant-agent-scoped-bash-and-write-edit-for-agent-brain-config-paths.md
@.planning/todos/pending/2026-03-19-eliminate-approval-fatigue-in-agent-brain-plugin-setup-commands-via-pre-authorized-agent-and-scripts.md
@.planning/phases/24-setup-agent-permissions-and-helper-script-to-eliminate-permission-prompts/24-02-SUMMARY.md

<interfaces>
From agent-brain-plugin/agents/setup-assistant.md:
```yaml
name: setup-assistant
skills:
  - configuring-agent-brain
```

From agent-brain-plugin/commands/agent-brain-config.md:
```bash
SCRIPT=$(find ~/.claude/plugins/agent-brain/scripts ~/.claude/skills/agent-brain/scripts agent-brain-plugin/scripts -name "ab-setup-check.sh" 2>/dev/null | head -1)
if [ -n "$SCRIPT" ]; then
  SETUP_STATE=$(bash "$SCRIPT")
fi
```

From agent-brain-plugin/commands/agent-brain-install.md:
```yaml
name: agent-brain-install
parameters: []
skills:
  - configuring-agent-brain
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add setup-assistant policy-island permissions and bind setup commands to the agent</name>
  <files>agent-brain-plugin/agents/setup-assistant.md, agent-brain-plugin/commands/agent-brain-config.md, agent-brain-plugin/commands/agent-brain-install.md, agent-brain-plugin/commands/agent-brain-setup.md, agent-brain-plugin/commands/agent-brain-init.md, agent-brain-plugin/commands/agent-brain-start.md, agent-brain-plugin/commands/agent-brain-verify.md</files>
  <read_first>
    - agent-brain-plugin/agents/setup-assistant.md
    - agent-brain-plugin/commands/agent-brain-config.md
    - agent-brain-plugin/commands/agent-brain-install.md
    - agent-brain-plugin/commands/agent-brain-setup.md
    - .planning/todos/pending/2026-03-19-fix-two-permission-gaps-in-setup-assistant-agent-scoped-bash-and-write-edit-for-agent-brain-config-paths.md
  </read_first>
  <action>Add an `allowed_tools` frontmatter block in `setup-assistant.md` with path-scoped script execution and config write/edit scopes exactly for `~/.claude/plugins/agent-brain/scripts/*`, `.claude/plugins/agent-brain/scripts/*`, `~/.agent-brain/**`, `~/.config/agent-brain/**`, and `.claude/agent-brain/**`; include `Read`/`Glob` if needed for command flow introspection. In each setup-flow command (`agent-brain-config`, `agent-brain-install`, `agent-brain-setup`, `agent-brain-init`, `agent-brain-start`, `agent-brain-verify`), add `context: fork` and `agent: setup-assistant` in frontmatter so these commands execute under the same policy island. Preserve existing descriptions/parameters and do not alter user-facing command semantics.</action>
  <acceptance_criteria>
    - `agent-brain-plugin/agents/setup-assistant.md` contains `allowed_tools:` and includes `Write(~/.agent-brain/**)` and `Edit(~/.agent-brain/**)`.
    - `agent-brain-plugin/agents/setup-assistant.md` includes `Bash(~/.claude/plugins/agent-brain/scripts/*)` and `Bash(.claude/plugins/agent-brain/scripts/*)`.
    - Each of the 6 command files contains both `context: fork` and `agent: setup-assistant` in frontmatter.
  </acceptance_criteria>
  <verify>
    <automated>python3 -m pytest agent-brain-plugin/tests/test_setup_permissions_spec.py -q</automated>
  </verify>
  <done>Setup-flow commands are all bound to setup-assistant and the assistant policy explicitly covers script execution and config writes/edits across supported config directories.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace inline install/config shell fragments with script-backed calls</name>
  <files>agent-brain-plugin/commands/agent-brain-config.md, agent-brain-plugin/commands/agent-brain-install.md, agent-brain-plugin/scripts/ab-pypi-version.sh, agent-brain-plugin/scripts/ab-uv-check.sh, agent-brain-plugin/tests/test_setup_permissions_spec.py</files>
  <read_first>
    - agent-brain-plugin/commands/agent-brain-config.md
    - agent-brain-plugin/commands/agent-brain-install.md
    - agent-brain-plugin/scripts/ab-setup-check.sh
    - .planning/todos/pending/2026-03-19-eliminate-approval-fatigue-in-agent-brain-plugin-setup-commands-via-pre-authorized-agent-and-scripts.md
  </read_first>
  <behavior>
    - Test 1: setup-config flow invokes `ab-setup-check.sh` by direct script path command substitution (not `bash "$SCRIPT"`).
    - Test 2: install flow references `ab-pypi-version.sh` for version resolution and `ab-uv-check.sh` for uv availability check.
    - Test 3: new scripts are executable and documented as single-responsibility helpers.
  </behavior>
  <action>Create `ab-pypi-version.sh` to print latest `agent-brain-rag` version from PyPI with non-zero exit on fetch/parse failure; create `ab-uv-check.sh` to report uv availability and an install hint when missing. Update `agent-brain-install.md` to call these scripts instead of inline `curl | python3` and direct uv check/install snippets for detection logic. Update `agent-brain-config.md` Step 2 to call `ab-setup-check.sh` directly through script path command substitution so the permission maps to path-scoped `Bash(...)` rules. Extend `test_setup_permissions_spec.py` with content assertions for these script-backed references and the no-`bash "$SCRIPT"` constraint.</action>
  <acceptance_criteria>
    - `agent-brain-plugin/commands/agent-brain-config.md` does not contain `SETUP_STATE=$(bash "$SCRIPT")`.
    - `agent-brain-plugin/commands/agent-brain-install.md` contains references to `ab-pypi-version.sh` and `ab-uv-check.sh`.
    - `agent-brain-plugin/scripts/ab-pypi-version.sh` and `agent-brain-plugin/scripts/ab-uv-check.sh` exist and are executable.
  </acceptance_criteria>
  <verify>
    <automated>python3 -m pytest agent-brain-plugin/tests/test_setup_permissions_spec.py -q</automated>
  </verify>
  <done>High-churn inline shell fragments are replaced by stable script entry points, and setup/config/install command text now aligns with path-scoped pre-approved permissions.</done>
</task>

</tasks>

<verification>
Run `python3 -m pytest agent-brain-plugin/tests/test_setup_permissions_spec.py -q` and confirm all assertions pass for permissions, agent binding, and script-first wiring.</verification>

<success_criteria>
1. setup-assistant has scoped script execution and config write/edit permissions.
2. setup/install/config command execution paths avoid runtime approval churn for expected operations.
3. Command frontmatter consistently routes setup flows through setup-assistant.
</success_criteria>

<output>
After completion, create `.planning/phases/39-plugin-and-setup-wizard-ux/39-01-SUMMARY.md`
</output>
