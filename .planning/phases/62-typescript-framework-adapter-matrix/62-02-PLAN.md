---
phase: 62-typescript-framework-adapter-matrix
plan: 02
type: execute
wave: 2
depends_on: ["62-01"]
files_modified:
  - framework-matrix/ts/test/mastra.test.ts
  - framework-matrix/ts/test/vercel-ai-sdk.test.ts
  - framework-matrix/ts/src/fingerprint.ts
  - framework-matrix/ts/README.md
autonomous: true
requirements: [FRAME-06, FRAME-07]
must_haves:
  truths:
    - "The Mastra smoke test connects to agent-brain-mcp via @mastra/mcp, calls search_documents, and asserts a non-empty result list"
    - "The Vercel AI SDK smoke test connects via experimental_createMCPClient from the 'ai' package, calls search_documents, and asserts a non-empty result list"
    - "A single `pnpm test` invocation runs BOTH framework tests"
    - "A failing test surfaces a clean per-framework, per-stage message (which framework, connect vs list-tools vs call vs assert) — not an opaque Node stack trace"
    - "Both tests are KEYLESS (exercise only the MCP tool surface; no OPENAI/ANTHROPIC key required by the test logic itself) and share the single seeded MCP subprocess fixture from 62-01"
  artifacts:
    - path: "framework-matrix/ts/test/mastra.test.ts"
      provides: "FRAME-06 Mastra @mastra/mcp connect -> search_documents -> assertNonEmptySearch"
      contains: "@mastra/mcp"
    - path: "framework-matrix/ts/test/vercel-ai-sdk.test.ts"
      provides: "FRAME-07 Vercel AI SDK experimental_createMCPClient connect -> search_documents -> assertNonEmptySearch"
      contains: "experimental_createMCPClient"
    - path: "framework-matrix/ts/src/fingerprint.ts"
      provides: "per-framework/per-stage failure wrapper (connect|list-tools|call|assert)"
      min_lines: 10
  key_links:
    - from: "framework-matrix/ts/test/mastra.test.ts"
      to: "agent-brain-mcp (shared stdio fixture from 62-01)"
      via: "stdioServerParams + @mastra/mcp MCPClient"
      pattern: "stdioServerParams"
    - from: "framework-matrix/ts/test/vercel-ai-sdk.test.ts"
      to: "agent-brain-mcp (shared stdio fixture from 62-01)"
      via: "experimental_createMCPClient with a stdio MCP transport"
      pattern: "search_documents"
    - from: "framework-matrix/ts/test/*.test.ts"
      to: "assertNonEmptySearch"
      via: "import from ../src/corpus"
      pattern: "assertNonEmptySearch"
---

<objective>
Add the two TypeScript framework smoke tests that close FRAME-06 (Mastra) and FRAME-07 (Vercel AI SDK).
Each test consumes the shared seeded MCP subprocess fixture from Plan 62-01, connects to
`agent-brain-mcp` over stdio, lists tools, calls `search_documents` with the Phase 61 smoke args, and
asserts a non-empty result list via the ported `assertNonEmptySearch`. Both run under a single
`pnpm test`, and a per-framework/per-stage failure wrapper guarantees clean fingerprinting instead of
opaque Node stack traces.

Purpose: Validate the agent-brain-mcp server against the 2 major TypeScript agent frameworks, mirroring
the Phase 61 Python `openai-agents/test_openai_agents_smoke.py` connect->call->assert pattern.

Output: framework-matrix/ts/test/mastra.test.ts, framework-matrix/ts/test/vercel-ai-sdk.test.ts,
framework-matrix/ts/src/fingerprint.ts, and a README update documenting both tests.
</objective>

<execution_context>
@/Users/richardhightower/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardhightower/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/62-typescript-framework-adapter-matrix/62-CONTEXT.md

# Phase 61 closest analogue — connect -> list_tools -> call_tool(search_documents) -> assert non-empty
@framework-matrix/openai-agents/test_openai_agents_smoke.py
@framework-matrix/_harness.py

# Foundation this plan consumes (created in 62-01)
@.planning/phases/62-typescript-framework-adapter-matrix/62-01-PLAN.md

<interfaces>
<!-- Consumed from Plan 62-01 (framework-matrix/ts/src/). Use these directly — no re-derivation. -->

From framework-matrix/ts/src/corpus.ts:
  export const SMOKE_TOOL: string;     // === "search_documents"
  export const SMOKE_QUERY: string;    // === "authenticate user login"
  export const SMOKE_ARGS: { query: string };  // { query: "authenticate user login" }
  export function assertNonEmptySearch(results: unknown): void;  // throws only on count 0

From framework-matrix/ts/src/harness.ts:
  export function stdioServerParams(stateDir: string):
    { command: string; args: string[]; env: Record<string,string> };
    // command="agent-brain-mcp", args=["--backend","uds","--state-dir","<stateDir>/.agent-brain"]
  export function prerequisitesAvailable(): { ok: boolean; reason: string };

From globalSetup (Plan 62-01): the shared seeded agent-brain-serve state dir is exposed to tests via
  process.env.AB_FWM_STATE_DIR (set in src/globalSetup.ts). Read it in each test's setup; skip when unset
  (prerequisites missing). Both tests open their OWN stdio agent-brain-mcp connection against this SAME
  shared seeded server using stdioServerParams(AB_FWM_STATE_DIR) — that is the "single shared MCP
  subprocess fixture" (one seeded server; the lightweight stdio MCP child connects per test and is torn
  down via each adapter's close()).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Per-framework/per-stage failure fingerprint wrapper</name>
  <files>framework-matrix/ts/src/fingerprint.ts</files>
  <read_first>
    - framework-matrix/openai-agents/test_openai_agents_smoke.py (the connect->list_tools->call->assert stages to fingerprint)
    - framework-matrix/_harness.py (assert_non_empty_search message style)
    - framework-matrix/ts/src/corpus.ts (assertNonEmptySearch signature from 62-01)
    - .planning/phases/62-typescript-framework-adapter-matrix/62-CONTEXT.md (Success Criterion 4: failures fingerprint cleanly, NOT opaque node tracebacks)
  </read_first>
  <action>
    Create `framework-matrix/ts/src/fingerprint.ts` exporting a helper that wraps each smoke-flow stage so
    a thrown error names the FRAMEWORK and the STAGE (one of: `connect`, `list-tools`, `call`, `assert`):

    - `export async function stage<T>(framework: string, stage: "connect"|"list-tools"|"call"|"assert",
      fn: () => Promise<T> | T): Promise<T>` — `await fn()`; on throw, re-throw a new `Error` whose message
      is `[${framework}] ${stage} failed: ${original.message}` and whose `.cause` is the original error
      (preserve the stack for debugging, but lead with the clean per-framework/per-stage line).
    - This is the Node analogue of Phase 61's clean AssertionError messages — Success Criterion 4 requires
      failures to fingerprint to a per-framework error, not an opaque node traceback. Both test files MUST
      route their connect / list-tools / call / assert steps through `stage("mastra", ...)` /
      `stage("vercel-ai-sdk", ...)`.
  </action>
  <verify>
    <automated>cd framework-matrix/ts && grep -q "list-tools" src/fingerprint.ts && grep -q "framework" src/fingerprint.ts && pnpm exec tsc --noEmit -p tsconfig.json && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `framework-matrix/ts/src/fingerprint.ts` exports a `stage` helper taking (framework, stage, fn)
    - The helper's stage union includes the literals `connect`, `list-tools`, `call`, `assert`
    - A thrown error message begins with `[<framework>] <stage> failed:` (grep for the bracketed prefix pattern)
    - `pnpm exec tsc --noEmit` typechecks fingerprint.ts with no errors
  </acceptance_criteria>
  <done>fingerprint.ts provides a stage(framework, stage, fn) wrapper that rethrows clean `[framework] stage failed: ...` messages preserving `.cause`; typechecks clean.</done>
</task>

<task type="auto">
  <name>Task 2: FRAME-06 Mastra smoke test via @mastra/mcp</name>
  <files>framework-matrix/ts/test/mastra.test.ts</files>
  <read_first>
    - framework-matrix/openai-agents/test_openai_agents_smoke.py (the connect -> list_tools -> assert SMOKE_TOOL present -> call_tool -> assert_non_empty pattern to mirror)
    - framework-matrix/ts/src/corpus.ts + framework-matrix/ts/src/harness.ts (SMOKE_* + stdioServerParams + assertNonEmptySearch from 62-01)
    - framework-matrix/ts/src/fingerprint.ts (the stage() wrapper from Task 1)
    - .planning/phases/62-typescript-framework-adapter-matrix/62-CONTEXT.md (FRAME-06: @mastra/mcp, keyless, assert non-empty)
  </read_first>
  <action>
    Create `framework-matrix/ts/test/mastra.test.ts` — a vitest test closing FRAME-06.

    Because research is disabled, FIRST resolve the EXACT current `@mastra/mcp` MCP-client API surface:
    run `npx ctx7@latest library "Mastra"` then `npx ctx7@latest docs <id> "MCPClient connect to a stdio
    MCP server, list tools, and call a tool"` to confirm the current class/function names and the stdio
    server-config shape (e.g. `new MCPClient({ servers: { agentBrain: { command, args, env } } })` and
    the `getTools()` / `getToolsets()` / call surface). Do NOT hard-code a guessed API — use the resolved
    names. If the resolved Mastra MCP client only exposes tools as callable functions (not a raw
    list-tools/call-tool pair), adapt: assert the `search_documents` tool key is present, then invoke it
    with SMOKE_ARGS and pass the return value to `assertNonEmptySearch`.

    Test body (mirror framework-matrix/openai-agents/test_openai_agents_smoke.py):
    1. In `beforeAll` / test setup: read `process.env.AB_FWM_STATE_DIR` (the shared seeded server from
       62-01's globalSetup). If unset OR `prerequisitesAvailable()` is not ok, `it.skip` / `ctx.skip()`
       with the reason — KEYLESS test logic, but the seeded server itself needs OPENAI_API_KEY to embed,
       so skip gracefully when absent (mirrors conftest.py prerequisites skip).
    2. `const { command, args, env } = stdioServerParams(process.env.AB_FWM_STATE_DIR)`.
    3. `await stage("mastra", "connect", () => /* construct the @mastra/mcp client with a stdio server
       entry {command, args, env} and connect */)`.
    4. `await stage("mastra", "list-tools", ...)` — fetch the tool set; assert `SMOKE_TOOL`
       (`"search_documents"`) is present in the available tools; on absence throw a message naming the
       available tool keys.
    5. `const result = await stage("mastra", "call", () => /* call search_documents with SMOKE_ARGS */)`.
    6. `stage("mastra", "assert", () => assertNonEmptySearch(result))`.
    7. Teardown: `await stage("mastra","...", () => client.close?.())` (or the resolved disconnect method)
       in `afterAll`/`finally` so the per-test stdio agent-brain-mcp child is closed — relies on the
       @mastra/mcp client's own SIGTERM-clean disconnect; the session-level orphan safety net is
       globalSetup's teardown + the harness `killStrayMcp()`.

    Add `pnpm exec tsc --noEmit` cleanliness — no `any` leaks that break strict mode (use `unknown` +
    pass straight into assertNonEmptySearch).
  </action>
  <verify>
    <automated>cd framework-matrix/ts && grep -q "@mastra/mcp" test/mastra.test.ts && grep -q "search_documents" test/mastra.test.ts && grep -q "stdioServerParams" test/mastra.test.ts && grep -q 'stage("mastra"' test/mastra.test.ts && pnpm exec tsc --noEmit -p tsconfig.json && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `framework-matrix/ts/test/mastra.test.ts` imports from `@mastra/mcp`
    - The test references `SMOKE_TOOL`/`"search_documents"`, `SMOKE_ARGS`, `stdioServerParams`, and `assertNonEmptySearch`
    - All four stages are wrapped: `grep -c 'stage("mastra"' test/mastra.test.ts` returns >=4 (connect, list-tools, call, assert)
    - The test skips gracefully when `AB_FWM_STATE_DIR` is unset or prerequisites are missing (grep for a skip call + reason)
    - `pnpm exec tsc --noEmit` typechecks the test with no errors
    - When prerequisites ARE present, the test passes (search_documents returns non-empty against the seeded corpus)
  </acceptance_criteria>
  <done>mastra.test.ts connects via the resolved @mastra/mcp client to the shared stdio agent-brain-mcp, asserts search_documents is present, calls it with SMOKE_ARGS, asserts non-empty via assertNonEmptySearch, fingerprints every stage, and skips cleanly without prerequisites. FRAME-06 closed.</done>
</task>

<task type="auto">
  <name>Task 3: FRAME-07 Vercel AI SDK smoke test via experimental_createMCPClient + run both under one pnpm test</name>
  <files>framework-matrix/ts/test/vercel-ai-sdk.test.ts, framework-matrix/ts/README.md</files>
  <read_first>
    - framework-matrix/openai-agents/test_openai_agents_smoke.py (connect -> list_tools -> call -> assert pattern)
    - framework-matrix/ts/src/corpus.ts + framework-matrix/ts/src/harness.ts (SMOKE_* + stdioServerParams + assertNonEmptySearch)
    - framework-matrix/ts/src/fingerprint.ts (stage() wrapper)
    - framework-matrix/ts/test/mastra.test.ts (the sibling test to mirror for structure + skip handling)
    - .planning/phases/62-typescript-framework-adapter-matrix/62-CONTEXT.md (FRAME-07: experimental_createMCPClient from the `ai` package, keyless, assert non-empty; SC4: single pnpm test runs both)
  </read_first>
  <action>
    Create `framework-matrix/ts/test/vercel-ai-sdk.test.ts` — a vitest test closing FRAME-07.

    Because research is disabled, FIRST resolve the EXACT current Vercel AI SDK MCP-client API:
    run `npx ctx7@latest library "Vercel AI SDK"` then `npx ctx7@latest docs <id> "experimental_createMCPClient
    with a stdio transport, list tools, call a tool"`. Confirm: (a) the import path —
    `import { experimental_createMCPClient } from 'ai'`; (b) the stdio transport shape — current versions
    take a transport built from `@modelcontextprotocol/sdk` stdio client
    (`new StdioClientTransport({ command, args, env })` from
    `@modelcontextprotocol/sdk/client/stdio.js`) OR the AI SDK's own `Experimental_StdioMCPTransport`
    from `ai/mcp-stdio` — use whichever the resolved current docs show; (c) the tool-access surface —
    `await client.tools()` returns a tool map; each tool is invoked via its `.execute(args, options)` (or
    the resolved equivalent). Do NOT hard-code a guessed signature — pin to what ctx7 returns.

    Test body (mirror mastra.test.ts + framework-matrix/openai-agents/test_openai_agents_smoke.py):
    1. Setup: read `process.env.AB_FWM_STATE_DIR`; skip with reason if unset or `prerequisitesAvailable()`
       not ok.
    2. `const { command, args, env } = stdioServerParams(process.env.AB_FWM_STATE_DIR)`.
    3. `await stage("vercel-ai-sdk", "connect", () => /* build the stdio transport {command,args,env} and
       experimental_createMCPClient({ transport }) */)`.
    4. `await stage("vercel-ai-sdk", "list-tools", () => client.tools())` — assert the returned tool map
       contains the `search_documents` key (`SMOKE_TOOL`); throw naming available keys on absence.
    5. `const result = await stage("vercel-ai-sdk", "call", () => /* invoke the search_documents tool's
       execute with SMOKE_ARGS */)`.
    6. `stage("vercel-ai-sdk", "assert", () => assertNonEmptySearch(result))`.
    7. Teardown: `await stage("vercel-ai-sdk","...", () => client.close())` in afterAll/finally.

    Then prove Success Criterion 4 — a SINGLE `pnpm test` runs BOTH framework tests:
    - Run `cd framework-matrix/ts && pnpm test` and confirm vitest collects + runs mastra.test.ts,
      vercel-ai-sdk.test.ts, and isolation.test.ts in one invocation.
    - Update `framework-matrix/ts/README.md`: document both tests (FRAME-06 Mastra, FRAME-07 Vercel AI
      SDK), the single `pnpm test` entry point, the keyless contract, the skip behavior without
      `OPENAI_API_KEY` / binaries, and that failures fingerprint per-framework via fingerprint.ts.
    - Deliberately verify fingerprinting: temporarily point one test at a bogus tool name OR a dead state
      dir locally to confirm the thrown message reads `[vercel-ai-sdk] <stage> failed: ...` and NOT a raw
      stack — then REVERT. (Document the manual fingerprint check in the SUMMARY; do not leave the bogus
      change.)
  </action>
  <verify>
    <automated>cd framework-matrix/ts && grep -q "experimental_createMCPClient" test/vercel-ai-sdk.test.ts && grep -q "search_documents" test/vercel-ai-sdk.test.ts && grep -q "stdioServerParams" test/vercel-ai-sdk.test.ts && grep -q 'stage("vercel-ai-sdk"' test/vercel-ai-sdk.test.ts && pnpm exec tsc --noEmit -p tsconfig.json && pnpm test 2>&1 | tail -15 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `framework-matrix/ts/test/vercel-ai-sdk.test.ts` imports `experimental_createMCPClient` from `'ai'`
    - The test references `search_documents`/`SMOKE_TOOL`, `SMOKE_ARGS`, `stdioServerParams`, and `assertNonEmptySearch`
    - All four stages wrapped: `grep -c 'stage("vercel-ai-sdk"' test/vercel-ai-sdk.test.ts` returns >=4
    - The test skips gracefully when `AB_FWM_STATE_DIR` is unset or prerequisites missing
    - `pnpm test` collects AND runs BOTH `mastra.test.ts` and `vercel-ai-sdk.test.ts` (plus isolation) in one invocation (Success Criterion 4)
    - `pnpm exec tsc --noEmit` typechecks all test files with no errors
    - `framework-matrix/ts/README.md` documents both tests + the single `pnpm test` entry point + the fingerprinting behavior
  </acceptance_criteria>
  <done>vercel-ai-sdk.test.ts connects via the resolved experimental_createMCPClient stdio path, asserts search_documents present, calls it with SMOKE_ARGS, asserts non-empty, fingerprints every stage, and skips cleanly without prerequisites. A single `pnpm test` runs both framework tests. README documents both. FRAME-07 closed.</done>
</task>

</tasks>

<verification>
- `framework-matrix/ts/test/mastra.test.ts` (FRAME-06) and `framework-matrix/ts/test/vercel-ai-sdk.test.ts`
  (FRAME-07) each connect to agent-brain-mcp over the shared stdio fixture, assert search_documents is
  present, call it with SMOKE_ARGS, and assert a non-empty result via assertNonEmptySearch.
- A single `cd framework-matrix/ts && pnpm test` runs BOTH framework tests.
- Failures surface `[mastra] <stage> failed: ...` / `[vercel-ai-sdk] <stage> failed: ...` (per-framework,
  per-stage), not opaque Node stack traces.
- Tests skip gracefully without OPENAI_API_KEY / agent-brain binaries; `pnpm exec tsc --noEmit` is clean.
- `task before-push` (repo root) remains green and never collects the TS suite.
</verification>

<success_criteria>
- FRAME-06 and FRAME-07 each close: a passing keyless smoke test against the live agent-brain-mcp server
  through the framework's MCP adapter.
- All 4 Phase 62 ROADMAP success criteria are satisfied: (1) ts/ shared fixture from 62-01; (2) Mastra
  non-empty; (3) Vercel non-empty; (4) one `pnpm test` runs both + clean per-framework fingerprinting.
</success_criteria>

<output>
After completion, create `.planning/phases/62-typescript-framework-adapter-matrix/62-02-SUMMARY.md`
recording: the resolved @mastra/mcp + Vercel AI SDK MCP-client API shapes (with ctx7 source/version), the
exact stdio-transport mechanism each used, the `pnpm test` run output (both tests pass or skip-with-reason),
the manual fingerprint-check observation, and any deviations.
</output>
