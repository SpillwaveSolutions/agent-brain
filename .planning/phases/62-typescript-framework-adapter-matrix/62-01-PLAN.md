---
phase: 62-typescript-framework-adapter-matrix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - framework-matrix/ts/package.json
  - framework-matrix/ts/pnpm-lock.yaml
  - framework-matrix/ts/tsconfig.json
  - framework-matrix/ts/vitest.config.ts
  - framework-matrix/ts/README.md
  - framework-matrix/ts/src/corpus.ts
  - framework-matrix/ts/src/harness.ts
  - framework-matrix/ts/src/globalSetup.ts
  - framework-matrix/ts/test/corpus.test.ts
  - framework-matrix/ts/test/isolation.test.ts
autonomous: true
requirements: [FRAME-06, FRAME-07]
must_haves:
  truths:
    - "framework-matrix/ts/ exists with package.json + tsconfig.json + vitest.config.ts + committed pnpm-lock.yaml"
    - "A single shared MCP subprocess fixture spawns agent-brain-mcp over stdio once and is consumable by both framework tests"
    - "The TS corpus + smoke-contract values are byte-identical to the Phase 61 _harness.py FRAMEWORK_CORPUS / SMOKE_QUERY / SMOKE_TOOL / SMOKE_ARGS"
    - "Every dependency in package.json is exact-pinned (no ^ or ~) with a source URL + pinned date note"
    - "The TS suite is NOT collected by task before-push or any pytest run"
    - "assertNonEmptySearch has a server-free unit test covering the Phase 61 5-shape normalization (0-count throws; MCP CallToolResult / content-list / plain-string / list / dict shapes pass)"
  artifacts:
    - path: "framework-matrix/ts/package.json"
      provides: "pnpm packageManager pin + exact-pinned deps + 'test': 'vitest run' script"
      contains: "\"test\": \"vitest run\""
    - path: "framework-matrix/ts/src/corpus.ts"
      provides: "FRAMEWORK_CORPUS (4 files, authenticate token) + SMOKE_QUERY/SMOKE_TOOL/SMOKE_ARGS mirrored from _harness.py"
      contains: "search_documents"
    - path: "framework-matrix/ts/src/harness.ts"
      provides: "spawn agent-brain-serve (seed corpus) + agent-brain-mcp stdio shared fixture + SIGTERM->grace->SIGKILL teardown + assertNonEmptySearch"
      contains: "SIGTERM"
    - path: "framework-matrix/ts/test/corpus.test.ts"
      provides: "server-free unit test of assertNonEmptySearch across the Phase 61 5 result shapes"
      contains: "assertNonEmptySearch"
    - path: "framework-matrix/ts/vitest.config.ts"
      provides: "vitest config wiring the shared globalSetup/fixture"
      min_lines: 5
  key_links:
    - from: "framework-matrix/ts/src/harness.ts"
      to: "agent-brain-mcp (stdio subprocess)"
      via: "child_process.spawn with --backend uds --state-dir"
      pattern: "agent-brain-mcp"
    - from: "framework-matrix/ts/src/harness.ts"
      to: "agent-brain-serve (seeded corpus over UDS)"
      via: "child_process.spawn + POST /index/ + /health/status poll"
      pattern: "agent-brain-serve"
    - from: "framework-matrix/ts/src/globalSetup.ts"
      to: "framework-matrix/ts/test/*.test.ts (62-02)"
      via: "process.env.AB_FWM_STATE_DIR (mandatory canonical handoff)"
      pattern: "AB_FWM_STATE_DIR"
    - from: "framework-matrix/ts/package.json"
      to: "pnpm"
      via: "packageManager field"
      pattern: "\"packageManager\": \"pnpm@"
---

<objective>
Build the `framework-matrix/ts/` foundation: a pnpm + vitest + TypeScript Node subtree that mirrors
the completed Phase 61 Python matrix. This plan ships the project scaffold, the SINGLE shared MCP
subprocess fixture (spawns `agent-brain-serve` once, seeds the tiny corpus, then exposes a stdio
`agent-brain-mcp` connection), the corpus + smoke-contract parity with Phase 61, exact dependency
pins with a committed lockfile, a server-free unit test of `assertNonEmptySearch`, and a verification
test proving the TS suite is invisible to `task before-push` / pytest.

Purpose: Phase 62's two smoke tests (Mastra FRAME-06, Vercel AI SDK FRAME-07, both in Plan 62-02)
share ONE MCP subprocess fixture. That fixture + the smoke contract must exist and be proven correct
before either test imports it. This is the interface-first foundation plan.

Output: `framework-matrix/ts/` with package.json (pnpm pin + exact deps + `vitest run` test script),
tsconfig.json, vitest.config.ts, committed pnpm-lock.yaml, README.md, src/corpus.ts (Phase 61 parity),
src/harness.ts (shared fixture + assertNonEmptySearch + SIGTERM teardown), test/corpus.test.ts
(server-free unit test), and an isolation test.
</objective>

<execution_context>
@/Users/richardhightower/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardhightower/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/62-typescript-framework-adapter-matrix/62-CONTEXT.md

# Phase 61 source-of-truth analogues — replicate the SAME contract values + teardown
@framework-matrix/README.md
@framework-matrix/_harness.py
@framework-matrix/conftest.py
@framework-matrix/pytest.ini
@.planning/phases/61-python-framework-adapter-matrix/61-01-SUMMARY.md

<interfaces>
<!-- Smoke contract values the TS port MUST replicate byte-for-byte. Source: framework-matrix/_harness.py -->

SMOKE_TOOL  = "search_documents"
SMOKE_QUERY = "authenticate user login"
SMOKE_ARGS  = { "query": "authenticate user login" }

FRAMEWORK_CORPUS = 4 files (each bears the `authenticate` token so search returns non-empty):
  - "auth.py"          (authenticate(username, password) -> bool; logout(user_id))
  - "auth.md"          (Authentication Guide; `authenticate` function during login workflow)
  - "query_service.py" (search(query, top_k); count_documents())
  - "config.md"        (Configuration Reference; /authenticate endpoint Bearer tokens)
  Copy the EXACT file contents from framework-matrix/_harness.py FRAMEWORK_CORPUS.

<!-- assertNonEmptySearch 5-shape contract. Source: framework-matrix/_harness.py:_count + assert_non_empty_search -->
_count dispatch order (port VERBATIM — the TS unit test in Task 2 asserts each shape):
  Shape 1: MCP SDK CallToolResult — `.structuredContent` dict preferred -> {total_results:N} | {results:[...]};
           else `.content` (list of text blocks holding a JSON string, OR a bare string)
  Shape 2: bare string -> JSON-parse then count; non-empty non-JSON string == 1
  Shape 3: object with `.raw_output` / `.raw_input` (LlamaIndex ToolOutput)
  Shape 4: plain Array -> count truthy entries (Pydantic AI parts / result list)
  Shape 5: object with `.result` (Autogen ToolResult: list of text blocks)
  dict fallback: {total_results} | len(results)
  TOLERANCE: any extraction error on a NON-null envelope is treated as count>=1 (do NOT throw a
  shape-sniffing error); throw ONLY when count is genuinely 0, with a message containing
  `search_documents returned 0 results`. A null/undefined envelope throws the "returned None" error.

<!-- stdio launch spec. Source: framework-matrix/_harness.py:stdio_server_params -->
command = "agent-brain-mcp"   (resolve on PATH; fall back to bare name)
args    = ["--backend", "uds", "--state-dir", "<state_dir>/.agent-brain"]
env     = { PATH, HOME, AGENT_BRAIN_STATE_DIR="<state_dir>/.agent-brain",
            + OPENAI_API_KEY / ANTHROPIC_API_KEY only when present in os env }

<!-- Seeded-server flow. Source: framework-matrix/conftest.py:start_seeded_server -->
1. mkdir <state_dir>/.agent-brain  and  <state_dir>/corpus ; write the 4 corpus files into corpus/
2. find a free loopback port; spawn `agent-brain-serve` with env:
     AGENT_BRAIN_STATE_DIR=<state_dir>/.agent-brain, AGENT_BRAIN_UDS=1,
     AGENT_BRAIN_UDS_PATH=<state_dir>/.agent-brain/agent-brain.sock,
     API_PORT=<port>, API_HOST=127.0.0.1
3. poll GET http://127.0.0.1:<port>/health/status until reachable (startup timeout ~60s)
4. POST http://127.0.0.1:<port>/index/  body {folder_path: <state_dir>/corpus, force:false, recursive:true}
5. poll /health/status until indexing_in_progress==false AND total_documents>0 (index timeout ~180s)
6. teardown: SIGTERM -> wait grace -> SIGKILL ; best-effort pkill -f agent-brain-mcp
   GRACE TIMING (mirror conftest.py exactly): the agent-brain-serve SERVER teardown uses
   proc.wait(timeout=10) -> 10000 ms; the lighter HTTP/MCP-child path uses 5s -> 5000 ms.

<!-- Opt-in isolation precedent. Source: framework-matrix/pytest.ini + Phase 61-01 SUMMARY -->
Python side keeps the suite out of before-push via the `framework` pytest marker + addopts=-m framework,
and framework-matrix is absent from all 4 package pyproject testpaths (each is testpaths=["tests"]).
The TS subtree is plain Node/pnpm — it must ALSO be absent from those 4 testpaths AND from the root
Taskfile before-push chain (which calls test:cov -> server/cli/uds/mcp test only; no Node step).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the pnpm + vitest + TypeScript project with exact-pinned deps and a committed lockfile</name>
  <files>framework-matrix/ts/package.json, framework-matrix/ts/pnpm-lock.yaml, framework-matrix/ts/tsconfig.json, framework-matrix/ts/vitest.config.ts, framework-matrix/ts/README.md, framework-matrix/ts/PINS.md</files>
  <read_first>
    - framework-matrix/README.md (Phase 61 matrix layout + opt-in contract to mirror)
    - framework-matrix/_harness.py (the requirements.txt-discipline precedent: exact pins + source URL + pin date)
    - .planning/phases/62-typescript-framework-adapter-matrix/62-CONTEXT.md (LOCKED toolchain: pnpm + vitest + TS)
    - .planning/phases/61-python-framework-adapter-matrix/61-01-SUMMARY.md (as-built pin/teardown decisions)
  </read_first>
  <action>
    Create the `framework-matrix/ts/` subtree (sibling to the Python framework dirs, NOT nested):

    1. `framework-matrix/ts/package.json`:
       - `"name": "agent-brain-framework-matrix-ts"`, `"private": true`, `"type": "module"`.
       - `"packageManager": "pnpm@<EXACT>"` — resolve the exact current pnpm version. Run
         `pnpm --version` (or `npx ctx7@latest library "pnpm"` if you need the latest published) and pin
         the resolved value with NO range.
       - `"scripts": { "test": "vitest run", "test:watch": "vitest" }`. The `test` script MUST be exactly
         `"vitest run"` so a single `pnpm test` runs BOTH framework tests (Success Criterion 4).
       - `"devDependencies"` exact-pinned (no `^`/`~`): `vitest`, `typescript`, `@types/node`.
       - `"dependencies"` exact-pinned (no `^`/`~`): `@mastra/mcp`, `ai` (the Vercel AI SDK that exports
         `experimental_createMCPClient`), `@modelcontextprotocol/sdk` (MCP stdio transport used by the
         Vercel client), and any required `@ai-sdk/*` MCP-client piece.
       - Because research is disabled, RESOLVE the exact latest version of EACH dependency at execution
         time and pin it: run `npm view <pkg> version` for each, and/or `npx ctx7@latest library "<name>"`
         to confirm the current package + API surface (e.g. `npx ctx7@latest library "Mastra"`,
         `npx ctx7@latest library "Vercel AI SDK"`, `npx ctx7@latest library "Model Context Protocol TypeScript SDK"`,
         `npx ctx7@latest library "Vitest"`). Do NOT hard-code a guessed version.
       - Add a `// pins note` — since package.json is JSON and cannot hold comments, ALSO create
         `framework-matrix/ts/PINS.md` recording, for every dep, the exact pinned version + the source URL
         (e.g. `https://www.npmjs.com/package/@mastra/mcp/v/<ver>`) + `pinned: 2026-06-11`.

    2. `framework-matrix/ts/tsconfig.json`: `"module": "ESNext"`, `"moduleResolution": "Bundler"` (or
       `"NodeNext"` to match the resolved SDK packaging — verify against the @mastra/mcp + ai package
       `exports` maps), `"target": "ES2022"`, `"strict": true`, `"types": ["node"]`, `"skipLibCheck": true`,
       `"esModuleInterop": true`. No `outDir` — vitest transpiles in-memory, no separate compile step.

    3. `framework-matrix/ts/vitest.config.ts`: a `defineConfig` from `vitest/config` with
       `test: { globalSetup: ['./src/globalSetup.ts'], testTimeout: 240_000, hookTimeout: 240_000,
       include: ['test/**/*.test.ts'] }`. Generous timeouts because globalSetup spawns + seeds a real
       agent-brain-serve (model download / OpenAI embedding on first run, mirroring conftest.py's
       60s startup + 180s index timeouts).

    4. Run `pnpm install` inside `framework-matrix/ts/` to produce and COMMIT `pnpm-lock.yaml`. Then run
       `pnpm install` a SECOND time and confirm it reports no changes/upgrades (the pnpm analogue of the
       Phase 61 bootstrap pin-freshness no-op check). If the second run reports adding/updating packages,
       tighten the pins until it is a no-op.

    5. `framework-matrix/ts/README.md`: mirror `framework-matrix/README.md` structure — explain this is the
       Phase 62 TypeScript leg, that it is OPT-IN (`pnpm test`, NOT in `task before-push`), that it requires
       pnpm (corepack) + `agent-brain-serve` + `agent-brain-mcp` on PATH + `OPENAI_API_KEY`, and that pins
       live in PINS.md. Note the two frameworks: Mastra (`@mastra/mcp`, FRAME-06) and Vercel AI SDK
       (`experimental_createMCPClient`, FRAME-07), both landing in Plan 62-02.
  </action>
  <verify>
    <automated>cd framework-matrix/ts && pnpm install --frozen-lockfile && grep -q '"test": "vitest run"' package.json && grep -Eq '"packageManager": "pnpm@[0-9]' package.json && ! grep -E '": "[\^~]' package.json && test -f pnpm-lock.yaml && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `framework-matrix/ts/package.json` contains `"test": "vitest run"` (exact)
    - `framework-matrix/ts/package.json` contains `"packageManager": "pnpm@` followed by a digit
    - `grep -E '": "[\^~]' framework-matrix/ts/package.json` returns NO matches (no `^`/`~` ranges on any dep)
    - `framework-matrix/ts/package.json` lists `@mastra/mcp`, `ai`, and `@modelcontextprotocol/sdk` each with an exact version
    - `framework-matrix/ts/pnpm-lock.yaml` exists and is committed
    - `framework-matrix/ts/PINS.md` exists and lists every dependency with version + source URL + `pinned: 2026-06-11`
    - `pnpm install --frozen-lockfile` exits 0 (lockfile is in sync = pins resolved cleanly, no upgrades)
    - `framework-matrix/ts/tsconfig.json` and `framework-matrix/ts/vitest.config.ts` exist
  </acceptance_criteria>
  <done>The ts/ project scaffolds, `pnpm install --frozen-lockfile` is a clean no-op, every dep is exact-pinned with a PINS.md source record, and `pnpm test` is wired to `vitest run`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Port the Phase 61 corpus + smoke contract, build the SHARED MCP subprocess fixture (SIGTERM teardown), and add the server-free assertNonEmptySearch unit test</name>
  <files>framework-matrix/ts/src/corpus.ts, framework-matrix/ts/src/harness.ts, framework-matrix/ts/src/globalSetup.ts, framework-matrix/ts/test/corpus.test.ts</files>
  <read_first>
    - framework-matrix/_harness.py (FRAMEWORK_CORPUS exact file contents + SMOKE_QUERY/SMOKE_TOOL/SMOKE_ARGS + stdio_server_params + the _count 5-shape dispatch + assert_non_empty_search tolerance — copy values byte-for-byte)
    - framework-matrix/conftest.py (start_seeded_server flow + _find_free_port + _poll_health + SIGTERM->grace->SIGKILL teardown + _kill_stray_mcp_subprocesses — port to Node; NOTE the SERVER path uses proc.wait(timeout=10) = 10s grace, the HTTP/MCP-child path uses 5s)
    - framework-matrix/ts/vitest.config.ts (the globalSetup wiring from Task 1)
    - .planning/phases/61-python-framework-adapter-matrix/61-01-SUMMARY.md (SIGTERM-only teardown + tolerant 5-shape normalization decisions)
  </read_first>
  <behavior>
    Port the Phase 61 _count 5-shape contract VERBATIM; the new server-free unit test
    (framework-matrix/ts/test/corpus.test.ts) MUST assert each of these WITHOUT a live server:
    - 0-count throws: `assertNonEmptySearch({ total_results: 0 })` and `assertNonEmptySearch({ results: [] })`
      each throw an Error whose message contains `search_documents returned 0 results`.
    - null/undefined throws the "returned None"-style error (envelope is null).
    - Shape 1 (MCP CallToolResult): `{ structuredContent: { total_results: 2 } }` passes;
      `{ structuredContent: { results: [{},{}] } }` passes; `{ content: [{ type:"text",
      text: '{"results":[{},{}]}' }] }` passes.
    - Shape 2 (plain string): `'{"total_results":3}'` passes; a non-empty non-JSON string `"hit"` passes (==1).
    - Shape 4 (plain array): `[{},{}]` passes (2 truthy entries); `[]` throws (0).
    - dict fallback: `{ total_results: 5 }` passes; `{ results: [{}] }` passes.
    - TOLERANCE: a non-null envelope that the extractor cannot parse is treated as >=1 (passes),
      NOT a thrown shape-sniffing error.
    The corpus module exports SMOKE_TOOL === "search_documents", SMOKE_QUERY === "authenticate user login",
    SMOKE_ARGS deep-equals { query: "authenticate user login" }, and FRAMEWORK_CORPUS has exactly the 4
    keys auth.py, auth.md, query_service.py, config.md.
  </behavior>
  <action>
    1. `framework-matrix/ts/src/corpus.ts` — port the Phase 61 smoke contract VERBATIM:
       - `export const SMOKE_TOOL = "search_documents";`
       - `export const SMOKE_QUERY = "authenticate user login";`
       - `export const SMOKE_ARGS = { query: SMOKE_QUERY } as const;`
       - `export const FRAMEWORK_CORPUS: Record<string,string>` with the SAME 4 files
         (auth.py, auth.md, query_service.py, config.md) and the SAME contents as
         framework-matrix/_harness.py FRAMEWORK_CORPUS (copy the strings exactly — the `authenticate`
         token must appear so search_documents returns non-empty).
       - `export function assertNonEmptySearch(results: unknown): void` — port `_count` + the tolerant
         `assert_non_empty_search` from _harness.py, replicating the 5-shape dispatch order:
         (1) `.structuredContent` dict preferred -> {total_results:N}|{results:[...]}, else `.content`
         (array of text blocks holding a JSON string, OR a bare string); (2) bare string -> JSON-parse
         then count, non-empty non-JSON == 1; (3) `.raw_output`/`.raw_input`; (4) plain Array -> truthy
         count; (5) `.result` list; dict fallback {total_results}|len(results). On any extraction error
         against a NON-null envelope, treat as count>=1 (do NOT throw the wrong error). Throw `Error`
         with a message containing the framework-agnostic phrase `search_documents returned 0 results`
         ONLY when the count is genuinely 0; a null/undefined envelope throws a "returned None"-style error.

    2. `framework-matrix/ts/test/corpus.test.ts` — a PURE, server-free vitest unit test of corpus.ts.
       It imports ONLY from `../src/corpus` (no harness, no globalSetup, no spawned process) and asserts
       the full <behavior> matrix above (every shape case + the 0-count throws + the null throws + the
       tolerance branch). This is what makes Task 2's `<verify>` runnable BEFORE Task 3 creates
       isolation.test.ts — vitest finds at least one test file. Also assert the contract constants
       (SMOKE_TOOL/QUERY/ARGS + the 4 FRAMEWORK_CORPUS keys). NOTE: globalSetup still runs for this file,
       but it SKIPS cleanly when prerequisites are absent (see globalSetup below), so corpus.test.ts
       passes server-free on any machine.

    3. `framework-matrix/ts/src/harness.ts` — the SHARED MCP subprocess fixture, porting
       conftest.py:start_seeded_server + stdio_server_params to Node `child_process`:
       - `findFreePort(): Promise<number>` (bind a `net.Server` to port 0 on 127.0.0.1, read the port, close).
       - `startSeededServer(stateDir): Promise<...>` — mkdir `<stateDir>/.agent-brain` + `<stateDir>/corpus`,
         write the 4 FRAMEWORK_CORPUS files into corpus/, spawn `agent-brain-serve` with env
         {...process.env, AGENT_BRAIN_STATE_DIR, AGENT_BRAIN_UDS:"1", AGENT_BRAIN_UDS_PATH, API_PORT, API_HOST:"127.0.0.1"},
         poll `GET /health/status` until reachable (deadline ~60s), POST `/index/`
         {folder_path:<corpus>, force:false, recursive:true}, poll until `indexing_in_progress===false &&
         total_documents>0` (deadline ~180s). Use Node 18+ global `fetch` for the HTTP calls.
       - `stdioServerParams(stateDir)` — return `{ command: "agent-brain-mcp", args: ["--backend","uds",
         "--state-dir", join(stateDir,".agent-brain")], env: { PATH, HOME, AGENT_BRAIN_STATE_DIR:
         join(stateDir,".agent-brain"), ...(OPENAI_API_KEY?), ...(ANTHROPIC_API_KEY?) } }` — mirror
         _harness.py:stdio_server_params exactly. This is the single stdio launch spec BOTH framework tests
         consume.
       - `prerequisitesAvailable(): {ok:boolean, reason:string}` — mirror conftest.py:prerequisites_available:
         require `OPENAI_API_KEY` set and `agent-brain-serve` + `agent-brain-mcp` resolvable on PATH
         (use `which`/`command -v` via execFileSync, or check PATH dirs). Tests SKIP (vitest `it.skip` /
         `ctx.skip()`) with the reason string when not ok.
       - `terminate(child, graceMs): Promise<void>` — the Phase 60 teardown contract:
         `child.kill("SIGTERM")`, wait up to graceMs for exit, then `child.kill("SIGKILL")` if still alive.
         Use SIGTERM as the FIRST signal — NEVER SIGINT (document this). GRACE TIMING — mirror conftest.py
         EXACTLY: the agent-brain-serve SERVER teardown path defaults to `graceMs=10000` (conftest.py's
         start_seeded_server uses `proc.wait(timeout=10)`); the lighter MCP-child / stdio path uses
         `graceMs=5000` (conftest.py's `_HTTP_LISTENER_GRACE_S = 5.0`). Make the call sites explicit:
         globalSetup's SERVER teardown passes 10000; any MCP-child cleanup uses 5000. Add a best-effort
         `killStrayMcp()` that runs `pkill -f agent-brain-mcp` (ignore failure / missing pkill).

    4. `framework-matrix/ts/src/globalSetup.ts` — vitest globalSetup that runs ONCE per `pnpm test`:
       check `prerequisitesAvailable()`. If NOT ok, do NOT spawn anything — leave `AB_FWM_STATE_DIR`
       UNSET and return a no-op teardown (so corpus.test.ts and every framework test skips gracefully and
       the suite is server-free on machines without OPENAI_API_KEY/binaries). If ok, create a short temp
       state dir (mirror conftest.py's `abfwm-` prefix to dodge the macOS 104-char AF_UNIX socket-path
       limit), call `startSeededServer`, and hand the seeded state dir to the tests.
       CANONICAL HANDOFF (warning #4 — this is MANDATORY, not "AND/OR"): ALWAYS set
       `process.env.AB_FWM_STATE_DIR = stateDir` as the single canonical mechanism both Plan 62-02 tests
       read. A file written under the temp dir MAY exist as an OPTIONAL secondary breadcrumb, but the env
       var is the required contract — 62-02 tests read `process.env.AB_FWM_STATE_DIR` and skip when unset.
       Return a teardown function that calls `terminate(serverChild, 10000)` (SERVER grace = 10s to match
       conftest.py) then `killStrayMcp()`. This is the ONE shared subprocess fixture (Success Criterion 1)
       — note the actual stdio `agent-brain-mcp` connections are opened per-test in 62-02 against this
       shared seeded server via `stdioServerParams`.

    5. Add a small JSDoc header on harness.ts pointing at framework-matrix/conftest.py +
       framework-matrix/_harness.py as the canonical source (for auditing), exactly as the Python conftest
       references _corpus.py.
  </action>
  <verify>
    <automated>cd framework-matrix/ts && pnpm exec tsc --noEmit -p tsconfig.json && pnpm vitest run test/corpus.test.ts 2>&1 | tee /tmp/ab_ts_t2.log && grep -q "search_documents" src/corpus.ts && grep -q "SIGTERM" src/harness.ts && grep -q "SIGKILL" src/harness.ts && ! grep -q 'kill("SIGINT")' src/harness.ts && grep -q "AB_FWM_STATE_DIR" src/globalSetup.ts && grep -q "10000" src/harness.ts && echo done</automated>
  </verify>
  <acceptance_criteria>
    - `framework-matrix/ts/src/corpus.ts` contains `"search_documents"` and `"authenticate user login"`
    - `framework-matrix/ts/src/corpus.ts` FRAMEWORK_CORPUS has exactly 4 keys: auth.py, auth.md, query_service.py, config.md (grep each)
    - `framework-matrix/ts/test/corpus.test.ts` exists, imports ONLY from `../src/corpus`, and passes server-free (`pnpm vitest run test/corpus.test.ts` exits 0)
    - The unit test covers the full 5-shape matrix: 0-count throws (`returned 0 results`), null throws, structuredContent/content/string/array/dict shapes pass, and the non-null-unparseable tolerance branch passes
    - `framework-matrix/ts/src/harness.ts` contains `SIGTERM` AND `SIGKILL`; the escalation issues both signals (`grep -c kill src/harness.ts` >= 2)
    - `framework-matrix/ts/src/harness.ts` does NOT call `kill("SIGINT")` for teardown (`grep 'kill("SIGINT")' src/harness.ts` returns 0; SIGINT may appear only in an explanatory comment)
    - `framework-matrix/ts/src/harness.ts` `terminate()` SERVER path uses a 10000 ms (10s) grace to mirror conftest.py's `proc.wait(timeout=10)`; the MCP-child path uses 5000 ms (`grep -q 10000 src/harness.ts` AND `grep -q 5000 src/harness.ts`)
    - `framework-matrix/ts/src/harness.ts` contains `agent-brain-serve` AND `agent-brain-mcp` AND `--backend` AND `uds` AND `--state-dir`
    - `framework-matrix/ts/src/harness.ts` contains `OPENAI_API_KEY` (forwarded only when present) and `prerequisitesAvailable`
    - `framework-matrix/ts/src/globalSetup.ts` ALWAYS sets `process.env.AB_FWM_STATE_DIR` when the server is seeded (mandatory canonical handoff) and returns a teardown calling `terminate(serverChild, 10000)` + `killStrayMcp()`; leaves AB_FWM_STATE_DIR unset (no-op teardown) when prerequisites are missing
    - `pnpm exec tsc --noEmit` typechecks corpus.ts, harness.ts, globalSetup.ts, and test/corpus.test.ts with no errors
  </acceptance_criteria>
  <done>corpus.ts mirrors the Phase 61 smoke contract byte-for-byte; test/corpus.test.ts proves assertNonEmptySearch's 5-shape behavior server-free; harness.ts spawns agent-brain-serve (seeded) + provides the shared stdio agent-brain-mcp launch spec with SIGTERM->grace(10s server/5s child)->SIGKILL teardown; globalSetup pins AB_FWM_STATE_DIR as the canonical handoff.</done>
</task>

<task type="auto">
  <name>Task 3: Prove opt-in isolation — the TS suite is absent from before-push / pytest, with an assertion test</name>
  <files>framework-matrix/ts/test/isolation.test.ts</files>
  <read_first>
    - framework-matrix/pytest.ini (the `framework` marker + addopts=-m framework opt-in precedent)
    - Taskfile.yml (root before-push chain: test:cov -> server/cli/uds/mcp test; no Node step)
    - agent-brain-server/pyproject.toml + agent-brain-cli/pyproject.toml + agent-brain-uds/pyproject.toml + agent-brain-mcp/pyproject.toml (each testpaths=["tests"] — confirm framework-matrix/ts is absent)
    - framework-matrix/ts/vitest.config.ts (the include glob from Task 1)
  </read_first>
  <action>
    Verify and lock the opt-in isolation invariant — the `framework-matrix/ts/` Node suite MUST be
    invisible to `task before-push` and every pytest collection (Success Criterion: opt-in only,
    mirroring Phase 61's `framework` marker isolation):

    1. Confirm `framework-matrix/ts/` does NOT appear in any of the 4 package pyproject testpaths
       (each is `testpaths = ["tests"]`) and is not referenced by any `addopts`/`testpaths`. Run:
       `grep -rn "framework-matrix" agent-brain-server/pyproject.toml agent-brain-cli/pyproject.toml
       agent-brain-uds/pyproject.toml agent-brain-mcp/pyproject.toml` — expect ZERO matches.
    2. Confirm the root `Taskfile.yml` before-push chain runs no Node/pnpm/vitest step: the
       `before-push:` task fans out to `test:cov` + `uds:before-push` + `mcp:before-push` only. Run
       `grep -nE "pnpm|vitest|framework-matrix/ts|node " Taskfile.yml` — expect ZERO matches that would
       pull the TS suite into before-push.
    3. Add `framework-matrix/ts/test/isolation.test.ts` — a vitest test that ASSERTS the isolation
       invariant programmatically so drift is caught: read the 4 package pyproject.toml files + the root
       Taskfile.yml from the repo root and assert none contains the substring `framework-matrix/ts`.
       This is the TS analogue of Phase 61 documenting the marker absence — it fails loudly if a future
       edit wires the TS suite into the Python gate. (Use `node:fs` + `node:path`; resolve repo root via
       walking up from `import.meta.url` to the dir containing `Taskfile.yml`.)
    4. Run `task before-push` from the REPO ROOT and confirm it still exits 0 with the SAME pass/deselect
       counts as the Phase 61-01 baseline (544 passed, framework tests not collected) — i.e. the new TS
       subtree changed nothing pytest collects. Record the exact counts in the SUMMARY.
    5. Run `pnpm test` from `framework-matrix/ts/` and confirm vitest collects ONLY `test/**/*.test.ts`
       (corpus.test.ts + isolation.test.ts now; the two framework tests land in 62-02) — proving
       `pnpm test` is the single opt-in entry point (Success Criterion 4 plumbing).
  </action>
  <verify>
    <automated>! grep -rq "framework-matrix" agent-brain-server/pyproject.toml agent-brain-cli/pyproject.toml agent-brain-uds/pyproject.toml agent-brain-mcp/pyproject.toml && ! grep -Eq "pnpm|vitest|framework-matrix/ts" Taskfile.yml && cd framework-matrix/ts && pnpm test 2>&1 | tail -5 && echo ISOLATION_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "framework-matrix" agent-brain-server/pyproject.toml agent-brain-cli/pyproject.toml agent-brain-uds/pyproject.toml agent-brain-mcp/pyproject.toml` returns 0 matches
    - `grep -nE "pnpm|vitest|framework-matrix/ts" Taskfile.yml` returns 0 matches
    - `framework-matrix/ts/test/isolation.test.ts` exists and asserts the 4 pyprojects + root Taskfile do NOT contain `framework-matrix/ts`
    - `task before-push` (run from repo root) exits 0 with framework/TS tests NOT collected (pytest count unchanged vs the 61-01 baseline)
    - `pnpm test` (run from framework-matrix/ts/) exits 0 and both corpus.test.ts + isolation.test.ts pass
  </acceptance_criteria>
  <done>The TS suite is provably absent from all 4 package pyprojects + the root before-push chain; an isolation vitest test enforces it; `task before-push` is unchanged (exit 0, same counts) and `pnpm test` is the sole opt-in entry point.</done>
</task>

</tasks>

<verification>
- `framework-matrix/ts/` exists with package.json (pnpm pin + `"test": "vitest run"` + exact-pinned deps),
  tsconfig.json, vitest.config.ts, committed pnpm-lock.yaml, README.md, PINS.md.
- `pnpm install --frozen-lockfile` is a clean no-op (pins resolved; no upgrade messages).
- corpus.ts + the smoke contract (SMOKE_TOOL/QUERY/ARGS + the 4-file authenticate corpus) match Phase 61
  _harness.py byte-for-byte; test/corpus.test.ts proves assertNonEmptySearch's 5-shape behavior server-free.
- harness.ts provides the single shared MCP subprocess fixture (seeded agent-brain-serve + stdio
  agent-brain-mcp launch spec) with SIGTERM->grace(10s server/5s child)->SIGKILL teardown and no SIGINT;
  globalSetup pins AB_FWM_STATE_DIR as the canonical 62-02 handoff.
- The TS suite is absent from all 4 package pyproject testpaths + the root Taskfile before-push chain; an
  isolation vitest test enforces it; `task before-push` exits 0 unchanged.
</verification>

<success_criteria>
- A different Claude executor can run `cd framework-matrix/ts && pnpm install --frozen-lockfile && pnpm test`
  and the foundation (scaffold + corpus unit test + isolation test) passes without further interpretation.
- Plan 62-02 can `import { stdioServerParams, assertNonEmptySearch, SMOKE_TOOL, SMOKE_ARGS } from '../src/...'`
  and consume the shared seeded server exposed via `process.env.AB_FWM_STATE_DIR` from globalSetup without
  re-deriving any spawn/seed logic.
- `task before-push` remains green and never collects the TS suite.
</success_criteria>

<output>
After completion, create `.planning/phases/62-typescript-framework-adapter-matrix/62-01-SUMMARY.md`
recording: exact pinned versions chosen (with source URLs), the resolved pnpm version, the as-built
shared-fixture mechanism (globalSetup + AB_FWM_STATE_DIR), the exact `task before-push` pass/deselect
counts, the server-free corpus.test.ts result, and any deviations.
</output>
</output>
