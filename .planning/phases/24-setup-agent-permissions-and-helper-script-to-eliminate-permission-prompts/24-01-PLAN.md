---
phase: 24-setup-agent-permissions-and-helper-script-to-eliminate-permission-prompts
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - agent-brain-plugin/templates/settings.json
  - agent-brain-plugin/commands/agent-brain-setup.md
autonomous: true
requirements:
  - PERM-01
  - PERM-02

must_haves:
  truths:
    - "A settings.json template exists in agent-brain-plugin/templates/ documenting the full wizard permission allowlist"
    - "The /agent-brain-setup command writes .claude/settings.json using the Write tool as its very first action before any Bash calls"
    - "After setup runs on a fresh project, subsequent Bash calls in the wizard do not trigger permission prompts"
  artifacts:
    - path: "agent-brain-plugin/templates/settings.json"
      provides: "Canonical permission allowlist for all Agent Brain wizard operations"
      contains: "Bash(agent-brain:*)"
    - path: "agent-brain-plugin/commands/agent-brain-setup.md"
      provides: "Updated setup command with Write-tool-first permission bootstrap"
      contains: ".claude/settings.json"
  key_links:
    - from: "agent-brain-plugin/commands/agent-brain-setup.md"
      to: ".claude/settings.json"
      via: "Write tool call before Step 1"
      pattern: "Write.*settings.json"
---

<objective>
Break the bootstrap problem: the `/agent-brain-setup` wizard currently triggers 15+ Bash permission prompts on a fresh project because `.claude/settings.json` does not exist. Fix this by having the setup command use the Write tool (always pre-authorized, no permission gate) to create `.claude/settings.json` as its very first action before invoking any shell commands.

Purpose: Users running `/agent-brain-setup` for the first time complete the full wizard flow without interruption from permission dialogs.

Output:
1. `agent-brain-plugin/templates/settings.json` — auditable template listing all wizard permissions
2. Updated `agent-brain-plugin/commands/agent-brain-setup.md` — adds a "Step 0: Bootstrap Permissions" block that writes `.claude/settings.json` before Step 1
</objective>

<execution_context>
@/Users/richardhightower/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardhightower/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

<!-- Phase 23 established XDG paths: ~/.config/agent-brain/ and ~/.local/state/agent-brain/ alongside legacy ~/.agent-brain/ -->
<!-- Phase 22 restored the full setup wizard (6 AskUserQuestion blocks, Steps 1-12) -->
<!-- The wizard needs: agent-brain, lsof, ollama, docker, mkdir, cat, jq, mv, du, ps, pgrep, pip, pipx, uv, python, rg, wc, curl, ls, find, chmod, grep, bash, python3, wc -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create settings.json permission template</name>
  <files>agent-brain-plugin/templates/settings.json</files>
  <action>
Create `agent-brain-plugin/templates/settings.json` with the complete allowlist required to run the setup wizard without permission prompts. Include ALL commands identified in the context research — existing ones (curl, ls, find, chmod, grep, python3, bash) AND newly required ones (agent-brain, lsof, ollama, docker, mkdir, cat, jq, mv, du, ps, pgrep, pip, pipx, uv, python, rg, wc).

The file must be valid JSON and include a top-level comment block (as a JSON key `_comment`) explaining the purpose. Structure:

```json
{
  "_comment": "Agent Brain setup permissions. Copy to .claude/settings.json in your project root, or /agent-brain-setup will write it automatically.",
  "permissions": {
    "allow": [
      "Bash(agent-brain:*)",
      "Bash(lsof:*)",
      "Bash(ollama:*)",
      "Bash(docker:*)",
      "Bash(mkdir:*)",
      "Bash(cat:*)",
      "Bash(jq:*)",
      "Bash(mv:*)",
      "Bash(du:*)",
      "Bash(ps:*)",
      "Bash(pgrep:*)",
      "Bash(pip:*)",
      "Bash(pipx:*)",
      "Bash(uv:*)",
      "Bash(python:*)",
      "Bash(python3:*)",
      "Bash(rg:*)",
      "Bash(wc:*)",
      "Bash(curl:*)",
      "Bash(ls:*)",
      "Bash(find:*)",
      "Bash(chmod:*)",
      "Bash(grep:*)",
      "Bash(bash:*)"
    ],
    "deny": []
  }
}
```

Group the entries with inline comments (use `_comment_groupN` keys) explaining: Group 1 = primary CLI tool, Group 2 = detection/read-only, Group 3 = configuration writes, Group 4 = installation, Group 5 = already-standard.

Do NOT add `"deny"` entries beyond the empty array. Do NOT include Write/Read tool permissions (those are always allowed).
  </action>
  <verify>
    <automated>python3 -c "import json; d=json.load(open('agent-brain-plugin/templates/settings.json')); assert 'Bash(agent-brain:*)' in d['permissions']['allow']; assert 'Bash(lsof:*)' in d['permissions']['allow']; assert 'Bash(docker:*)' in d['permissions']['allow']; print('settings.json valid, entries:', len(d['permissions']['allow']))"</automated>
  </verify>
  <done>File exists, is valid JSON, contains at minimum: Bash(agent-brain:*), Bash(lsof:*), Bash(docker:*), Bash(ollama:*), Bash(mkdir:*), Bash(cat:*), Bash(jq:*), Bash(mv:*), Bash(du:*), Bash(ps:*), Bash(pgrep:*), Bash(pip:*), Bash(pipx:*), Bash(uv:*), Bash(python:*), Bash(rg:*), Bash(wc:*)</done>
</task>

<task type="auto">
  <name>Task 2: Add Step 0 permission bootstrap to agent-brain-setup.md</name>
  <files>agent-brain-plugin/commands/agent-brain-setup.md</files>
  <action>
Edit `agent-brain-plugin/commands/agent-brain-setup.md` to add a new **Step 0: Bootstrap Permissions** block immediately after the `## Execution` heading, BEFORE the existing `### Step 1: Check Installation Status`. This step must:

1. Check whether `.claude/settings.json` already exists and contains the `agent-brain` permission entry
2. If not present, use the Write tool to create `.claude/settings.json` with the full permission set from `agent-brain-plugin/templates/settings.json` (inline the JSON content directly — do not shell-exec a copy)
3. Inform the user: "I've written `.claude/settings.json` with the permissions required to run setup without interruption."
4. If `.claude/` directory does not exist, note that the Write tool creates parent directories

Step 0 content to insert (use this exact structure in the markdown):

```markdown
### Step 0: Bootstrap Permissions

Before running any shell commands, ensure `.claude/settings.json` exists with the required permissions for the setup wizard. Use the Write tool (not Bash) to create this file — the Write tool is always available without a permission gate.

Check if the file already exists:

```bash
ls .claude/settings.json 2>/dev/null && echo "EXISTS" || echo "MISSING"
```

If missing or if it does not contain `"Bash(agent-brain:*)"`, write the following content to `.claude/settings.json` using the Write tool:

```json
{
  "_comment": "Agent Brain setup permissions — written by /agent-brain-setup. Safe to commit.",
  "permissions": {
    "allow": [
      "Bash(agent-brain:*)",
      "Bash(lsof:*)",
      "Bash(ollama:*)",
      "Bash(docker:*)",
      "Bash(mkdir:*)",
      "Bash(cat:*)",
      "Bash(jq:*)",
      "Bash(mv:*)",
      "Bash(du:*)",
      "Bash(ps:*)",
      "Bash(pgrep:*)",
      "Bash(pip:*)",
      "Bash(pipx:*)",
      "Bash(uv:*)",
      "Bash(python:*)",
      "Bash(python3:*)",
      "Bash(rg:*)",
      "Bash(wc:*)",
      "Bash(curl:*)",
      "Bash(ls:*)",
      "Bash(find:*)",
      "Bash(chmod:*)",
      "Bash(grep:*)",
      "Bash(bash:*)"
    ],
    "deny": []
  }
}
```

After writing the file, tell the user:
"Wrote `.claude/settings.json` with Agent Brain setup permissions. These allow the wizard to run without permission prompts. The file is safe to commit — it grants access only to standard development tools."

**IMPORTANT:** If `.claude/settings.json` already exists with custom content, MERGE: add any missing `Bash(...)` entries from the list above into the existing `allow` array rather than replacing the file.
```

Also update the Output section's progress display to show `[0/10]` for Step 0 before `[1/10]`:

```
[0/10] Bootstrapping permissions...
       .claude/settings.json [WRITTEN]
```

Do NOT change any other steps. The numbered steps 1-12 remain unchanged.
  </action>
  <verify>
    <automated>grep -n "Bootstrap Permissions\|Step 0\|settings.json" agent-brain-plugin/commands/agent-brain-setup.md | head -20</automated>
  </verify>
  <done>agent-brain-setup.md contains a "Step 0: Bootstrap Permissions" section before "Step 1: Check Installation Status" that instructs writing .claude/settings.json via Write tool, including the full JSON permission content inline</done>
</task>

</tasks>

<verification>
1. `agent-brain-plugin/templates/settings.json` is valid JSON: `python3 -c "import json; json.load(open('agent-brain-plugin/templates/settings.json'))"`
2. Template contains `Bash(agent-brain:*)` entry
3. `agent-brain-setup.md` contains "Step 0" before "Step 1"
4. `agent-brain-setup.md` contains the full JSON permission block inline
5. Plugin tests pass: `cd agent-brain-cli && poetry run pytest tests/ -x -q 2>&1 | tail -5`
</verification>

<success_criteria>
- `agent-brain-plugin/templates/settings.json` exists with 24+ permission entries
- `agent-brain-plugin/commands/agent-brain-setup.md` instructs writing `.claude/settings.json` using Write tool BEFORE Step 1
- The inline JSON in agent-brain-setup.md matches the template (same entries, same structure)
- No existing tests broken
</success_criteria>

<output>
After completion, create `.planning/phases/24-setup-agent-permissions-and-helper-script-to-eliminate-permission-prompts/24-01-SUMMARY.md`
</output>
