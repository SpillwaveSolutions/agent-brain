---
phase: 70-split-as-rs-keycloak-in-ci-integration-tests
plan: 03
type: execute
wave: 3
depends_on: ["70-01", "70-02"]
files_modified:
  - agent-brain-mcp/Taskfile.yml
  - agent-brain-mcp/tests/test_oauth_keycloak_e2e.py
  - .github/workflows/mcp-keycloak-integration.yml
  - docs/plans/2026-06-14-mcp-v4-oauth-design.md
  - agent-brain-mcp/docs/SPLIT_AS_RS.md
autonomous: true
requirements: [OAUTH-11, OAUTH-12]

must_haves:
  truths:
    - "The Keycloak-backed E2E path (Keycloak JWT → authorized tool call → token refresh → scope-boundary 403) is exercised via direct-grant against the official MCP SDK client; the full browser PKCE dance (401 → PRM → OASM → PKCE → tool call) is proven in Phase 69's test_oauth_client_dance_e2e.py against the co-located AS (referenced by docstring). SC#4 is satisfied by the Phase 69 + Phase 70 combination."
    - "A read-only-scoped Keycloak token calling an admin tool returns HTTP 403 (insufficient_scope), not 401, in the E2E flow"
    - "agent_brain_mcp/oauth coverage is gated at ≥90% via task mcp:oauth-cov, measured authoritatively in the Keycloak CI job"
    - "The MCP Authorization live spec (2026-07-28 RC status) is re-verified and the result acknowledged in the design doc before shipping"
    - "An operator can configure split AS/RS mode from documented env vars (AGENT_BRAIN_OAUTH_JWKS_URI / INTROSPECTION_URL)"
  artifacts:
    - path: "agent-brain-mcp/Taskfile.yml"
      provides: "task mcp:oauth-cov (90% CI / 85% local) + task mcp:keycloak targets"
      contains: "oauth-cov"
    - path: "agent-brain-mcp/tests/test_oauth_keycloak_e2e.py"
      provides: "SC#4 full-dance E2E tests (tool call, refresh, scope-boundary 403)"
      contains: "test_scope_boundary_403"
    - path: "agent-brain-mcp/docs/SPLIT_AS_RS.md"
      provides: "Operator doc for split AS/RS config + RFC 8707 Keycloak deviation"
      contains: "AGENT_BRAIN_OAUTH_JWKS_URI"
    - path: "docs/plans/2026-06-14-mcp-v4-oauth-design.md"
      provides: "2026-07-28 RC re-verification acknowledgement (Phase 70 obligation closed)"
      contains: "2026-07-28 RC"
  key_links:
    - from: ".github/workflows/mcp-keycloak-integration.yml"
      to: "task mcp:oauth-cov"
      via: "CI job runs the 90% coverage gate (authoritative measurement)"
      pattern: "mcp:oauth-cov"
    - from: "agent-brain-mcp/Taskfile.yml (mcp:keycloak)"
      to: "pytest -m keycloak"
      via: "task target runs the keycloak-marked suite"
      pattern: "-m keycloak"
---

<objective>
Close the milestone DoD: the Keycloak-backed SC#4 E2E path runs against the official MCP
SDK client, the ≥90% oauth-module coverage gate is wired and binding in CI, the Phase 70
live-spec re-verification obligation is discharged, and operators have docs for split AS/RS
config.

SC#4 is satisfied by the Phase 69 + Phase 70 combination: the full browser PKCE dance
(401 → PRM → OASM → PKCE loopback → tool call) is proven against the co-located AS in
Phase 69's test_oauth_client_dance_e2e.py (CI Keycloak is headless — RESEARCH Open Q3).
The Phase 70 Keycloak tier proves the live RS path independently: a Keycloak-issued JWT is
accepted through the running RS for an authorized tool call, the refresh path works, and a
read-only-scoped token hitting an admin tool yields scope-boundary 403. Together they prove
the complete chain.

Purpose: Make the milestone shippable — the 90% gate blocks the release, the spec
acknowledgement satisfies the design-doc obligation, and operators can deploy split mode.
Output: Taskfile targets (mcp:oauth-cov + mcp:keycloak), SC#4 Keycloak E2E tests, the
operator doc, and the spec re-verification note in the design doc.
</objective>

<execution_context>
@/Users/richardhightower/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardhightower/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-split-as-rs-keycloak-in-ci-integration-tests/70-CONTEXT.md
@.planning/phases/70-split-as-rs-keycloak-in-ci-integration-tests/70-RESEARCH.md

<interfaces>
<!-- From 70-01 + 70-02 -->
verifier.py: JwksTokenVerifier, IntrospectionTokenVerifier, build_verifier.
keycloak marker registered (pyproject.toml). conftest_keycloak.py: keycloak_available
skip fixture, keycloak_access_token (direct-grant, scope="openid agent-brain:read") fixture,
keycloak_token_for_scope(scope) factory fixture, _ISSUER/_RESOURCE/_KC_BASE. These fixtures
are reachable because tests/conftest.py bridge-imports them (70-02 Task 2).
scripts/keycloak_bootstrap.sh creates realm agent-brain + client agent-brain-mcp + the
4 agent-brain scopes (agent-brain:read / :index / :admin / :subscribe).

Existing co-located dance E2E (Phase 69): agent-brain-mcp/tests/test_oauth_client_dance_e2e.py
drives the full PKCE browser-loopback flow against the co-located AS — reference it in a
docstring for the SC#4 PKCE leg; reuse its subprocess + OAuthClientProvider conventions for
the Phase 70 Keycloak tool-call assertions.

Scope-to-tool SOT (Phase 68): _tool_matrix.py / TOOL_SCOPE_REQUIREMENTS — admin tools
include cancel_job, remove_folder, clear_cache. A read-only (agent-brain:read) token calling
an admin tool must yield HTTP 403 insufficient_scope (design doc §"Token Validation on /mcp"
check #6).

Existing coverage gate task: agent-brain-mcp/Taskfile.yml `pr-qa-gate` runs
`--cov={{.SRC_DIR}} --cov-fail-under=80`. Add the per-module oauth gate alongside it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SC#4 Keycloak E2E suite (authorized tool call, token refresh, scope-boundary 403)</name>
  <files>
    agent-brain-mcp/tests/test_oauth_keycloak_e2e.py
  </files>
  <read_first>
    - agent-brain-mcp/tests/test_oauth_client_dance_e2e.py (Phase 69 — full PKCE browser-loopback dance against the co-located AS; OAuthClientProvider subprocess driver + tool-call assertions to reuse and to reference in the SC#4 docstring)
    - agent-brain-mcp/tests/conftest_keycloak.py (keycloak_access_token direct-grant fixture AND keycloak_token_for_scope(scope) factory fixture defined in 70-02; both are bridged via tests/conftest.py)
    - .planning/phases/70-split-as-rs-keycloak-in-ci-integration-tests/70-RESEARCH.md (§"SC#4 E2E Flow Structure"; Open Q3 — co-located full PKCE, Keycloak headless direct-grant)
    - docs/plans/2026-06-14-mcp-v4-oauth-design.md (§"Token Validation on /mcp" check #6 403 vs 401; §"Scope-to-Tool Mapping")
  </read_first>
  <action>
    Extend test_oauth_keycloak_e2e.py with the SC#4 Keycloak-path assertions (all
    `@pytest.mark.keycloak`, guarded by keycloak_available):
    - `test_full_oauth_dance_tool_call` (SC#4 core, Keycloak RS path): start an
      agent-brain-mcp HTTP subprocess with AGENT_BRAIN_AUTH=oauth,
      AGENT_BRAIN_OAUTH_RESOURCE=http://localhost:8000,
      AGENT_BRAIN_OAUTH_JWKS_URI={_ISSUER}/protocol/openid-connect/certs,
      AGENT_BRAIN_OAUTH_ISSUER={_ISSUER}. Obtain a Keycloak JWT with scope
      "agent-brain:read" via the keycloak_access_token fixture, then drive the official MCP
      SDK HTTP client with that Bearer token to call a read tool (e.g. get_corpus_status or
      search_documents) and assert a 200/successful MCP result. SCOPE NOTE for the docstring:
      the full browser PKCE-loopback dance leg (401 → PRM → OASM → /authorize → /token) is
      asserted against the CO-LOCATED AS in the existing Phase 69
      test_oauth_client_dance_e2e.py — reference that test by name in this test's docstring;
      the Keycloak tier here proves JWT acceptance through the live RS path with a
      direct-grant token (headless CI — RESEARCH Open Q3). SC#4 is the Phase 69 + Phase 70
      combination, not a single test.
    - `test_token_refresh_path` (SC#4 refresh): obtain an access+refresh token pair from
      Keycloak (offline_access or standard refresh), POST {_ISSUER}/protocol/openid-connect/token
      grant_type=refresh_token to get a NEW access token, then drive the MCP client with the
      refreshed token and assert the tool call still succeeds (200).
    - `test_scope_boundary_403` (SC#4 scope boundary — the headline OAUTH-06 cross-check):
      mint a Keycloak token scoped ONLY to read using the scoped-token factory —
      `token = keycloak_token_for_scope("openid agent-brain:read")` (NOT a broad/admin
      token) — drive the MCP client to call an ADMIN tool (clear_cache or remove_folder),
      and assert the response status is HTTP 403 with WWW-Authenticate containing
      `insufficient_scope` — assert it is NOT 401.
    Reuse the subprocess-spawn + OAuthClientProvider/streamablehttp_client patterns from the
    Phase 69 dance test. Mark the subprocess-driven tests with both `@pytest.mark.keycloak`
    and `@pytest.mark.e2e_http` if they spawn an HTTP subprocess so they are excluded from
    the fast tier by either marker.
  </action>
  <verify>
    <automated>cd agent-brain-mcp && poetry run pytest -m keycloak tests/test_oauth_keycloak_e2e.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - test_oauth_keycloak_e2e.py contains `test_full_oauth_dance_tool_call`, `test_token_refresh_path`, `test_scope_boundary_403`
    - test_scope_boundary_403 uses `keycloak_token_for_scope("openid agent-brain:read")` (the read-only scoped token from the 70-02 factory fixture)
    - test_scope_boundary_403 asserts `insufficient_scope` and asserts status is 403 (grep shows both `403` and `insufficient_scope` in that test body)
    - test_full_oauth_dance_tool_call references `AGENT_BRAIN_OAUTH_JWKS_URI` and references `test_oauth_client_dance_e2e` in a docstring (the Phase 69 PKCE-dance leg)
    - `cd agent-brain-mcp && poetry run pytest -m keycloak tests/test_oauth_keycloak_e2e.py --collect-only -q` succeeds (collection clean; skips without container)
    - `cd agent-brain-mcp && poetry run pytest -m 'not e2e and not e2e_http and not contract and not stress and not keycloak' -q` still excludes these (no new fast-tier cost)
  </acceptance_criteria>
  <done>SC#4 Keycloak path — authorized tool call, token refresh, and read-only-token-vs-admin-tool 403 — are expressed as keycloak-marked E2E tests against the official MCP SDK client, with the browser PKCE dance leg referenced to Phase 69's co-located test.</done>
</task>

<task type="auto">
  <name>Task 2: Coverage gate task targets (mcp:oauth-cov 90% CI / 85% local + mcp:keycloak) and wire into CI</name>
  <files>
    agent-brain-mcp/Taskfile.yml,
    .github/workflows/mcp-keycloak-integration.yml
  </files>
  <read_first>
    - agent-brain-mcp/Taskfile.yml (existing test:cov ~line 78, pr-qa-gate ~line 217 with --cov-fail-under=80, e2e/contract/stress target conventions, SRC_DIR/TEST_DIR vars)
    - .github/workflows/mcp-keycloak-integration.yml (the job created in 70-02 — add the oauth-cov gate step)
    - .planning/phases/70-split-as-rs-keycloak-in-ci-integration-tests/70-RESEARCH.md (§"Coverage gate wiring"; Open Q4 — 90% CI hard / 85% local soft; Pitfall 4 — local-vs-CI coverage)
    - .planning/phases/70-split-as-rs-keycloak-in-ci-integration-tests/70-CONTEXT.md (Coverage gate decision: authoritative measurement in the CI Keycloak job)
  </read_first>
  <action>
    agent-brain-mcp/Taskfile.yml — add two targets (bare names; root Taskfile's `includes: mcp:`
    alias namespaces them as task mcp:oauth-cov / task mcp:keycloak):
    - `oauth-cov`: desc "OAuth module coverage gate (90% CI hard / 85% local soft per RESEARCH
      Open Q4)". Accept a COV_FAIL_UNDER var defaulting to 85 locally; CI passes 90. Cmd:
      `poetry run pytest {{.TEST_DIR}} --cov=agent_brain_mcp.oauth --cov-report=term-missing
      --cov-fail-under={{.COV_FAIL_UNDER | default "85"}}`. Add a vars block defaulting
      COV_FAIL_UNDER: '85'. Document in the desc that the BINDING 90% gate runs in the CI
      Keycloak job (Keycloak-only paths are skipped locally — Pitfall 4).
    - `keycloak`: desc "Run the Keycloak-backed integration suite (CI/nightly — requires a
      running Keycloak container; opt-in)". Cmd: `poetry run pytest {{.TEST_DIR}} -v -m keycloak`.
      deps: [install].

    .github/workflows/mcp-keycloak-integration.yml — after the `task mcp:keycloak` step (or
    as a combined run), add the authoritative coverage gate step that runs the FULL suite
    (fast + keycloak) with the 90% hard gate:
    `cd agent-brain-mcp && poetry run pytest tests/ -m 'not e2e and not contract and not stress'
    --cov=agent_brain_mcp.oauth --cov-fail-under=90` — i.e. include the keycloak-marked tests
    so the Keycloak-only lines count toward the 90%, with env KEYCLOAK_URL +
    AGENT_BRAIN_OAUTH_RESOURCE set. (Equivalently invoke `task mcp:oauth-cov COV_FAIL_UNDER=90`
    if the keycloak tests are included by that target — ensure the target run in CI does NOT
    exclude `-m keycloak` so the gate measures the full picture.) Add a YAML comment: this is
    the binding 90% DoD gate (SC#5) and it BLOCKS the milestone.
  </action>
  <verify>
    <automated>cd agent-brain-mcp && task --list 2>/dev/null | grep -E "oauth-cov|keycloak" && python3 -c "import yaml; yaml.safe_load(open('.github/workflows/mcp-keycloak-integration.yml')); print('YAML OK')"</automated>
  </verify>
  <acceptance_criteria>
    - agent-brain-mcp/Taskfile.yml contains `oauth-cov` and `keycloak` targets
    - Taskfile.yml oauth-cov contains `--cov=agent_brain_mcp.oauth` and `--cov-fail-under`
    - Taskfile.yml keycloak target contains `-m keycloak`
    - `cd agent-brain-mcp && task --list` lists both targets (or root `task --list` shows mcp:oauth-cov / mcp:keycloak)
    - mcp-keycloak-integration.yml contains `--cov=agent_brain_mcp.oauth` and `--cov-fail-under=90`
    - mcp-keycloak-integration.yml is valid YAML (python3 yaml.safe_load exits 0)
  </acceptance_criteria>
  <done>task mcp:oauth-cov and task mcp:keycloak exist; the binding ≥90% oauth coverage gate (SC#5) runs authoritatively in the CI Keycloak job, including the keycloak-marked lines.</done>
</task>

<task type="auto">
  <name>Task 3: Live-spec re-verification acknowledgement + operator split-AS/RS docs</name>
  <files>
    docs/plans/2026-06-14-mcp-v4-oauth-design.md,
    agent-brain-mcp/docs/SPLIT_AS_RS.md
  </files>
  <read_first>
    - docs/plans/2026-06-14-mcp-v4-oauth-design.md (§"Spec Version Citation" ~line 45 + "2026-07-28 RC Staleness Acknowledgement" ~line 58 — append the Phase 70 re-verification result here; §"Deployment Shape B" ~line 691)
    - .planning/phases/70-split-as-rs-keycloak-in-ci-integration-tests/70-RESEARCH.md (§"State of the Art" → MCP 2026-07-28 RC status verified 2026-06-22; §"CRITICAL Finding" RFC 8707 workaround for the operator doc)
    - .planning/phases/70-split-as-rs-keycloak-in-ci-integration-tests/70-CONTEXT.md (§"Live-spec re-verification obligation")
  </read_first>
  <action>
    Re-verify the live MCP Authorization spec status using context7 (the `ctx7` CLI per the
    global rule): resolve `/modelcontextprotocol/modelcontextprotocol` and query whether the
    2026-07-28 RC (SEP-1442 MCP-stateless / no-initialize handshake, PR #2575) has merged into
    the normative authorization spec, and whether the mcp Python SDK has shipped a 1.28+ with
    auth API changes affecting RequireAuthMiddleware / OAuthClientProvider. If context7/network
    is unavailable, record that the check was attempted and fall back to the 2026-06-22 RESEARCH
    finding (RC still draft; 2025-11-25 baseline normative; zero auth-logic impact).

    docs/plans/2026-06-14-mcp-v4-oauth-design.md — append a dated "Phase 70 Spec Re-Verification
    (YYYY-MM-DD)" note under the "2026-07-28 RC Staleness Acknowledgement" section recording:
    (a) the verification date, (b) whether PR #2575 / the RC has landed, (c) the mcp SDK version
    in pyproject.toml (^1.27.2) and whether a bump is required, (d) the conclusion that
    RequireAuthMiddleware validates Bearer tokens per-request independently of the initialize
    handshake → no auth-logic change required for Phase 70. If a bump IS required, note the
    follow-up issue to file.

    agent-brain-mcp/docs/SPLIT_AS_RS.md — NEW operator doc covering split AS/RS deployment:
    - The two deployment shapes (co-located vs split) and when to use split (external IdP).
    - Env vars: AGENT_BRAIN_AUTH=oauth, AGENT_BRAIN_OAUTH_RESOURCE (canonical aud URI),
      AGENT_BRAIN_OAUTH_ISSUER (MUST equal the IdP issuer incl. realm path — Pitfall 7 example
      http://idp:8080/realms/agent-brain), AGENT_BRAIN_OAUTH_JWKS_URI (selects JwksTokenVerifier),
      AGENT_BRAIN_OAUTH_INTROSPECTION_URL + _CLIENT_ID + _CLIENT_SECRET (selects
      IntrospectionTokenVerifier for opaque tokens). State precedence: JWKS_URI wins if both set.
    - A Keycloak configuration subsection documenting the RFC 8707 deviation: Keycloak lacks
      native Resource Indicators until 26.8; bind aud via the audience scope mapper
      (Included Custom Audience = AGENT_BRAIN_OAUTH_RESOURCE). Reference scripts/keycloak_bootstrap.sh.
    - Revocation behavior: split path honors revocation via introspection (active:false → 401);
      co-located path uses the in-memory jti denylist. No public /revoke endpoint (deferred to v10.4.1).
  </action>
  <verify>
    <automated>grep -q "Phase 70 Spec Re-Verification" docs/plans/2026-06-14-mcp-v4-oauth-design.md && grep -q "AGENT_BRAIN_OAUTH_JWKS_URI" agent-brain-mcp/docs/SPLIT_AS_RS.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - docs/plans/2026-06-14-mcp-v4-oauth-design.md contains `Phase 70 Spec Re-Verification` with a date and the RequireAuthMiddleware per-request conclusion
    - agent-brain-mcp/docs/SPLIT_AS_RS.md exists and contains `AGENT_BRAIN_OAUTH_JWKS_URI`, `AGENT_BRAIN_OAUTH_INTROSPECTION_URL`, and `AGENT_BRAIN_OAUTH_ISSUER`
    - SPLIT_AS_RS.md documents the RFC 8707 Keycloak deviation (`audience scope mapper`) and references `keycloak_bootstrap.sh`
    - SPLIT_AS_RS.md documents revocation (introspection active:false + jti denylist) and that no public /revoke ships in this phase
  </acceptance_criteria>
  <done>The Phase 70 live-spec re-verification obligation is discharged in the design doc and operators have a documented path to deploy split AS/RS mode with the Keycloak RFC 8707 workaround.</done>
</task>

</tasks>

<verification>
Local — fast tier green, keycloak tier cleanly skipped, soft local oauth gate:
```bash
cd agent-brain-mcp && task oauth-cov            # 85% soft local gate
cd "$(git rev-parse --show-toplevel)" && task before-push
cd "$(git rev-parse --show-toplevel)" && task pr-qa-gate
```
CI (Keycloak job) — full suite incl. keycloak tests with the binding 90% gate:
```bash
# runs in .github/workflows/mcp-keycloak-integration.yml
pytest tests/ -m 'not e2e and not contract and not stress' --cov=agent_brain_mcp.oauth --cov-fail-under=90
```
</verification>

<success_criteria>
- SC#4 Keycloak path (tool call + refresh + scope-boundary 403) verified against the MCP SDK client; full PKCE dance proven by Phase 69 (SC#4 = Phase 69 + Phase 70)
- task mcp:oauth-cov + task mcp:keycloak exist; the ≥90% gate (SC#5) is binding in the CI Keycloak job
- The 2026-07-28 RC re-verification is recorded in the design doc (Phase 70 obligation closed)
- Operators have a split-AS/RS config doc including the Keycloak RFC 8707 workaround
- `task before-push` and `task pr-qa-gate` exit 0
</success_criteria>

<output>
After completion, create `.planning/phases/70-split-as-rs-keycloak-in-ci-integration-tests/70-03-SUMMARY.md`
</output>
