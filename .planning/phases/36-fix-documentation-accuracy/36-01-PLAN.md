---
phase: 36-fix-documentation-accuracy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/CONFIGURATION.md
  - docs/QUICK_START.md
  - docs/PLUGIN_GUIDE.md
autonomous: true
requirements:
  - CFGDOC-01
  - GUIDE-02
  - GUIDE-03
gap_closure: true

must_haves:
  truths:
    - "No documentation file contains the stale path .claude/agent-brain/"
    - "All references to the project state directory use .agent-brain/"
    - "QUICK_START.md init description matches actual CLI behavior"
    - "PLUGIN_GUIDE.md init description and code example use correct path"
  artifacts:
    - path: "docs/CONFIGURATION.md"
      provides: "Corrected path references at lines 34, 290, 300, 525, 644"
      contains: ".agent-brain/config.json"
    - path: "docs/QUICK_START.md"
      provides: "Corrected init description"
      contains: ".agent-brain/"
    - path: "docs/PLUGIN_GUIDE.md"
      provides: "Corrected init description and code example"
      contains: ".agent-brain/"
  key_links:
    - from: "docs/CONFIGURATION.md"
      to: "agent-brain-cli/agent_brain_cli/commands/init.py"
      via: "STATE_DIR_NAME consistency"
      pattern: "\\.agent-brain"
---

<objective>
Replace all stale `.claude/agent-brain/` path references with `.agent-brain/` in CONFIGURATION.md, QUICK_START.md, and PLUGIN_GUIDE.md.

Purpose: Users following these docs will see paths that match what `agent-brain init` actually creates (`.agent-brain/`).
Output: Three documentation files with corrected directory paths.
</objective>

<execution_context>
@/Users/richardhightower/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardhightower/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v10.0-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix stale paths in CONFIGURATION.md</name>
  <files>docs/CONFIGURATION.md</files>
  <read_first>docs/CONFIGURATION.md, agent-brain-cli/agent_brain_cli/commands/init.py</read_first>
  <action>
Replace every occurrence of `.claude/agent-brain/` with `.agent-brain/` in docs/CONFIGURATION.md. The specific lines from the audit:

- Line 34: `.claude/agent-brain/config.json` -> `.agent-brain/config.json`
- Line 290: `export AGENT_BRAIN_STATE_DIR="/path/to/.claude/agent-brain"` -> `export AGENT_BRAIN_STATE_DIR="/path/to/.agent-brain"`
- Line 300: `agent-brain start --state-dir /path/to/.claude/agent-brain` -> `agent-brain start --state-dir /path/to/.agent-brain`
- Line 525: `Create .claude/agent-brain/config.json` -> `Create .agent-brain/config.json`
- Line 644: `Project config (.claude/agent-brain/config.json):` -> `Project config (.agent-brain/config.json):`

Use a global find-and-replace for `.claude/agent-brain/` -> `.agent-brain/` and then verify no occurrences remain.
  </action>
  <verify>
    <automated>grep -c "\.claude/agent-brain/" docs/CONFIGURATION.md | grep -q "^0$" && echo "PASS: no stale paths" || echo "FAIL: stale paths remain"</automated>
  </verify>
  <acceptance_criteria>
- `grep -c ".claude/agent-brain/" docs/CONFIGURATION.md` returns 0
- `grep -c ".agent-brain/config.json" docs/CONFIGURATION.md` returns at least 3
- Line 34 contains `.agent-brain/config.json` (not `.claude/agent-brain/config.json`)
- Line 290 contains `/path/to/.agent-brain` (not `/path/to/.claude/agent-brain`)
- Line 525 contains `.agent-brain/config.json`
  </acceptance_criteria>
  <done>Zero occurrences of `.claude/agent-brain/` in CONFIGURATION.md. All five audit-identified lines corrected to `.agent-brain/`.</done>
</task>

<task type="auto">
  <name>Task 2: Fix stale paths in QUICK_START.md and PLUGIN_GUIDE.md</name>
  <files>docs/QUICK_START.md, docs/PLUGIN_GUIDE.md</files>
  <read_first>docs/QUICK_START.md, docs/PLUGIN_GUIDE.md</read_first>
  <action>
In docs/QUICK_START.md:
- Line 57: Change `This creates a .claude/agent-brain/ directory` to `This creates a .agent-brain/ directory`

In docs/PLUGIN_GUIDE.md:
- Line 364: Change `Creates .claude/agent-brain/ with project configuration.` to `Creates .agent-brain/ with project configuration.`
- Line 639: Change `Path(".claude/agent-brain/runtime.json")` to `Path(".agent-brain/runtime.json")`

For both files, do a global search for `.claude/agent-brain/` to catch any additional occurrences beyond the audit-identified lines.
  </action>
  <verify>
    <automated>grep -c "\.claude/agent-brain/" docs/QUICK_START.md docs/PLUGIN_GUIDE.md | grep -v ":0$" | wc -l | grep -q "^0$" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
- `grep -c ".claude/agent-brain/" docs/QUICK_START.md` returns 0
- `grep -c ".claude/agent-brain/" docs/PLUGIN_GUIDE.md` returns 0
- `grep ".agent-brain/ directory" docs/QUICK_START.md` finds the corrected line 57
- `grep "Creates .agent-brain/" docs/PLUGIN_GUIDE.md` finds the corrected line 364
- `grep ".agent-brain/runtime.json" docs/PLUGIN_GUIDE.md` finds the corrected line 639
  </acceptance_criteria>
  <done>Zero occurrences of `.claude/agent-brain/` in both QUICK_START.md and PLUGIN_GUIDE.md. All three audit-identified lines corrected.</done>
</task>

</tasks>

<verification>
Run across all three files to confirm zero stale paths remain:
```bash
grep -rn "\.claude/agent-brain/" docs/CONFIGURATION.md docs/QUICK_START.md docs/PLUGIN_GUIDE.md
```
Expected: no output (zero matches).

Confirm correct paths are present:
```bash
grep -c "\.agent-brain/" docs/CONFIGURATION.md docs/QUICK_START.md docs/PLUGIN_GUIDE.md
```
Expected: each file shows at least 1 match.
</verification>

<success_criteria>
- Zero occurrences of `.claude/agent-brain/` across CONFIGURATION.md, QUICK_START.md, PLUGIN_GUIDE.md
- All audit-identified lines (CONFIGURATION.md lines 34/290/300/525/644, QUICK_START.md line 57, PLUGIN_GUIDE.md lines 364/639) corrected to `.agent-brain/`
</success_criteria>

<output>
After completion, create `.planning/phases/36-fix-documentation-accuracy/36-01-SUMMARY.md`
</output>
