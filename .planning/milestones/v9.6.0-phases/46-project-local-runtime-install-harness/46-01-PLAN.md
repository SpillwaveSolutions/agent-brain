---
phase: 46-project-local-runtime-install-harness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - e2e-cli/.gitignore
  - e2e-cli/run.sh
  - e2e-cli/README.md
  - e2e-cli/fixtures/runtime-project-template/README.md
  - e2e-cli/lib/harness.sh
  - e2e-cli/lib/report.sh
  - e2e-cli/lib/runtime_parity.sh
  - e2e-cli/tests/test_runtime_project_plumbing.sh
autonomous: true
requirements: [ISO-01, ISO-02]

must_haves:
  truths:
    - "Runtime parity workspaces are created only under a repo-owned, git-ignored e2e_workdir root"
    - "Every runtime workspace gets an isolated <runtime>-runtime directory plus a cleanup area"
    - "The e2e harness can create, log, report on, and clean project-local workspaces without relying on missing helper files"
    - "Project-local install plumbing rejects user-global runtime directories and targets only repo-owned fixture/workspace paths"
  artifacts:
    - path: ".gitignore"
      provides: "Root-level ignore rule for repo-owned runtime workspaces"
      contains: "e2e_workdir/"
    - path: "e2e-cli/lib/harness.sh"
      provides: "Workspace lifecycle, server lifecycle, assertion reset, and scenario logging helpers used by run.sh and tests"
      contains: "workspace_create"
    - path: "e2e-cli/lib/report.sh"
      provides: "Result aggregation helpers used by run.sh"
      contains: "report_add_result"
    - path: "e2e-cli/lib/runtime_parity.sh"
      provides: "Runtime-specific workspace preparation and project-local install target validation under e2e_workdir"
      contains: "runtime_workspace_prepare"
    - path: "e2e-cli/tests/test_runtime_project_plumbing.sh"
      provides: "Regression coverage for project-local runtime workspace creation and forbidden global target rejection"
      contains: "e2e_workdir"
  key_links:
    - from: "e2e-cli/run.sh"
      to: "e2e-cli/lib/harness.sh"
      via: "source statements + workspace/server lifecycle calls"
      pattern: "workspace_create"
    - from: "e2e-cli/run.sh"
      to: "e2e-cli/lib/report.sh"
      via: "source statements + result recording calls"
      pattern: "report_add_result"
    - from: "e2e-cli/lib/runtime_parity.sh"
      to: "e2e-cli/tests/test_runtime_project_plumbing.sh"
      via: "runtime_workspace_prepare and runtime_install_project_local exercised by shell regression test"
      pattern: "runtime_workspace_prepare"
---

<objective>
Repair the E2E harness foundation and move runtime parity workspaces to a repo-owned, git-ignored `e2e_workdir/` layout with isolated per-runtime directories and cleanup support.

Purpose: Phase 46 cannot deliver project-local runtime parity if the harness still points at `.runs/`, if the workspace helpers referenced by `run.sh` do not exist, or if runtime workspace preparation is not aligned with the approved `e2e_workdir/<runtime>-runtime/cleanup/` structure.
Output: A runnable harness helper layer (`harness.sh`, `report.sh`, updated `runtime_parity.sh`) plus docs/tests that lock runtime parity plumbing to `e2e_workdir/`.
</objective>

<execution_context>
@/Users/richardhightower/clients/spillwave/src/agent-brain/.codex/get-shit-done/workflows/execute-plan.md
@/Users/richardhightower/clients/spillwave/src/agent-brain/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/46-project-local-runtime-install-harness/46-CONTEXT.md

<interfaces>
From e2e-cli/run.sh:
```bash
source "$E2E_ROOT/lib/harness.sh"
source "$E2E_ROOT/lib/report.sh"
source "$E2E_ROOT/lib/runtime_parity.sh"

RUN_ID="$(date +%Y%m%d-%H%M%S)-$$"
RUNS_DIR="${E2E_ROOT}/.runs/${RUN_ID}"

server_start
report_init
workspace_create "$local_name"
workspace_clean "$SCENARIO_WORKSPACE"
report_add_result ...
```

From e2e-cli/lib/runtime_parity.sh:
```bash
runtime_fixture_template_dir()
runtime_expected_target_relpath()
runtime_is_forbidden_global_path()
runtime_assert_repo_owned_project_dir()
runtime_workspace_prepare()
runtime_install_project_local()
runtime_parity_snapshot_global_opencode()
runtime_parity_detect_global_mutation()
```

From 46-CONTEXT.md:
```text
D-01: use a git-ignored e2e_workdir/ root
D-02: use e2e_workdir/<runtime>-runtime/
D-03: include cleanup/ in each runtime workspace
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore the missing harness helper layer and redirect workspace roots to e2e_workdir</name>
  <files>
    .gitignore,
    e2e-cli/.gitignore,
    e2e-cli/run.sh,
    e2e-cli/lib/harness.sh,
    e2e-cli/lib/report.sh
  </files>
  <read_first>
    - .gitignore
    - e2e-cli/.gitignore
    - e2e-cli/run.sh
    - e2e-cli/tests/test_runtime_project_plumbing.sh
    - .planning/phases/46-project-local-runtime-install-harness/46-CONTEXT.md
  </read_first>
  <action>
Create `e2e-cli/lib/harness.sh` with the concrete helper functions `log_info`, `log_warn`, `log_fail`, `workspace_create`, `workspace_clean`, `assert_reset`, `assert_success`, `assert_failure`, `assert_contains`, `assert_matches`, `assert_json`, `assert_gt`, `assert_all_passed`, `server_is_running`, `server_start`, and `server_stop`, because `run.sh` and `test_runtime_project_plumbing.sh` already source and call those names.

In `e2e-cli/lib/harness.sh`, make workspace creation target a repo-owned root under the repository, not `e2e-cli/.runs/`. Use an exported workspace root variable with this fallback order:
1. `RUNTIME_WORKDIR_ROOT` if already set
2. `${REPO_ROOT}/e2e_workdir`

Have `workspace_create "$scenario_name"` create `${RUNTIME_WORKDIR_ROOT}/${RUN_ID}/${ADAPTER_NAME}/${scenario_name}` and export that absolute path as `SCENARIO_WORKSPACE`. Create `${SCENARIO_WORKSPACE}/cleanup`, `${SCENARIO_WORKSPACE}/logs`, and `${SCENARIO_WORKSPACE}/scenario.log`. Ensure `workspace_clean` removes only `${SCENARIO_WORKSPACE}/project` on success and preserves the workspace/logs root.

Add root `.gitignore` entry `e2e_workdir/` and update `e2e-cli/.gitignore` comments so they no longer describe `.runs/` as the active runtime workspace root.

Update `e2e-cli/run.sh` so `RUNS_DIR` points at `"${REPO_ROOT}/e2e_workdir/${RUN_ID}"` and all user-facing log messages/docs refer to `e2e_workdir/` instead of `.runs/`. Keep the existing adapter/scenario structure and keep `report_init`, `report_add_result`, and workspace cleanup behavior intact.

Create `e2e-cli/lib/report.sh` with concrete helpers `report_init`, `report_add_result`, and `report_finalize` that write `report.json` and `report.md` into `${RUNS_DIR}`. `report_init` must initialize JSON/markdown report files, and `report_add_result` must append scenario results with fields `scenario`, `status`, `duration_sec`, `assertions_passed`, `assertions_total`, and `message`.
  </action>
  <verify>
    <automated>cd /Users/richardhightower/clients/spillwave/src/agent-brain && bash -n e2e-cli/run.sh e2e-cli/lib/harness.sh e2e-cli/lib/report.sh && rg -n "e2e_workdir/" .gitignore e2e-cli/run.sh e2e-cli/lib/harness.sh e2e-cli/.gitignore</automated>
  </verify>
  <acceptance_criteria>
    - `test -f e2e-cli/lib/harness.sh` returns 0
    - `test -f e2e-cli/lib/report.sh` returns 0
    - `rg -n "workspace_create|workspace_clean|server_start|server_stop" e2e-cli/lib/harness.sh` returns 0
    - `rg -n "report_init|report_add_result" e2e-cli/lib/report.sh e2e-cli/run.sh` returns 0
    - `rg -n "e2e_workdir/" .gitignore e2e-cli/run.sh e2e-cli/lib/harness.sh` returns 0
    - `bash -n e2e-cli/run.sh e2e-cli/lib/harness.sh e2e-cli/lib/report.sh` exits 0
    - `e2e-cli/run.sh` no longer contains `E2E_ROOT/.runs`
  </acceptance_criteria>
  <done>The harness helper layer exists, `run.sh` uses repo-owned `e2e_workdir/`, and the workspace/report functions referenced by the harness are implemented</done>
</task>

<task type="auto">
  <name>Task 2: Align runtime workspace preparation and plumbing tests with e2e_workdir/<runtime>-runtime/cleanup</name>
  <files>
    e2e-cli/lib/runtime_parity.sh,
    e2e-cli/tests/test_runtime_project_plumbing.sh,
    e2e-cli/README.md,
    e2e-cli/fixtures/runtime-project-template/README.md
  </files>
  <read_first>
    - e2e-cli/lib/runtime_parity.sh
    - e2e-cli/tests/test_runtime_project_plumbing.sh
    - e2e-cli/README.md
    - e2e-cli/fixtures/runtime-project-template/README.md
    - e2e-cli/lib/harness.sh
    - .planning/phases/46-project-local-runtime-install-harness/46-CONTEXT.md
  </read_first>
  <action>
Update `e2e-cli/lib/runtime_parity.sh` so runtime workspace preparation follows D-01 through D-03 exactly:
- `runtime_assert_repo_owned_project_dir` must allow repo-owned paths rooted under `${REPO_ROOT}/e2e_workdir`
- `runtime_workspace_prepare "$runtime" "$scenario_root"` must create the disposable project at `${scenario_root}/${runtime}-runtime/project`
- it must also create `${scenario_root}/${runtime}-runtime/cleanup` and `${scenario_root}/${runtime}-runtime/logs`
- it must refuse any target under `${HOME}/.codex`, `${HOME}/.config/opencode`, or `${HOME}/.config/gemini`

Update `e2e-cli/tests/test_runtime_project_plumbing.sh` to assert the new runtime-specific workspace shape:
- set `RUNS_DIR="$REPO_ROOT/e2e_workdir/$RUN_ID"`
- verify `PROJECT_DIR` equals `"$SCENARIO_WORKSPACE/codex-runtime/project"`
- verify `"$SCENARIO_WORKSPACE/codex-runtime/cleanup"` exists
- keep the forbidden-global-path assertions and fake `poetry` install-agent interception
- keep the fixture template hash snapshot so the checked-in template remains untouched

Update `e2e-cli/README.md` and `e2e-cli/fixtures/runtime-project-template/README.md` so runtime parity documentation describes `e2e_workdir/<run-id>/<adapter>/<scenario>/<runtime>-runtime/` as the disposable workspace root and calls out the `cleanup/` directory explicitly.
  </action>
  <verify>
    <automated>cd /Users/richardhightower/clients/spillwave/src/agent-brain && bats e2e-cli/tests/test_runtime_project_plumbing.sh</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "e2e_workdir" e2e-cli/lib/runtime_parity.sh e2e-cli/tests/test_runtime_project_plumbing.sh e2e-cli/README.md e2e-cli/fixtures/runtime-project-template/README.md` returns 0
    - `rg -n "cleanup" e2e-cli/lib/runtime_parity.sh e2e-cli/tests/test_runtime_project_plumbing.sh e2e-cli/README.md` returns 0
    - `test_runtime_project_plumbing.sh` asserts `codex-runtime/project`
    - `runtime_assert_repo_owned_project_dir` allows repo-owned `e2e_workdir` paths and still rejects user-global runtime roots
    - `bats e2e-cli/tests/test_runtime_project_plumbing.sh` exits 0
  </acceptance_criteria>
  <done>Runtime workspace preparation, docs, and regression coverage all use repo-owned e2e_workdir runtime directories with cleanup support</done>
</task>

</tasks>

<verification>
- `bash -n e2e-cli/run.sh e2e-cli/lib/harness.sh e2e-cli/lib/report.sh e2e-cli/lib/runtime_parity.sh` passes
- `bats e2e-cli/tests/test_runtime_project_plumbing.sh` passes
- `rg -n "e2e_workdir/" .gitignore e2e-cli/run.sh e2e-cli/lib/runtime_parity.sh e2e-cli/README.md` returns expected matches
- `rg -n "workspace_create|report_add_result|cleanup" e2e-cli/lib/harness.sh e2e-cli/lib/report.sh e2e-cli/lib/runtime_parity.sh` returns expected helper definitions/usages
</verification>

<success_criteria>
1. Runtime parity workspaces are created only under repo-owned `e2e_workdir/`
2. Every runtime workspace contains runtime-specific directories plus a cleanup area
3. The harness helper files referenced by `run.sh` exist and are runnable
4. Project-local install plumbing remains protected against user-global runtime paths
5. Runtime workspace documentation and regression tests match the implemented layout
</success_criteria>

<output>
After completion, create `.planning/phases/46-project-local-runtime-install-harness/46-01-SUMMARY.md`
</output>
