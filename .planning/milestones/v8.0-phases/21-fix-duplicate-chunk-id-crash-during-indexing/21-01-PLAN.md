---
phase: 21-fix-duplicate-chunk-id-crash-during-indexing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agent-brain-server/agent_brain_server/storage/vector_store.py
  - agent-brain-server/agent_brain_server/storage/postgres/backend.py
  - agent-brain-server/tests/unit/storage/test_vector_store_deduplication.py
autonomous: true
requirements:
  - DEDUP-01
  - DEDUP-02
  - DEDUP-03

must_haves:
  truths:
    - "Indexing a directory containing duplicate files completes without DuplicateIDError"
    - "Duplicate chunk IDs within a single upsert batch are deduplicated with last-occurrence-wins semantics"
    - "A warning is logged when duplicates are detected, including count and sample IDs"
    - "Batches without duplicates pass through unchanged (no data loss)"
    - "All existing tests continue to pass"
  artifacts:
    - path: "agent-brain-server/agent_brain_server/storage/vector_store.py"
      provides: "Dict-based O(N) deduplication in upsert_documents() before ChromaDB call"
      contains: "seen"
    - path: "agent-brain-server/agent_brain_server/storage/postgres/backend.py"
      provides: "Same deduplication guard for PostgreSQL backend consistency"
      contains: "seen"
    - path: "agent-brain-server/tests/unit/storage/test_vector_store_deduplication.py"
      provides: "Regression tests for duplicate and non-duplicate batches"
      min_lines: 50
  key_links:
    - from: "agent-brain-server/agent_brain_server/storage/vector_store.py"
      to: "chromadb collection.upsert()"
      via: "deduplication before upsert call"
      pattern: "seen\\[id_\\]"
    - from: "agent-brain-server/agent_brain_server/storage/postgres/backend.py"
      to: "keyword_ops.upsert + vector_ops.upsert"
      via: "deduplication before DB operations"
      pattern: "seen\\[id_\\]"
---

<objective>
Fix the DuplicateIDError crash that occurs when indexing directories containing duplicate files (e.g., Confluence exports with the same file in multiple subdirectories). Add dict-based O(N) deduplication to both ChromaDB and PostgreSQL upsert paths, with warning logging and regression tests.

Purpose: Indexing should never crash due to duplicate chunk IDs in a batch -- defensive deduplication at the storage layer handles any upstream source of colliding IDs.
Output: Patched upsert_documents() in both backends + regression test file
</objective>

<execution_context>
@/Users/richardhightower/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardhightower/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-fix-duplicate-chunk-id-crash-during-indexing/21-RESEARCH.md

@agent-brain-server/agent_brain_server/storage/vector_store.py
@agent-brain-server/agent_brain_server/storage/postgres/backend.py
@agent-brain-server/tests/unit/storage/test_vector_store_metadata.py

<interfaces>
<!-- Key method signatures the executor needs -->

From agent_brain_server/storage/vector_store.py (line 250):
```python
async def upsert_documents(
    self,
    ids: list[str],
    embeddings: list[list[float]],
    documents: list[str],
    metadatas: list[dict[str, Any]] | None = None,
) -> int:
```

From agent_brain_server/storage/postgres/backend.py (line 122):
```python
async def upsert_documents(
    self,
    ids: list[str],
    embeddings: list[list[float]],
    documents: list[str],
    metadatas: list[dict[str, Any]],
) -> int:
```

From tests/unit/storage/test_vector_store_metadata.py (fixture pattern):
```python
@pytest.fixture
def mock_collection():
    col = MagicMock()
    col.upsert = MagicMock()
    col.metadata = {}
    return col
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add deduplication to ChromaDB and PostgreSQL upsert paths</name>
  <files>
    agent-brain-server/tests/unit/storage/test_vector_store_deduplication.py,
    agent-brain-server/agent_brain_server/storage/vector_store.py,
    agent-brain-server/agent_brain_server/storage/postgres/backend.py
  </files>
  <behavior>
    - test_upsert_deduplicates_batch: Pass ids=["a","b","a"] with embeddings/docs/metas; ChromaDB receives 2 unique entries; last-occurrence values for "a" are kept; return count is 2
    - test_upsert_no_duplicates_unchanged: Pass ids=["x","y"] with no duplicates; ChromaDB receives exact same lists; return count is 2
    - test_upsert_logs_warning_on_duplicates: Pass batch with duplicates; verify logger.warning called with duplicate count
    - test_postgres_upsert_deduplicates: Mock PostgreSQL backend; pass batch with duplicates; verify deduplicated lists reach keyword_ops/vector_ops
    - test_upsert_empty_batch: Pass empty lists; no crash, return 0
  </behavior>
  <action>
    1. Create `tests/unit/storage/test_vector_store_deduplication.py` with tests for ALL behaviors above. Follow the mock pattern from `test_vector_store_metadata.py` — mock `chromadb.PersistentClient` and set `store._collection = mock_collection`. For PostgreSQL tests, mock the `keyword_ops` and `vector_ops` on the backend instance.

    2. Run tests (RED — they should fail because deduplication logic does not exist yet).

    3. In `vector_store.py` `upsert_documents()`, AFTER the existing length check and BEFORE `async with self._lock`:
       - Compute `safe_metadatas = metadatas or [{}] * len(ids)` (move this line up from inside the lock)
       - Build `seen: dict[str, tuple[list[float], str, dict[str, Any]]] = {}` by zipping `ids, embeddings, documents, safe_metadatas`, last-writer-wins
       - Fast path: if `len(seen) == len(ids)`, no duplicates, continue with original lists
       - Slow path: log WARNING with `dup_count = len(ids) - len(seen)` and sample of up to 5 duplicate IDs, then reconstruct `ids, embeddings, documents, safe_metadatas` from `seen`
       - Update the return to `return len(ids)` (post-dedup count)
       - Inside the lock, use `safe_metadatas` directly (already computed above)

    4. In `postgres/backend.py` `upsert_documents()`, AFTER the existing length check:
       - Same dict-based deduplication pattern with `seen` dict
       - Same WARNING log on duplicates
       - Reconstruct all four lists from `seen` if duplicates found

    5. Run tests (GREEN — all should pass).

    Important: Do NOT change the method signatures. Do NOT deduplicate in chunking.py. Do NOT raise exceptions on duplicates — silently deduplicate with a warning log. Use `logger = logging.getLogger(__name__)` (already present in both files).
  </action>
  <verify>
    <automated>cd /Users/richardhightower/clients/spillwave/src/agent-brain/agent-brain-server && poetry run pytest tests/unit/storage/test_vector_store_deduplication.py -x -v</automated>
  </verify>
  <done>All 5 tests pass. Duplicate IDs are removed before ChromaDB/PostgreSQL calls. Warning logged. Non-duplicate batches unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Full test suite validation</name>
  <files>
    agent-brain-server/agent_brain_server/storage/vector_store.py,
    agent-brain-server/agent_brain_server/storage/postgres/backend.py
  </files>
  <action>
    Run `task before-push` from the repo root to verify:
    1. Black formatting passes on all modified files
    2. Ruff linting passes
    3. mypy type checking passes (the deduplication uses only stdlib types already imported)
    4. ALL existing tests pass (no regressions)
    5. New deduplication tests pass

    If any check fails, fix the issue:
    - Line too long: run `black` on the file
    - Type error: ensure `seen` dict typing matches existing `Any` imports
    - Import order: run `ruff check --fix`
  </action>
  <verify>
    <automated>cd /Users/richardhightower/clients/spillwave/src/agent-brain && task before-push</automated>
  </verify>
  <done>`task before-push` exits with code 0. All existing tests pass. No regressions.</done>
</task>

</tasks>

<verification>
1. `cd agent-brain-server && poetry run pytest tests/unit/storage/test_vector_store_deduplication.py -x -v` — all 5 deduplication tests pass
2. `cd agent-brain-server && poetry run pytest -x -q` — full server test suite passes
3. `task before-push` — format, lint, typecheck, all tests pass
</verification>

<success_criteria>
- Batch with duplicate chunk IDs is deduplicated before ChromaDB upsert (last-occurrence wins)
- Warning logged with count and sample IDs when duplicates detected
- PostgreSQL backend has same deduplication guard
- No data loss for batches without duplicates
- All existing tests continue to pass
- `task before-push` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/21-fix-duplicate-chunk-id-crash-during-indexing/21-01-SUMMARY.md`
</output>
