---
phase: 25-setup-wizard-coverage-gaps-graphrag-opt-in-bm25-postgresql-awareness-search-mode-education
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - agent-brain-plugin/skills/configuring-agent-brain/SKILL.md
  - agent-brain-plugin/skills/configuring-agent-brain/references/configuration-guide.md
autonomous: true
requirements:
  - WIZARD-GAP-03
  - WIZARD-GAP-04
  - WIZARD-GAP-05

must_haves:
  truths:
    - "SKILL.md has a 'Caching' section (renamed from 'Embedding Cache Tuning') that covers both embedding and query caches"
    - "SKILL.md Query Mode Selection table notes that graph and multi modes require ChromaDB backend (not just GraphRAG)"
    - "configuration-guide.md has QUERY_CACHE_TTL and QUERY_CACHE_MAX_SIZE in the environment variables table"
    - "configuration-guide.md PostgreSQL storage section notes that BM25 is replaced by tsvector full-text search"
    - "configuration-guide.md has a 'Query Cache' configuration subsection"
  artifacts:
    - path: "agent-brain-plugin/skills/configuring-agent-brain/SKILL.md"
      provides: "Complete caching section covering both caches + GraphRAG/PostgreSQL compatibility note in mode table"
      contains: "Query cache"
    - path: "agent-brain-plugin/skills/configuring-agent-brain/references/configuration-guide.md"
      provides: "Query cache env vars + PostgreSQL BM25 note"
      contains: "QUERY_CACHE_TTL"
  key_links:
    - from: "SKILL.md Query Mode Selection table"
      to: "ChromaDB backend requirement"
      via: "'Requires GraphRAG + ChromaDB' column"
      pattern: "ChromaDB"
    - from: "configuration-guide.md Storage Backend"
      to: "PostgreSQL BM25 note"
      via: "Note after postgres config block"
      pattern: "tsvector"
---

<objective>
Update the configuring-agent-brain skill to close two documentation gaps:

1. **Query cache undocumented**: SKILL.md and configuration-guide.md document the embedding cache but say nothing about the query cache (Phase 17). Add query cache to the caching section of both files.

2. **BM25/PostgreSQL not documented**: Neither file mentions that PostgreSQL replaces the disk-based BM25 index with tsvector full-text search. Add a note in the PostgreSQL storage section of both files.

3. **GraphRAG mode table missing ChromaDB requirement**: The Query Mode Selection table in SKILL.md says `graph` and `multi` require GraphRAG but doesn't mention they also require ChromaDB backend. Add this constraint.

Purpose: A user who reads the skill documentation after setup should have complete information about caching behavior and PostgreSQL/BM25 trade-offs without needing to read server source code.

Output: Updated SKILL.md and configuration-guide.md with query cache docs, PostgreSQL BM25 note, and GraphRAG+ChromaDB mode constraint.
</objective>

<execution_context>
@/Users/richardhightower/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardhightower/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-setup-wizard-coverage-gaps-graphrag-opt-in-bm25-postgresql-awareness-search-mode-education/25-RESEARCH.md
@agent-brain-plugin/skills/configuring-agent-brain/SKILL.md
@agent-brain-plugin/skills/configuring-agent-brain/references/configuration-guide.md

Key facts from research:
- Query cache (Phase 17): TTL-based (default 300s), max 256 entries, excludes graph/multi modes, invalidated on reindex
- Embedding cache (Phase 16): aiosqlite two-layer LRU, auto-enabled, 1000 mem entries + 500MB disk
- PostgreSQL keyword search: uses tsvector/websearch_to_tsquery (not rank-bm25), same user interface
- GraphRAG incompatibility with PostgreSQL: hard-coded check in query_service.py raises ValueError
- QUERY_CACHE_TTL and QUERY_CACHE_MAX_SIZE are in settings.py but not in any plugin doc
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update SKILL.md — add query cache, fix mode table, add PostgreSQL BM25 note</name>
  <files>agent-brain-plugin/skills/configuring-agent-brain/SKILL.md</files>
  <action>
Make three targeted changes to SKILL.md:

**Change 1: Rename "Embedding Cache Tuning" section to "Caching" and add query cache content.**

Find the "Embedding Cache Tuning" section header (currently in the Environment Variables Reference section). Rename it to "### Caching" and add a query cache subsection after the existing embedding cache content:

```markdown
### Query Cache

The query cache is **automatic** — no setup required. Identical queries within
the TTL window return instantly without hitting storage.

- **`graph` and `multi` modes bypass the cache** — each call reaches storage
  for fresh results.
- Cache is **invalidated on every completed reindex job** (file watcher or manual).
- Configurable via environment variables (see Configuration Guide for details):
  - `QUERY_CACHE_TTL` — cache TTL in seconds (default: 300, i.e., 5 minutes)
  - `QUERY_CACHE_MAX_SIZE` — max cached query results (default: 256)
```

**Change 2: Update Query Mode Selection table.**

Find the Query Mode Selection table in SKILL.md. The current table has a "Requires GraphRAG" column. Update the header and rows for `graph` and `multi` to clarify the ChromaDB requirement:

Change column header from `Requires GraphRAG` to `Requirements`.

Update rows:
- `hybrid` → Requirements: None
- `semantic` → Requirements: None
- `bm25` → Requirements: None
- `graph` → Requirements: GraphRAG + ChromaDB backend
- `multi` → Requirements: GraphRAG + ChromaDB backend

Add a note below the table:
```
**Note**: `graph` and `multi` modes are not available with PostgreSQL backend.
GraphRAG uses an in-memory/Kuzu graph store that is separate from the vector
store — it currently integrates only with ChromaDB.
```

**Change 3: Add PostgreSQL BM25 note to Storage Backend Configuration section.**

Find the "GraphRAG Configuration" section. In the table of `graphrag.*` env vars, there's already content about PostgreSQL. Find the section about `storage.backend` and add a note:

Actually, look for where `storage.backend` is discussed in the wizard questions table near "Step 4 | Storage Backend | `storage.backend`". After the storage backend row in the wizard table, add a note:

```
**PostgreSQL + BM25**: When `storage.backend: "postgres"` is selected, the
disk-based BM25 index is replaced by PostgreSQL's built-in full-text search
(`tsvector` + `websearch_to_tsquery`). The `--mode bm25` command works
identically from the user's perspective. Language is configurable via
`storage.postgres.language` (default: `"english"`).
```

If there is no nearby storage backend section, add this note at the end of the "Setup Wizard" section, just before "## Prerequisites".
  </action>
  <verify>
    <automated>grep -n "Query cache\|query cache\|QUERY_CACHE" /Users/richardhightower/clients/spillwave/src/agent-brain/agent-brain-plugin/skills/configuring-agent-brain/SKILL.md && grep -n "ChromaDB backend\|ChromaDB" /Users/richardhightower/clients/spillwave/src/agent-brain/agent-brain-plugin/skills/configuring-agent-brain/SKILL.md | grep -i "mode\|graph\|multi" && grep -n "tsvector" /Users/richardhightower/clients/spillwave/src/agent-brain/agent-brain-plugin/skills/configuring-agent-brain/SKILL.md</automated>
  </verify>
  <done>SKILL.md contains: (1) query cache documentation with QUERY_CACHE_TTL and QUERY_CACHE_MAX_SIZE references, (2) GraphRAG mode table updated with ChromaDB requirement for graph/multi modes, (3) PostgreSQL BM25/tsvector note</done>
</task>

<task type="auto">
  <name>Task 2: Update configuration-guide.md — add query cache env vars and PostgreSQL BM25 note</name>
  <files>agent-brain-plugin/skills/configuring-agent-brain/references/configuration-guide.md</files>
  <action>
Make two targeted additions to configuration-guide.md:

**Addition 1: Add QUERY_CACHE_TTL and QUERY_CACHE_MAX_SIZE to environment variables table.**

Find the "Environment Variables Reference" table. Add two new rows after the existing embedding cache variables:

```markdown
| `QUERY_CACHE_TTL` | No | 300 | Query cache TTL in seconds (0 = disabled) |
| `QUERY_CACHE_MAX_SIZE` | No | 256 | Max number of cached query results |
```

**Addition 2: Add Query Cache configuration subsection.**

After the "GraphRAG Configuration" section (or after "GraphRAG in config.yaml" and "GraphRAG via Environment Variables"), add a new "## Query Cache Configuration" section:

```markdown
## Query Cache Configuration

The query cache stores identical query results for a configurable TTL window.
It is auto-enabled — no setup required.

### Behavior

- Identical queries within the TTL return instantly without hitting storage
- Cache is invalidated when any reindex job completes (watcher or manual)
- `graph` and `multi` query modes are **never cached** — each call reaches storage
- Cache is in-memory and does not persist across server restarts

### Environment Variables

| Variable | Default | Description |
|----------|---------|-------------|
| `QUERY_CACHE_TTL` | `300` | Cache TTL in seconds. Set to `0` to disable. |
| `QUERY_CACHE_MAX_SIZE` | `256` | Maximum cached query results (LRU eviction) |

### Example

```bash
# Extend cache TTL to 10 minutes for stable codebases
export QUERY_CACHE_TTL=600

# Increase cache size for large query workloads
export QUERY_CACHE_MAX_SIZE=512
```

### Disable Query Cache

```bash
export QUERY_CACHE_TTL=0
```
```

**Addition 3: Add PostgreSQL BM25 note to Storage Backend Configuration section.**

Find the "Storage Backend Configuration" section. After the PostgreSQL YAML config block (with `host`, `port`, `language`, `hnsw_m` etc.), add a note:

```markdown
**BM25 and Full-Text Search with PostgreSQL**

When using the PostgreSQL backend, the disk-based BM25 index is replaced by
PostgreSQL's built-in full-text search (`tsvector` + `websearch_to_tsquery`).

- `--mode bm25` queries use `ts_rank` scoring with `websearch_to_tsquery` syntax
- Scores are normalized to 0-1 to match ChromaDB BM25 output format
- The `storage.postgres.language` setting (default: `"english"`) controls the
  tsvector language configuration
- No BM25 configuration or index files are needed with PostgreSQL
```
  </action>
  <verify>
    <automated>grep -n "QUERY_CACHE_TTL\|QUERY_CACHE_MAX_SIZE" /Users/richardhightower/clients/spillwave/src/agent-brain/agent-brain-plugin/skills/configuring-agent-brain/references/configuration-guide.md && grep -n "tsvector\|BM25.*PostgreSQL\|PostgreSQL.*BM25" /Users/richardhightower/clients/spillwave/src/agent-brain/agent-brain-plugin/skills/configuring-agent-brain/references/configuration-guide.md</automated>
  </verify>
  <done>configuration-guide.md contains: (1) QUERY_CACHE_TTL and QUERY_CACHE_MAX_SIZE in env vars table, (2) Query Cache Configuration section with TTL/size settings and example, (3) PostgreSQL BM25/tsvector note in Storage Backend section</done>
</task>

</tasks>

<verification>
Verify all three files have the expected content:
```bash
echo "=== SKILL.md checks ===" && \
grep -c "Query cache\|QUERY_CACHE" /Users/richardhightower/clients/spillwave/src/agent-brain/agent-brain-plugin/skills/configuring-agent-brain/SKILL.md && \
grep -c "tsvector" /Users/richardhightower/clients/spillwave/src/agent-brain/agent-brain-plugin/skills/configuring-agent-brain/SKILL.md && \
echo "=== configuration-guide.md checks ===" && \
grep -c "QUERY_CACHE_TTL" /Users/richardhightower/clients/spillwave/src/agent-brain/agent-brain-plugin/skills/configuring-agent-brain/references/configuration-guide.md && \
grep -c "tsvector" /Users/richardhightower/clients/spillwave/src/agent-brain/agent-brain-plugin/skills/configuring-agent-brain/references/configuration-guide.md
```

Each count should be >= 1.
</verification>

<success_criteria>
- SKILL.md caching section covers both embedding and query caches with QUERY_CACHE_TTL/MAX_SIZE references
- SKILL.md Query Mode Selection table notes that graph and multi require ChromaDB backend (not just GraphRAG)
- SKILL.md has a PostgreSQL BM25/tsvector note
- configuration-guide.md Environment Variables table includes QUERY_CACHE_TTL and QUERY_CACHE_MAX_SIZE
- configuration-guide.md has a "Query Cache Configuration" section
- configuration-guide.md Storage Backend section notes BM25 is replaced by tsvector for PostgreSQL
- All changes are additive — no existing content is removed
</success_criteria>

<output>
After completion, create `.planning/phases/25-setup-wizard-coverage-gaps-graphrag-opt-in-bm25-postgresql-awareness-search-mode-education/25-02-SUMMARY.md`
</output>
