---
phase: 22-restore-setup-wizard-with-full-configuration-prompts
plan: 02
type: execute
wave: 2
depends_on:
  - 22-01
files_modified:
  - agent-brain-server/tests/test_plugin_wizard_spec.py
autonomous: true
requirements:
  - WIZARD-06

must_haves:
  truths:
    - "Removing any wizard AskUserQuestion block from agent-brain-setup.md causes test failure"
    - "Removing storage backend mention causes test failure"
    - "Removing GraphRAG mention causes test failure"
    - "Removing query mode mention causes test failure"
    - "Regression test runs as part of task before-push"
    - "Developer running poetry run pytest in agent-brain-server sees wizard regression tests discovered and passing"
  artifacts:
    - path: "agent-brain-server/tests/test_plugin_wizard_spec.py"
      provides: "Regression prevention tests for wizard content"
      min_lines: 40
      contains: "test_setup_wizard"
  key_links:
    - from: "agent-brain-server/tests/test_plugin_wizard_spec.py"
      to: "agent-brain-plugin/commands/agent-brain-setup.md"
      via: "Reads file content and asserts required sections present"
      pattern: "agent-brain-setup\\.md"
    - from: "agent-brain-server/tests/test_plugin_wizard_spec.py"
      to: "agent-brain-plugin/commands/agent-brain-config.md"
      via: "Reads file content and asserts required sections present"
      pattern: "agent-brain-config\\.md"
---

<objective>
Create regression prevention tests that assert all required wizard sections are present in the plugin command markdown files. If a future refactor silently removes an AskUserQuestion block, these tests fail before merge. This is the regression test requirement (WIZARD-06) — the automated safety net that prevents future wizard degradation.

Purpose: The wizard has been incomplete before because there was no automated check enforcing its content structure. This plan adds that safety net.

Output: `test_plugin_wizard_spec.py` with assertions covering all 5 wizard configuration dimensions.
</objective>

<execution_context>
@/Users/richardhightower/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardhightower/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/22-restore-setup-wizard-with-full-configuration-prompts/22-RESEARCH.md
@.planning/phases/22-restore-setup-wizard-with-full-configuration-prompts/22-01-SUMMARY.md

@agent-brain-plugin/commands/agent-brain-setup.md
@agent-brain-plugin/commands/agent-brain-config.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create regression test for wizard content</name>
  <files>agent-brain-server/tests/test_plugin_wizard_spec.py</files>
  <behavior>
    - test_setup_wizard_asks_embedding_provider: agent-brain-setup.md contains "embedding" and "AskUserQuestion" (or "provider" in embedding context)
    - test_setup_wizard_asks_summarization_provider: agent-brain-setup.md contains "summarization" or "Summarization"
    - test_setup_wizard_asks_storage_backend: agent-brain-setup.md contains "ChromaDB" and "PostgreSQL"
    - test_setup_wizard_asks_graphrag: agent-brain-setup.md contains "GraphRAG" or "graphrag"
    - test_setup_wizard_asks_query_mode: agent-brain-setup.md contains "query mode" or "Query Mode" (case-insensitive)
    - test_setup_wizard_has_multiple_ask_blocks: agent-brain-setup.md contains at least 5 occurrences of "AskUserQuestion"
    - test_config_wizard_has_provider_selection: agent-brain-config.md contains "AskUserQuestion"
    - test_config_wizard_mentions_chromadb: agent-brain-config.md contains "ChromaDB"
    - test_config_wizard_mentions_postgresql: agent-brain-config.md contains "PostgreSQL"
    - test_config_wizard_mentions_ollama: agent-brain-config.md contains "Ollama"
  </behavior>
  <action>
Create `agent-brain-server/tests/test_plugin_wizard_spec.py` with pytest tests that read the plugin command markdown files and assert required wizard sections are present.

Use `Path(__file__).parents[2] / "agent-brain-plugin"` to locate the plugin directory (test file is at `agent-brain-server/tests/test_plugin_wizard_spec.py`, plugin is at `agent-brain-plugin/` which is two levels up from `agent-brain-server/tests/`).

Structure:
```python
"""Regression tests for Agent Brain setup wizard content.

These tests ensure that required interactive wizard prompts remain present
in the plugin command markdown files. If a future refactor silently removes
a wizard step, these tests fail before merge.
"""
import re
from pathlib import Path

import pytest

PLUGIN_ROOT = Path(__file__).parents[2] / "agent-brain-plugin"
SETUP_CMD = PLUGIN_ROOT / "commands" / "agent-brain-setup.md"
CONFIG_CMD = PLUGIN_ROOT / "commands" / "agent-brain-config.md"
```

Each test reads the file content and uses simple string assertions or `re.search` for case-insensitive checks. Error messages must be descriptive: "Wizard regression detected — {filename} is missing required section: {what}. Restore the {dimension} wizard step."

Include a `@pytest.fixture` that skips all tests if the plugin directory does not exist (graceful degradation for CI environments that only check out agent-brain-server).

Run tests with: `cd agent-brain-server && poetry run pytest tests/test_plugin_wizard_spec.py -v`

Verify all tests pass (they should, since Plan 01 already added the wizard content).
  </action>
  <verify>
    <automated>cd agent-brain-server && poetry run pytest tests/test_plugin_wizard_spec.py -v</automated>
  </verify>
  <done>All 10 regression tests pass. Tests cover: embedding provider, summarization provider, storage backend, GraphRAG, query mode presence in setup wizard; AskUserQuestion block count >= 5; and config command coverage for providers, ChromaDB, PostgreSQL, Ollama.</done>
</task>

</tasks>

<verification>
- `cd agent-brain-server && poetry run pytest tests/test_plugin_wizard_spec.py -v` — all 10 tests pass
- `cd agent-brain-server && poetry run pytest` — full test suite still passes (no regressions)
- `cd agent-brain-server && poetry run mypy agent_brain_server` — type checking passes
- `cd agent-brain-server && poetry run ruff check agent_brain_server tests` — linting passes
</verification>

<success_criteria>
- test_plugin_wizard_spec.py exists with >= 10 test functions
- All tests pass when run against the wizard content from Plan 01
- Removing any wizard section from agent-brain-setup.md causes at least one test to fail
- Tests integrate with existing pytest discovery (no special config needed)
</success_criteria>

<output>
After completion, create `.planning/phases/22-restore-setup-wizard-with-full-configuration-prompts/22-02-SUMMARY.md`
</output>
