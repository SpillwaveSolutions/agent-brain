---
phase: 40-active-doc-path-consistency-and-flow-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/DEVELOPERS_GUIDE.md
  - docs/ARCHITECTURE.md
  - docs/SETUP_PLAYGROUND.md
autonomous: true
requirements:
  - CFGDOC-01
  - GUIDE-02
  - GUIDE-03
must_haves:
  truths:
    - "Active setup/architecture docs no longer reference `.claude/agent-brain/` in canonical instructions."
    - "All project-level state/config path guidance in these docs uses `.agent-brain/`."
    - "Updated docs remain aligned with existing CONFIGURATION.md, QUICK_START.md, and PLUGIN_GUIDE.md path conventions."
  artifacts:
    - path: docs/DEVELOPERS_GUIDE.md
      provides: Per-project state and config path guidance aligned to `.agent-brain/`
      contains: ".agent-brain/config.json"
    - path: docs/ARCHITECTURE.md
      provides: Architecture implementation path aligned to current state-dir model
      contains: ".agent-brain/"
    - path: docs/SETUP_PLAYGROUND.md
      provides: Setup examples and precedence table aligned to `.agent-brain/config.yaml`
      contains: ".agent-brain/config.yaml"
  key_links:
    - from: docs/DEVELOPERS_GUIDE.md
      to: docs/QUICK_START.md
      via: setup flow path consistency
      pattern: "\\.agent-brain/"
    - from: docs/ARCHITECTURE.md
      to: docs/CONFIGURATION.md
      via: state directory convention consistency
      pattern: "Implementation.*\\.agent-brain/"
    - from: docs/SETUP_PLAYGROUND.md
      to: docs/CONFIGURATION.md
      via: config file location and precedence consistency
      pattern: "\\.agent-brain/config\\.yaml"
---

<objective>
Normalize active setup and architecture documentation to use `.agent-brain/` as the canonical per-project state path and remove stale `.claude/agent-brain/` references.

Purpose: Users following setup docs and developers reading architecture docs should see one consistent path model that matches current runtime behavior.
Output: Updated active docs with no stale path references in the targeted Phase 40 scope.
</objective>

<execution_context>
@/Users/richardhightower/clients/spillwave/src/agent-brain/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/richardhightower/clients/spillwave/src/agent-brain/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@docs/CONFIGURATION.md
@docs/QUICK_START.md
@docs/PLUGIN_GUIDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace stale state-dir path references in DEVELOPERS_GUIDE and ARCHITECTURE</name>
  <files>docs/DEVELOPERS_GUIDE.md, docs/ARCHITECTURE.md</files>
  <read_first>
    - docs/DEVELOPERS_GUIDE.md
    - docs/ARCHITECTURE.md
    - docs/QUICK_START.md
    - docs/CONFIGURATION.md
  </read_first>
  <action>In `docs/DEVELOPERS_GUIDE.md`, replace the two stale references at the current lines matching `Each project stores its state in .claude/agent-brain/` and `Project config (.claude/agent-brain/config.json)` with `.agent-brain/` and `.agent-brain/config.json`. In `docs/ARCHITECTURE.md`, update the implementation bullet that currently says `.claude/agent-brain/` to `.agent-brain/`. Keep all surrounding behavioral claims unchanged; this task is path normalization only.</action>
  <acceptance_criteria>
    - `grep -n "\\.claude/agent-brain/" docs/DEVELOPERS_GUIDE.md docs/ARCHITECTURE.md` returns no matches.
    - `docs/DEVELOPERS_GUIDE.md` contains `.agent-brain/` in the per-project state section.
    - `docs/DEVELOPERS_GUIDE.md` contains `.agent-brain/config.json` in the init flow section.
    - `docs/ARCHITECTURE.md` contains `.agent-brain/` in the implementation line for per-project state storage.
  </acceptance_criteria>
  <verify>
    <automated>grep -n "\.claude/agent-brain/" docs/DEVELOPERS_GUIDE.md docs/ARCHITECTURE.md</automated>
  </verify>
  <done>DEVELOPERS_GUIDE and ARCHITECTURE use `.agent-brain/` consistently with no stale `.claude/agent-brain/` references.</done>
</task>

<task type="auto">
  <name>Task 2: Normalize SETUP_PLAYGROUND examples, data-path notes, and config precedence table</name>
  <files>docs/SETUP_PLAYGROUND.md</files>
  <read_first>
    - docs/SETUP_PLAYGROUND.md
    - docs/CONFIGURATION.md
    - docs/ARCHITECTURE.md
  </read_first>
  <action>In `docs/SETUP_PLAYGROUND.md`, replace every stale `.claude/agent-brain/config.yaml` and `.claude/agent-brain/chroma_db/` reference in code blocks, comments, prose notes, and the configuration precedence table with `.agent-brain/config.yaml` and `.agent-brain/chroma_db/` respectively. Preserve command intent and YAML payloads; only update path tokens. In the precedence table near the end, set rank 1 path to `.agent-brain/config.yaml` and keep the user-wide `~/.agent-brain/config.yaml` row unchanged.</action>
  <acceptance_criteria>
    - `grep -n "\\.claude/agent-brain/" docs/SETUP_PLAYGROUND.md` returns no matches.
    - `docs/SETUP_PLAYGROUND.md` contains `.agent-brain/config.yaml` in all config-file labels and heredoc examples.
    - `docs/SETUP_PLAYGROUND.md` contains `.agent-brain/chroma_db/` in the data location note.
    - The precedence table includes rank 1 as `.agent-brain/config.yaml` and rank 2 as `~/.agent-brain/config.yaml`.
  </acceptance_criteria>
  <verify>
    <automated>grep -n "\.claude/agent-brain/" docs/SETUP_PLAYGROUND.md</automated>
  </verify>
  <done>SETUP_PLAYGROUND no longer shows `.claude/agent-brain/` in active setup instructions, examples, or precedence guidance.</done>
</task>

</tasks>

<verification>
Run:

1. `grep -n "\.claude/agent-brain/" docs/DEVELOPERS_GUIDE.md docs/ARCHITECTURE.md docs/SETUP_PLAYGROUND.md`
2. `grep -n "\.agent-brain/" docs/DEVELOPERS_GUIDE.md docs/ARCHITECTURE.md docs/SETUP_PLAYGROUND.md`

Expected:
- Command 1 returns no matches.
- Command 2 returns matches in all three files.
</verification>

<success_criteria>
1. No stale `.claude/agent-brain/` references remain in Phase 40 active-doc sweep targets.
2. Developer/setup docs in this scope consistently present `.agent-brain/` as canonical project state directory.
3. CFGDOC-01/GUIDE-02/GUIDE-03 path consistency blockers are removed in targeted docs.
</success_criteria>

<output>
After completion, create `.planning/phases/40-active-doc-path-consistency-and-flow-closure/40-01-SUMMARY.md`
</output>
