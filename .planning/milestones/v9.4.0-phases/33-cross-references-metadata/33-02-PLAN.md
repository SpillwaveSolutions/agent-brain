---
phase: 33-cross-references-metadata
plan: 02
type: execute
wave: 2
depends_on:
  - 33-01
files_modified:
  - docs/*.md
  - agent-brain-plugin/commands/*.md
  - agent-brain-plugin/skills/*/references/*.md
  - agent-brain-plugin/agents/*.md
  - README.md
  - CLAUDE.md
  - AGENTS.md
  - .claude/CLAUDE.md
autonomous: true
requirements:
  - XREF-03

must_haves:
  truths:
    - "Every audited documentation file has a last_validated frontmatter field set to 2026-03-16"
    - "Files that already have YAML frontmatter get the field added/updated within existing frontmatter"
    - "Files without YAML frontmatter get a new frontmatter block prepended"
  artifacts:
    - path: "scripts/add_audit_metadata.py"
      provides: "Script to add last_validated frontmatter to markdown files"
      min_lines: 30
  key_links:
    - from: "scripts/add_audit_metadata.py"
      to: "docs/*.md"
      via: "frontmatter insertion"
      pattern: "last_validated"
---

<objective>
Add `last_validated: 2026-03-16` frontmatter metadata to every audited documentation file, establishing a traceable record of when each doc was verified accurate.

Purpose: Enable future audits to identify stale docs by checking the `last_validated` date against feature release dates.
Output: All ~78 audited markdown files carry `last_validated` frontmatter.
</objective>

<execution_context>
@/Users/richardhightower/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardhightower/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/33-cross-references-metadata/33-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build and run audit metadata script</name>
  <files>scripts/add_audit_metadata.py</files>
  <action>
Create a Python script `scripts/add_audit_metadata.py` that:

1. Accepts an optional `--date` argument (defaults to today's date in YYYY-MM-DD format) and optional `--dry-run` flag
2. Operates on the same audited doc set as `check_doc_links.py`:
   - `docs/*.md` (excluding `docs/plans/` and `docs/design/`)
   - `agent-brain-plugin/commands/*.md`
   - `agent-brain-plugin/skills/*/references/*.md`
   - `agent-brain-plugin/agents/*.md`
   - `README.md`, `CLAUDE.md`, `AGENTS.md`, `.claude/CLAUDE.md`
3. For each file:
   - If file starts with `---\n` (has YAML frontmatter):
     - Parse the frontmatter block (between first `---` and second `---`)
     - If `last_validated` key exists, update its value to the audit date
     - If `last_validated` key does not exist, add it as the last field before the closing `---`
   - If file does NOT have YAML frontmatter:
     - Prepend a new frontmatter block: `---\nlast_validated: YYYY-MM-DD\n---\n\n` before existing content
4. In `--dry-run` mode, print what would be changed without writing files
5. Print a summary: N files updated, N files already current, N files with new frontmatter

Run the script with `--date 2026-03-16`:
```bash
python scripts/add_audit_metadata.py --date 2026-03-16
```

IMPORTANT: Do NOT modify files in `docs/plans/` or `docs/design/` (out of scope).
IMPORTANT: Preserve all existing frontmatter fields — only add/update `last_validated`.
IMPORTANT: Handle edge cases: files with empty frontmatter (`---\n---`), files starting with `# heading` (no frontmatter), files with frontmatter containing colons in values.
  </action>
  <verify>
    <automated>cd /Users/richardhightower/clients/spillwave/src/agent-brain && python scripts/add_audit_metadata.py --date 2026-03-16 --dry-run 2>&1 | tail -5</automated>
  </verify>
  <done>Script runs in dry-run mode showing all files that will be updated. No errors.</done>
</task>

<task type="auto">
  <name>Task 2: Apply metadata and verify all audited docs have last_validated</name>
  <files>docs/*.md, agent-brain-plugin/**/*.md, README.md, CLAUDE.md, AGENTS.md, .claude/CLAUDE.md</files>
  <action>
1. Run the metadata script for real (not dry-run):
   ```bash
   python scripts/add_audit_metadata.py --date 2026-03-16
   ```

2. Verify every audited file has the frontmatter by running a verification grep:
   ```bash
   # For each audited file, confirm last_validated: 2026-03-16 appears
   ```

3. Spot-check 3-5 files to ensure:
   - Existing frontmatter fields are preserved (not deleted or reordered destructively)
   - The `last_validated` field is correctly formatted
   - File content below frontmatter is unchanged

4. Re-run the link checker from Plan 01 to confirm no links were broken by frontmatter changes:
   ```bash
   python scripts/check_doc_links.py | python -c "import json,sys; r=json.load(sys.stdin); print(f'Broken: {r[\"stats\"][\"broken_links\"]} links, {r[\"stats\"][\"broken_paths\"]} paths')"
   ```
  </action>
  <verify>
    <automated>cd /Users/richardhightower/clients/spillwave/src/agent-brain && python -c "
import glob, os
files = []
for g in ['docs/*.md', 'agent-brain-plugin/commands/*.md', 'agent-brain-plugin/skills/*/references/*.md', 'agent-brain-plugin/agents/*.md']:
    files.extend(glob.glob(g))
for f in ['README.md', 'CLAUDE.md', 'AGENTS.md', '.claude/CLAUDE.md']:
    if os.path.exists(f):
        files.append(f)
# Exclude out-of-scope
files = [f for f in files if '/plans/' not in f and '/design/' not in f]
missing = []
for f in files:
    content = open(f).read()
    if 'last_validated: 2026-03-16' not in content:
        missing.append(f)
if missing:
    print(f'FAIL: {len(missing)} files missing last_validated')
    for m in missing[:10]:
        print(f'  - {m}')
    exit(1)
else:
    print(f'OK: All {len(files)} audited files have last_validated: 2026-03-16')
"</automated>
  </verify>
  <done>Every audited documentation file contains `last_validated: 2026-03-16` in YAML frontmatter. Existing content and frontmatter fields preserved. Link check still passes.</done>
</task>

</tasks>

<verification>
Grep all audited files for `last_validated: 2026-03-16` — every file must match. Re-run `scripts/check_doc_links.py` to confirm no regressions from frontmatter changes.
</verification>

<success_criteria>
- Every audited doc file has `last_validated: 2026-03-16` in YAML frontmatter
- No existing frontmatter fields were lost or corrupted
- Link verification from Plan 01 still passes clean
</success_criteria>

<output>
After completion, create `.planning/phases/33-cross-references-metadata/33-02-SUMMARY.md`
</output>
