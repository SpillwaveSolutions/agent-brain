---
phase: 31-user-guides
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/USER_GUIDE.md
  - docs/QUICK_START.md
autonomous: true
requirements:
  - GUIDE-01
  - GUIDE-02

must_haves:
  truths:
    - "USER_GUIDE.md documents all 30 plugin commands including folders, inject, cache, types, and install-agent"
    - "USER_GUIDE.md has sections for folder management, file type presets, content injection, chunk eviction, file watcher, and embedding cache"
    - "QUICK_START.md installation steps reference current package names and work end-to-end"
    - "QUICK_START.md mentions multi-runtime install as an option"
  artifacts:
    - path: "docs/USER_GUIDE.md"
      provides: "Complete user guide reflecting v7-v9 features"
      contains: "agent-brain-folders"
    - path: "docs/QUICK_START.md"
      provides: "Working quick start guide"
      contains: "install-agent"
  key_links:
    - from: "docs/USER_GUIDE.md"
      to: "agent-brain-plugin/commands/"
      via: "command documentation references"
      pattern: "agent-brain-(folders|inject|cache|types|install)"
---

<objective>
Update USER_GUIDE.md and QUICK_START.md to accurately reflect all v7-v9 features.

Purpose: New users following these guides should encounter no stale instructions and see all current features documented.
Output: Updated docs/USER_GUIDE.md and docs/QUICK_START.md
</objective>

<execution_context>
@/Users/richardhightower/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardhightower/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@docs/USER_GUIDE.md
@docs/QUICK_START.md

Source of truth for commands (30 total):
@agent-brain-plugin/commands/agent-brain-folders.md
@agent-brain-plugin/commands/agent-brain-inject.md
@agent-brain-plugin/commands/agent-brain-cache.md
@agent-brain-plugin/commands/agent-brain-types.md
@agent-brain-plugin/commands/agent-brain-install-agent.md

Source of truth for v7-v9 features:
@agent-brain-server/agent_brain_server/services/folder_manager.py
@agent-brain-server/agent_brain_server/services/file_type_presets.py
@agent-brain-server/agent_brain_server/services/content_injector.py
@agent-brain-server/agent_brain_server/services/chunk_eviction_service.py
@agent-brain-server/agent_brain_server/services/file_watcher_service.py
@agent-brain-server/agent_brain_server/services/embedding_cache.py
@agent-brain-server/agent_brain_server/services/manifest_tracker.py
@agent-brain-cli/agent_brain_cli/commands/folders.py
@agent-brain-cli/agent_brain_cli/commands/inject.py
@agent-brain-cli/agent_brain_cli/commands/cache.py
@agent-brain-cli/agent_brain_cli/commands/types.py
@agent-brain-cli/agent_brain_cli/commands/install_agent.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update USER_GUIDE.md with v7-v9 features</name>
  <files>docs/USER_GUIDE.md</files>
  <action>
Read the current USER_GUIDE.md and the plugin command files to identify all gaps. Then update the guide with these specific changes:

1. **Fix command count**: Change "24 commands" to "30 commands" in the Overview table and anywhere else it appears.

2. **Add missing commands to Plugin Commands tables**. Add these sections/rows:

   Under "Server Commands" or a new "Index Management Commands" section:
   - `/agent-brain-folders` — Manage indexed folders (list, add, remove)
   - `/agent-brain-inject` — Inject content into the index
   - `/agent-brain-types` — File type presets for indexing (--include-type)
   - `/agent-brain-cache` — Manage embedding cache

   Under "Setup Commands":
   - `/agent-brain-install-agent` — Install for different AI runtimes (was partially there under Runtime Installation)

3. **Add "Folder Management" section** (after Indexing, before Job Queue):
   - Explain `agent-brain folders list`, `agent-brain folders add <path>`, `agent-brain folders remove <path>`
   - Show how folders are tracked in the manifest for incremental re-indexing
   - Reference the file watcher integration

4. **Add "File Type Presets" section** (after Folder Management):
   - Explain `--include-type` flag on indexing commands (e.g., `--include-type python`, `--include-type web`)
   - List available presets by reading the `file_type_presets.py` source
   - Show examples: `agent-brain index . --include-type python`

5. **Add "Content Injection" section** (after File Type Presets):
   - Explain `agent-brain inject <content>` for adding arbitrary content to the index
   - Show use cases: injecting meeting notes, external API docs, etc.

6. **Add "Chunk Eviction" section** (after Content Injection):
   - Explain how stale chunks are evicted when files change or are removed
   - Reference manifest tracking

7. **Add "File Watcher" section** (after Chunk Eviction):
   - Explain `watch_mode` config (auto/off)
   - How the file watcher detects changes and triggers incremental re-indexing
   - Configuration via config.yaml

8. **Add "Embedding Cache" section** (after File Watcher):
   - Explain how embeddings are cached (aiosqlite) to avoid re-computation
   - Cache management commands via `/agent-brain-cache`
   - Show `agent-brain cache stats`, `agent-brain cache clear`

Read each referenced source file to get accurate CLI flags, config keys, and behavior descriptions. Do NOT invent features — only document what exists in the source code.
  </action>
  <verify>
    <automated>grep -c "agent-brain-folders\|agent-brain-inject\|agent-brain-cache\|agent-brain-types\|Folder Management\|File Type Presets\|Content Injection\|Chunk Eviction\|File Watcher\|Embedding Cache" docs/USER_GUIDE.md | xargs test 10 -le</automated>
  </verify>
  <done>USER_GUIDE.md contains sections for all v7-v9 features: folder management, file type presets, content injection, chunk eviction, file watcher, embedding cache. All 30 commands are listed. No stale "24 commands" references remain.</done>
</task>

<task type="auto">
  <name>Task 2: Update QUICK_START.md with current installation and features</name>
  <files>docs/QUICK_START.md</files>
  <action>
Read the current QUICK_START.md and update it with these changes:

1. **Fix command count**: Change "24 commands" to "30 commands" wherever it appears.

2. **Update Step 6 (Index Your Documentation)**: Add examples using file type presets:
   ```
   /agent-brain-index . --include-type python
   ```
   Also mention folder management:
   ```
   /agent-brain-folders add ./src
   /agent-brain-folders add ./docs
   ```

3. **Add multi-runtime install note** after the main setup flow (before "All-in-One Setup"):
   Add a brief section "Install for Other AI Runtimes" showing:
   ```
   agent-brain install-agent --agent codex
   agent-brain install-agent --agent opencode
   agent-brain install-agent --agent gemini
   ```
   Keep it brief — point to USER_GUIDE.md for full details.

4. **Update Search Modes Quick Reference table**: Ensure it matches current command set (it currently looks correct but verify).

5. **Verify all referenced commands still exist** by cross-referencing with the plugin commands directory. Fix any stale references.

Read the plugin command files for install-agent and folders to get accurate syntax.
  </action>
  <verify>
    <automated>grep -c "30 commands\|install-agent\|include-type\|folders" docs/QUICK_START.md | xargs test 3 -le</automated>
  </verify>
  <done>QUICK_START.md references 30 commands, includes file type preset examples, mentions folder management, and has multi-runtime install section. All commands referenced exist in the plugin.</done>
</task>

</tasks>

<verification>
- `grep "24 commands" docs/USER_GUIDE.md docs/QUICK_START.md` returns no matches (stale count removed)
- `grep "30 commands" docs/USER_GUIDE.md docs/QUICK_START.md` returns matches in both files
- `grep -c "Folder Management\|File Type Presets\|Content Injection\|Chunk Eviction\|File Watcher\|Embedding Cache" docs/USER_GUIDE.md` returns 6+ (all sections present)
- `grep "install-agent" docs/QUICK_START.md` returns matches
</verification>

<success_criteria>
- USER_GUIDE.md documents all 30 plugin commands with no stale count
- USER_GUIDE.md has dedicated sections for: folder management, file type presets, content injection, chunk eviction, file watcher, embedding cache
- QUICK_START.md installation steps are current and include multi-runtime option
- All command references in both files correspond to actual plugin command files
</success_criteria>

<output>
After completion, create `.planning/phases/31-user-guides/31-01-SUMMARY.md`
</output>
