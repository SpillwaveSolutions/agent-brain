---
phase: 31-user-guides
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/PLUGIN_GUIDE.md
  - docs/POSTGRESQL_SETUP.md
  - docs/GRAPHRAG_GUIDE.md
autonomous: true
requirements:
  - GUIDE-03
  - GUIDE-04
  - GUIDE-05

must_haves:
  truths:
    - "PLUGIN_GUIDE.md lists all 30 commands including folders, inject, cache, types, and install-agent"
    - "PLUGIN_GUIDE.md agent and skill descriptions match current implementations"
    - "POSTGRESQL_SETUP.md Docker Compose instructions reference the correct template file and image"
    - "GRAPHRAG_GUIDE.md query examples use accurate CLI syntax and config keys"
  artifacts:
    - path: "docs/PLUGIN_GUIDE.md"
      provides: "Complete plugin reference with all 30 commands"
      contains: "agent-brain-folders"
    - path: "docs/POSTGRESQL_SETUP.md"
      provides: "Working PostgreSQL setup instructions"
      contains: "docker-compose.postgres.yml"
    - path: "docs/GRAPHRAG_GUIDE.md"
      provides: "Accurate GraphRAG guide"
      contains: "ENABLE_GRAPH_INDEX"
  key_links:
    - from: "docs/PLUGIN_GUIDE.md"
      to: "agent-brain-plugin/commands/"
      via: "command documentation references"
      pattern: "agent-brain-(folders|inject|cache|types)"
    - from: "docs/POSTGRESQL_SETUP.md"
      to: "agent-brain-plugin/templates/docker-compose.postgres.yml"
      via: "template file reference"
      pattern: "docker-compose.postgres.yml"
---

<objective>
Update PLUGIN_GUIDE.md, POSTGRESQL_SETUP.md, and GRAPHRAG_GUIDE.md to accurately reflect current features and behavior.

Purpose: Users following these guides should find accurate command references, working Docker setup, and correct GraphRAG examples.
Output: Updated docs/PLUGIN_GUIDE.md, docs/POSTGRESQL_SETUP.md, docs/GRAPHRAG_GUIDE.md
</objective>

<execution_context>
@/Users/richardhightower/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardhightower/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@docs/PLUGIN_GUIDE.md
@docs/POSTGRESQL_SETUP.md
@docs/GRAPHRAG_GUIDE.md

Source of truth for commands (30 total — list all .md files in commands/):
@agent-brain-plugin/commands/agent-brain-folders.md
@agent-brain-plugin/commands/agent-brain-inject.md
@agent-brain-plugin/commands/agent-brain-cache.md
@agent-brain-plugin/commands/agent-brain-types.md
@agent-brain-plugin/commands/agent-brain-install-agent.md

Source of truth for agents:
@agent-brain-plugin/agents/search-assistant.md
@agent-brain-plugin/agents/research-assistant.md
@agent-brain-plugin/agents/setup-assistant.md

Source of truth for skills:
@agent-brain-plugin/skills/using-agent-brain/SKILL.md
@agent-brain-plugin/skills/configuring-agent-brain/SKILL.md

Source of truth for Docker template:
@agent-brain-plugin/templates/docker-compose.postgres.yml

Source of truth for GraphRAG config:
@agent-brain-server/agent_brain_server/config/settings.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update PLUGIN_GUIDE.md with all 30 commands and current features</name>
  <files>docs/PLUGIN_GUIDE.md</files>
  <action>
Read the current PLUGIN_GUIDE.md and ALL 30 plugin command .md files in agent-brain-plugin/commands/. Then update:

1. **Fix command count**: Change "24 commands" to "30 commands" in all occurrences (Installation section, Quick Setup section, etc.).

2. **Add missing command sections**. The following commands are missing from the guide and need their own subsections following the existing format (name, description, usage examples):

   Add a new "Index Management Commands" section (between Server Commands and Setup Commands):
   - `/agent-brain-folders` — Manage indexed folder list (list, add, remove tracked folders)
   - `/agent-brain-inject` — Inject arbitrary content into the index
   - `/agent-brain-types` — File type presets for indexing (show available presets, use with --include-type)
   - `/agent-brain-cache` — Manage embedding cache (stats, clear)
   - `/agent-brain-install-agent` — Install Agent Brain for other AI runtimes (claude, opencode, gemini, codex, skill-runtime)

   For each command, read the corresponding .md file in agent-brain-plugin/commands/ to get the accurate description, usage syntax, and options.

3. **Update the Skills section**: Read the current SKILL.md files for both skills. Update descriptions to mention v7-v9 features:
   - `using-agent-brain` skill should mention file watcher, embedding cache, folder management awareness
   - `configuring-agent-brain` skill should mention multi-runtime installation, embedding cache config

4. **Update the Agents section**: Read each agent .md file and verify the triggers and examples match. Update if the agent files have changed since the guide was written.

5. **Verify all referenced links** in the Reference Documentation table at the bottom still point to existing files. Fix any broken references.

Read each source file to get accurate information. Do NOT invent capabilities.
  </action>
  <verify>
    <automated>grep -c "agent-brain-folders\|agent-brain-inject\|agent-brain-cache\|agent-brain-types\|agent-brain-install-agent\|30 commands" docs/PLUGIN_GUIDE.md | xargs test 6 -le</automated>
  </verify>
  <done>PLUGIN_GUIDE.md lists all 30 commands with accurate descriptions. Missing commands (folders, inject, cache, types, install-agent) have dedicated subsections. Skills descriptions mention v7-v9 features. No stale "24 commands" references remain.</done>
</task>

<task type="auto">
  <name>Task 2: Verify and update POSTGRESQL_SETUP.md</name>
  <files>docs/POSTGRESQL_SETUP.md</files>
  <action>
Read the current POSTGRESQL_SETUP.md and verify against the actual Docker Compose template and server config:

1. **Verify template path**: Confirm `agent-brain-plugin/templates/docker-compose.postgres.yml` exists and the guide references it correctly.

2. **Read the Docker Compose template** and verify:
   - Image name matches (should be `pgvector/pgvector:pg16` or similar)
   - Default credentials match (user, password, database name)
   - Port mapping matches
   - Environment variable overrides match

3. **Verify config.yaml instructions**: Read the server's settings.py to confirm the YAML keys for PostgreSQL configuration (`storage.backend`, `storage.postgres.*`). Ensure the guide tells users the correct keys.

4. **Add config.yaml example** if missing — show the actual YAML block users need:
   ```yaml
   storage:
     backend: postgres
     postgres:
       host: localhost
       port: 5432
       database: agent_brain
       user: agent_brain
       password: agent_brain_dev
   ```
   Verify these field names match the Pydantic model in settings.py.

5. **Add DATABASE_URL override** documentation if the server supports it (check settings.py for DATABASE_URL env var support).

6. **Verify the "No Auto-Migration" note** is still accurate.

Read each referenced source file for accuracy.
  </action>
  <verify>
    <automated>grep -c "docker-compose.postgres.yml\|storage.backend\|pgvector" docs/POSTGRESQL_SETUP.md | xargs test 3 -le</automated>
  </verify>
  <done>POSTGRESQL_SETUP.md Docker Compose instructions match the actual template. Config keys match server settings schema. Users can follow the guide to get a working PostgreSQL backend.</done>
</task>

<task type="auto">
  <name>Task 3: Verify and update GRAPHRAG_GUIDE.md</name>
  <files>docs/GRAPHRAG_GUIDE.md</files>
  <action>
Read the current GRAPHRAG_GUIDE.md and verify against the server source code:

1. **Verify environment variables**: Read settings.py and graph-related source files. Confirm every env var listed in the guide (ENABLE_GRAPH_INDEX, GRAPH_STORE_TYPE, GRAPH_INDEX_PATH, GRAPH_USE_CODE_METADATA, GRAPH_USE_LLM_EXTRACTION, GRAPH_EXTRACTION_MODEL, GRAPH_MAX_TRIPLETS_PER_CHUNK, GRAPH_TRAVERSAL_DEPTH, GRAPH_RRF_K) actually exists in the codebase with those exact names.

2. **Verify config.yaml approach**: If the server supports config.yaml for GraphRAG settings (in addition to env vars), add a config.yaml example showing the YAML structure. Check settings.py for the field names.

3. **Verify CLI query syntax**: Confirm `agent-brain query "..." --mode graph` and `--mode multi` still work. Check the CLI query command source for accurate flag names.

4. **Verify status output format**: The guide shows a `graph_index` block in status output. Read the health model and router to verify the actual response shape matches.

5. **Check for new features**: Look for any GraphRAG features added in v7-v9 that aren't documented (e.g., new extraction methods, new graph store backends, new query options).

6. **Fix any inaccuracies** found during verification. If env var names changed, CLI flags changed, or response shapes changed, update the guide.

Read each source file for accuracy. Only fix what's wrong — don't rewrite sections that are already correct.
  </action>
  <verify>
    <automated>grep -c "ENABLE_GRAPH_INDEX\|GRAPH_STORE_TYPE\|--mode graph\|--mode multi" docs/GRAPHRAG_GUIDE.md | xargs test 4 -le</automated>
  </verify>
  <done>GRAPHRAG_GUIDE.md environment variables, CLI syntax, and status output match current source code. Query examples use accurate syntax. Any v7-v9 GraphRAG changes are documented.</done>
</task>

</tasks>

<verification>
- `grep "24 commands" docs/PLUGIN_GUIDE.md` returns no matches
- `grep "30 commands" docs/PLUGIN_GUIDE.md` returns matches
- `grep -c "agent-brain-folders\|agent-brain-inject\|agent-brain-cache\|agent-brain-types\|agent-brain-install-agent" docs/PLUGIN_GUIDE.md` returns 5+
- `grep "docker-compose.postgres.yml" docs/POSTGRESQL_SETUP.md` returns matches
- `grep "ENABLE_GRAPH_INDEX" docs/GRAPHRAG_GUIDE.md` returns matches
</verification>

<success_criteria>
- PLUGIN_GUIDE.md documents all 30 commands with no stale count, including dedicated sections for folders, inject, cache, types, install-agent
- POSTGRESQL_SETUP.md Docker Compose instructions match the actual template and produce a working backend
- GRAPHRAG_GUIDE.md env vars, CLI syntax, and examples match current source code behavior
</success_criteria>

<output>
After completion, create `.planning/phases/31-user-guides/31-02-SUMMARY.md`
</output>
